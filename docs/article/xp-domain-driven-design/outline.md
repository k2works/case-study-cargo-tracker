---
type: Article
title: "XP によるドメイン駆動設計の実践 — 執筆アウトライン"
description: "XP によるドメイン駆動設計の実践シリーズの執筆計画。章構成・通底する主題・引用元の対応。"
tags: [article, xp-domain-driven-design]
status: stable
generated: { by: human:kakimomokuri, at: 2026-08-13T02:17:02Z }
---

# XP によるドメイン駆動設計の実践 — 執筆アウトライン

本ドキュメントは記事シリーズ **「XP によるドメイン駆動設計の実践」** の計画書です。章立て・各章の主題・引用元・執筆順序を定めます。記事本文ではありません。

## 1. シリーズの位置づけ

| 項目 | 内容 |
| :--- | :--- |
| シリーズ名 | XP によるドメイン駆動設計の実践 |
| 記事ディレクトリ | `docs/article/xp-domain-driven-design/` |
| 参照元実装 | `docs/article/source/java-2/apps/`（`java/take-6`）＋ `e2e/manual/manual-screenshots.spec.js` |
| 参照元一次資料 | `docs/article/source/java-2/docs/`（開発戦略・計画・ふりかえり・ADR・設計・マニュアル） |
| 題材 | 国際貨物輸送管理システム（Cargo Tracker） |
| 章数 | 13（導入 1 ＋ 計画ゲーム 3 ＋ テクニカルプラクティス 4 ＋ チームプラクティス 4 ＋ 総括 1） |
| 実績規模 | 20 イテレーション／7 境界づけられたコンテキスト＋共有カーネル＋Security サブドメイン／ADR 25 本／テスト 1,578 件／v2.1.0 出荷済み |

### 既存 3 シリーズとの関係

`docs/article/index.md` には既に 3 シリーズがあります。本シリーズは 4 本目として並びます。

| シリーズ | 問い |
| :--- | :--- |
| モノリスアーキテクチャ実装比較 | 同じ設計方針を違うランタイムに載せると何が分かれるか |
| 関数型ドメインモデリング | 業務ルールをどこまで型に埋め込めるか |
| 実践 AI 駆動開発 | AI と組んだ開発で、何が品質を守り、何が守らなかったか |
| **XP によるドメイン駆動設計の実践（本シリーズ）** | **ドメインモデルは、どのプラクティスによって育つのか** |

**参照元は「実践 AI 駆動開発」と同じ `java/take-6` です。** 違うのは軸で、AI 駆動開発シリーズが**誰が書いたか**（開発者 1 名＋ AI エージェント）を扱うのに対し、本シリーズは**どういう規律で書いたか**を扱います。開発者が AI であっても人であっても同じように働く XP のプラクティスと、それがドメインモデルに与えた変化だけを追います。

そのため AI エージェントの体制・Skill 体系・自律実行の話は本シリーズでは扱わず、AI 駆動開発シリーズへのリンクで済ませます。

### この記事が扱わないこと

| 対象外 | 理由 |
| :--- | :--- |
| DDD の用語解説・パターンカタログ | 既刊の書籍が扱っており、本シリーズは 20 イテレーションの実績側から書くため |
| XP プラクティスの一般的な入門 | `docs/reference/エクストリームプログラミング.md` を参照すれば足りるため |
| 多言語比較 | モノリスアーキテクチャ実装比較シリーズが扱っているため |
| 型システムによる不正状態の排除 | 関数型ドメインモデリングシリーズが扱っているため |
| AI エージェントの運用・Skill 体系 | 実践 AI 駆動開発シリーズが扱っているため |

## 2. 通底する主題

20 イテレーションの計画・ふりかえり・ADR を、**「設計判断がいつ・何をきっかけに変わったか」**という視点で通読すると、共通する形が見えます。本シリーズの背骨はこれです。

| # | 主題 | 具体的に何が起きたか |
| :--- | :--- | :--- |
| M1 | **ドメインモデルは設計されるのではなく、イテレーションで育つ** | 7 つの BC は一度に立たなかった。Handling は ADR-010 で後から独立し、最後の BC（Estimation）が本格的に立ち上がったのは IT18 |
| M2 | **境界は最初から正しくない。動かして初めてずれが見える** | 共有カーネルを 2 要素に限定する判断（ADR-005）も、BC 横断イベント（ADR-009）も、越境の痛みが出てから決まった |
| M3 | **ユビキタス言語は、放っておくと実装から離れる** | 対訳表・Javadoc・遷移表が仕様を語り、実装が守っていない状態が繰り返し検出された |
| M4 | **設計の規則は、検査に落とさなければ守られない** | ADR-009 は 7 イテレーションのあいだ半分しか守られず、違反が 5 本増えていた |
| M5 | **リファクタリングの余地は、テストの厚みでしか買えない** | ADR-024 のドメインモデル分割は、テスト 1,500 件超と ArchUnit の契約があって初めて実行できた |

**DDD の戦術パターンを正しく適用したから良いモデルになった、という話にはしません。** 実際に効いたのは、短いイテレーション・テストファースト・リファクタリング・継続的インテグレーション・ふりかえりという XP 側の規律であり、DDD のパターンはその規律が働く**場所**を与えたにすぎない、というのが本シリーズの主張です。

### 業務領域の分割がイテレーションの単位になった

**本シリーズは、`design/domain-model.md` の業務領域の分割を元にイテレーションを進めたことを明記して解説します。**

`domain-model.md` は境界づけられたコンテキストを列挙するだけでなく、**差別化の度合いと業務ロジックの複雑さで業務領域を分類**しています（mermaid の quadrantChart）。この分類が、どの領域にどれだけ投資するか＝**どの順序でイテレーションを回すか**の判断根拠になっています。

| 業務領域 | 分類（差別化・複雑さ） | 主に扱ったイテレーション |
| :--- | :--- | :--- |
| Booking Context | **中核**（0.75, 0.82） | IT2・IT3・IT5〜IT12・IT15・IT16 |
| Tracking Context | **中核**（0.82, 0.68） | IT6〜IT8・IT10・IT11 |
| Routing Context | 中核寄り（0.62, 0.60） | IT3〜IT5・IT8・IT9・IT18 |
| Shipper Context | 一般（0.65, 0.28） | IT1・IT2・IT7・IT13 |
| Billing Context | **補完**（0.30, 0.65） | IT7・IT13〜IT15・IT19 |
| Estimation Context | 補完寄り（0.50, 0.35） | IT2・IT18 |
| Handling Context | ADR-010 で独立した BC に昇格 | IT6・IT7・IT11・IT12・IT16 |
| Security サブドメイン | 補完サブドメイン（ADR-007） | IT1・IT5・IT7〜IT9・IT15〜IT17 |
| Shared Domain | 汎用（0.18, 0.22） | 全期間（2 要素に固定 — ADR-005） |

読み取れることは次の通りです。

| 観察 | 内容 |
| :--- | :--- |
| **中核は長く触り続けた** | Booking は 20 イテレーション中 11 回、Tracking は 5 回登場する。**中核とは「一度で作り切らない領域」のこと**であり、分類は投資の総量ではなく**繰り返し回数**として現れた |
| **補完は後ろに寄せた** | Billing の本格着手は IT13、Estimation は IT18。分類が着手順を決めている |
| **序盤は最も単純な領域を選んだ** | IT1 は中核ではなく Shipper（複雑さ 0.28）。ウォーキングスケルトンは**業務の難所ではなく貫通のしやすさで選ぶ**という判断 |
| **汎用は増やさない対象として管理した** | Shared は全期間を通じて 2 要素に固定（ADR-005）。分類は「作る計画」だけでなく「**作らない計画**」にも使われた |

`development_strategy.md` の 7 局面のうち「精算（IT13-15）」「見積（IT18）」は、**局面名がそのまま業務領域名**です。局面の切り替えは技術的な都合ではなく、**業務領域の切り替え**として設計されていました。第 3 章・第 4 章はこの対応を軸に書きます。

一方で、**`domain-model.md` の概要は「6 つの境界付けられたコンテキスト」と書きながら、同じ表に 7 つを列挙しています。** Handling が ADR-010 で独立 BC に昇格した際、本文の件数だけが取り残されたものです。業務領域の分割そのものがイテレーションで変わり、**文書の記述が実態から遅れた実例**として第 9 章で扱います（M1・M3）。

### データモデリングは「先に全体、あとから追加」で進んだ

**本シリーズは、データモデリングのアプローチ（最初に全体を設計・構築し、各イテレーションで追加・変更する）も解説します。**

ドメインモデルが**イテレーションで育った**（M1）のに対し、**データモデルは最初に全体を作り、以後は差分で変えていく**という逆の進め方をしています。この非対称が本シリーズの見どころの一つです。実績は Flyway のマイグレーション 46 本に残っています。

| 段階 | 実績 |
| :--- | :--- |
| 最初に全体を構築 | `V1__init.sql` の 1 本で **20 テーブルを一度に作成**。7 BC すべて（`location`／`users`／`shipper`／`cargo`／`voyage`／`leg`／`tracking_activity`／`handling_activity`／`invoice`／`estimate` ほか）を最初から張っている |
| 以後は差分で変更 | `V2`〜`V41` の 40 本。うち**新規テーブルは 5 本だけ**（`booking_notification`／`customs_status_history`／`handling_correction`／`invoice_reminder`／`booking_cancellation`）で、残りは **`ALTER TABLE` 計 109 文**による列追加・制約追加 |
| 方言差の分離 | `common/`（41 本）と `postgresql/`（V101〜V104）・`h2/`（V103）に分離。**部分インデックスなど方言固有のものだけを別ディレクトリへ隔離**する |

設計側も 2 段階です。`design/data-model.md` は**概念データモデル**（業務の関心事）を先に置き、そこから **BC ごとの論理データモデル**（Shared／Booking／Routing／Tracking＋Handling／Billing／Estimation／Security）へ展開しています。テーブル定義とマイグレーションは論理モデルの写像です。

なぜドメインモデルと進め方が違うのか、を次の観点で説明します。

| 観点 | 内容 |
| :--- | :--- |
| **スキーマは後戻りが高い** | 集約はテストがあれば作り直せるが、テーブルはデータが載った瞬間に移行コストが跳ね上がる。**全体像を先に作るのはリスクの置き所を変える判断**であり、DDD の教条ではなく XP の「変更コストが高い箇所を先に決める」判断 |
| **それでも初期スキーマは正しくなかった** | `V3__cargo_specification.sql` が `ALTER` 10 文、`V21__cargo_special_handling.sql` が 8 文、`V30__invoice_charge_status.sql` が 9 文。**先に全体を作っても、業務ルールが分かるのはイテレーションの中**（M2 のデータ側の現れ） |
| **追加は「列」、分割は「テーブル」** | 後から増えた 5 テーブルはいずれも**新しい業務概念**（通知・通関履歴・是正・督促・キャンセル）。既存概念の詳細化は列追加で吸収している。**テーブルが増えるときは、ドメイン側にも新しい概念が立っている** |
| **不変条件の追加は既存行を壊す** | 列を後から NOT NULL 化する類の変更は、その列が無かったころの行を読めなくする。復元時には検査せず新規受け入れ時だけ検査する、という扱いを実例で示す |
| **設計ドキュメントとの一致を検査に落とした** | `DataModelDocumentSchemaTest` が `data-model.md` と実スキーマを突き合わせる。さらに ADR-015 のテーブル所有 BC は `data-model.md` の表が正典で、`MapperTableOwnershipTest` が**その表自身を読んで**マッパーの SQL を検査する（IT16 で新設、IT17 で名簿の一致自体を検査対象に）。**片方だけ直すと赤くなる**（M4） |

この主題は第 3 章（計画）と第 10 章（規則の検査化）に配し、**第 10 章でデータモデルの検査を扱う**構成にします。

### JIG で設計を可視化した — 「こう設計した」と「こう実装されている」を突き合わせる

**本シリーズは、設計の見える化を実現する JIG の活用も明記します。**（生成物 `build/jig/` はビルド成果物のため未収録。ドキュメントポータルで閲覧できる）

設計ドキュメントは書いた時点の意図であり、**実装がそのとおりである保証はありません**（M3）。参照元はこの隙間を、**バイトコードと Javadoc から設計ドキュメントを生成する** JIG で埋めています。`build.gradle` のコメントがこの位置づけを直接述べています。

> `docs/design` は「こう設計した」、JIG の出力は「こう実装されている」を示す。
> **両者を突き合わせることで、設計と実装の乖離を目視ではなく生成物で検出する。**
>
> — `apps/cargo-tracker/build.gradle`

| 項目 | 実績 |
| :--- | :--- |
| 導入 | `org.dddjava.jig-gradle-plugin` **2026.7.4**。`modelPattern = '.+'` で**全パッケージを対象**（BC ごとの構造を見るため） |
| 出力 | `build/jig/` に HTML 11 種＋データ 10 種（計 46 ファイル・7.5M）。解析対象クラス **611** |
| 主な出力 | `domain.html`（ドメインモデル）／`glossary.html`（Javadoc 抽出のユビキタス言語）／`package.html`（パッケージ構造）／`usecase.html`／`inbound.html`・`outbound.html`（入出力の依存）／`insight.html`／`library-dependency.html` |
| ER 図 | `jig-erd 0.2.2` を別タスク `jigErd` で実行。**Testcontainers 上に Flyway で構築した実スキーマ**から ER 図を生成する。「`data-model.md` の ER 図は設計、jig-erd の出力は実際のスキーマである」 |
| 分離の判断 | `jigErd` は Docker と Graphviz に依存するため、通常の `test` からは除外している |

解説する観点は次の通りです。

| 観点 | 内容 |
| :--- | :--- |
| **設計ドキュメントの「対」を持つ** | `domain-model.md` ↔ `domain.html`、対訳表 ↔ `glossary.html`、`data-model.md` の ER 図 ↔ `jigErd` の出力。**設計の各成果物に、実装から生成した相方がある**という構図そのものが本シリーズの主題 |
| **可視化は検査ではない** | JIG は乖離を**見せる**が、赤くはしない。だから `DataModelDocumentSchemaTest`・`MapperTableOwnershipTest`・ArchUnit のように**検査へ落とした項目だけが守られた**（M4）。**JIG と検査の役割分担**を明示する |
| **`glossary.html` は Javadoc の裏返し** | Javadoc を書かなければ用語集は空になる。`package-info.java` の存在を `PackageInfoPresenceTest` で強制しているのは、**可視化の入力そのものを検査している**ため |
| **生成物なので腐らない** | 手で描いた図は更新されないが、JIG の出力は**ビルドのたびに実装から作り直される**。マニュアルの画面キャプチャ自動再生成と同じ発想 |
| **BC ごとの構造が見える** | `modelPattern = '.+'` で全パッケージを対象にしたのは、**BC 分割が実装に現れているかを見るため**。ADR-024 の building block 分割（第 7 章）の前後を JIG の出力で対比できる |

この主題は第 9 章（ユビキタス言語の突合）を主軸に、第 7 章（分割前後の可視化）・第 10 章（可視化と検査の役割分担）に配します。

### マニュアルを受け入れの道具として使った — 生成 AI による生成と更新

**本シリーズは、受け入れツールとしてのユーザーマニュアル（`docs/article/source/java-2/docs/manual/`）を、生成 AI による生成と更新の実績とあわせて明記します。**

XP の受入テストは「仕様どおり動くか」を確かめますが、**「業務担当者がその画面で仕事を終えられるか」までは確かめません**。参照元はその隙間をユーザーマニュアルで埋めています。マニュアルは成果物であると同時に、**書けないことが実装の欠陥を暴く受け入れの道具**として機能しました。

| 項目 | 実績 |
| :--- | :--- |
| 構成 | 16 ファイル（本編 13 章＋はじめに・業務フロー・用語集・トラブルシューティング）、画面キャプチャ **62 枚** |
| 読者の定義 | 「読者は業務担当者であり、開発者ではありません」と `index.md` 冒頭で明示 |
| 価値の定義 | 「網羅性ではなく、**画面の前に座った担当者がその場で手を動かせること**」 |
| 執筆テンプレート | 画面・タブごとに 1 節。**「この画面でできること → 画面の開き方 → 画面の説明」の順序は読者が実際に迷う順序**であり、崩すと読者は自分の状況に対応する記述を探せなくなる、と規約に明記 |
| キャプチャの生成 | Playwright（`source/java-2/e2e/manual/manual-screenshots.spec.js`、1,119 行）で**画面キャプチャを自動再生成**。UI が変われば撮り直す |
| 運用 | `creating-manual` スキルにより生成 AI が執筆・更新。**マニュアル更新はイテレーションの DoD** に組み込まれ、イテレーション計画 **20 本すべて**とふりかえり **15 本**で言及されている |
| 実装済みのみ | 「本マニュアルは実装済みの画面のみを扱います」— **書けるかどうかが実装の有無と一致する**よう制約している |

解説する観点は次の通りです。

| 観点 | 内容 |
| :--- | :--- |
| **書けないことが欠陥を暴く** | 「この画面の開き方」が書けない＝**その画面へ到達する導線が無い**。ロール別到達性や状態別到達性の欠落は、受入テストが緑でもマニュアル執筆で初めて見つかる |
| **章番号を業務の順序にしない** | 章は画面の並び順、業務の順序は「01. 業務フロー」が担う。**画面の構造と業務の流れを別々に持つ**ことで、どちらのずれも隠れなくなる |
| **生成 AI は書くのが速いぶん、実態から離れるのも速い** | キャプチャの自動再生成と「実装済みのみ」の制約が、**文章が実態を先回りする**（M3）ことへの歯止めになっている |
| **用語集は業務担当者向けのユビキタス言語** | `用語集.md` は `domain-model.md` の対訳表と別の読者に向けた同じ語彙。**6 層（§「ユビキタス言語の出所」）の外側にもう一つ出口がある**ことを示す |
| **キャプチャは腐る** | 62 枚は放置すれば実画面と食い違う。自動再生成という**検査に落とす**形（M4）で扱っている |

この主題は第 5 章（受け入れの入口として）・第 9 章（用語集をユビキタス言語の出口として）・第 12 章（DoD としての運用）に配します。

### ユビキタス言語の出所 — 戦略と要件に遡って解説する

**本シリーズは、XP プラクティスのユビキタス言語を `strategy/` と `requirements/` の設計に関連付けて解説します。**

XP のプラクティス名（メタファー・ストーリー・計画ゲーム・オンサイト顧客・受入テスト）は、それ自体は開発手法の語彙であって業務の語彙ではありません。**両者が同じ言葉で結ばれていなければ、プラクティスは業務から切り離された作業手順に退化します。** 参照元には、業務の言葉が戦略から実装まで一本で降りている経路が残っており、これを各章の土台に置きます。

| 層 | 一次資料 | そこで定まる語彙 | 対応する XP プラクティス |
| :--- | :--- | :--- | :--- |
| 戦略 | `strategy/inception-deck.md`（10 の質問）、`strategy/business_architecture.md`（ビジネスモデル／バリューストリーム／ケイパビリティ／組織マップ／情報マップ） | **システムメタファー**の素。「なぜやるのか」「スコープの範囲はどこか」で扱う対象と扱わない対象の名前が決まる | メタファー、計画ゲーム（トレードオフスライダー） |
| 要件 | `requirements/requirements_definition.md`（RDRA 2.0：システム価値／外部環境／境界／システム） | アクター・業務フロー・情報の名前。**BC の候補がここで初めて名前を持つ** | オンサイト顧客、チーム全体 |
| ユースケース | `requirements/business_usecase.md`（アクター・目的リスト）、`requirements/system_usecase.md`（UC01〜UC22） | 業務手順の名前。UC の粒度が**受入テストの単位**になる | 受入テスト |
| ストーリー | `requirements/user_story.md`（US01〜US23＋追加分、受入条件つき） | 実装単位の名前。US の並び順が**イテレーションの単位**になる | ストーリー、小さなリリース |
| ドメインモデル | `design/domain-model.md` の対訳表（英語コード名／日本語業務用語／使用コンテキスト） | **コード上の識別子**。ここで初めて英語になる | シンプルな設計 |
| 実装 | `apps/` のパッケージ名・型名・`package-info.java`・Javadoc | 実際に動いている語彙。JIG の `glossary.html` で抽出できる | 共同所有、継続的インテグレーション |

**この 6 層は、上から下へ一方向に流れたわけではありません。** 実装で見つかった業務上の区別が上流の語彙を書き換えた箇所（M1・M2）と、上流の語彙が実装に降りきらず取り残された箇所（M3）の両方があります。**どちらの向きに動いたかを、章ごとに具体名で示す**のが本シリーズの記述方針です。

書き方の規律は次の通りです。

| 規律 | 内容 |
| :--- | :--- |
| プラクティス名を単独で使わない | 「受入テストを書いた」ではなく「UC13『荷役作業を記録する』を受入テストの単位にした」と、**要件側の ID と名前で書く** |
| 語彙の初出を示す | 業務用語を最初に出す章で、その語がどの層（戦略／要件／UC／US）で定まったかを明記する |
| 英語と日本語を対で扱う | コード上の識別子を出すときは `domain-model.md` の対訳表にある日本語業務用語を併記する |
| 断絶を隠さない | 上流の語彙と実装名が食い違っている箇所は、揃っている箇所と同じ比重で書く（M3 の実例になる） |

## 3. 章構成

### 導入

| 章 | タイトル | 主題 |
| :--- | :--- | :--- |
| 第 1 章 | XP と DDD をなぜ一緒に語るのか | サークルオブライフに戦略的設計・戦術的設計・ユビキタス言語を置いた拡張版の説明。**§2 の 6 層（戦略→要件→UC→US→ドメインモデル→実装）を全体地図として最初に提示する**。20 IT／7 BC ＋共有カーネル＋ Security ／1,578 テストの実績サマリー、本シリーズの読み方 |

### 第 1 部：計画ゲーム — 業務を分割する（ビジネスプラクティス × 戦略的設計）

| 章 | タイトル | 主題 |
| :--- | :--- | :--- |
| 第 2 章 | インセプションデッキから境界づけられたコンテキストへ | **メタファーの出所**。`inception-deck.md` の 10 の質問（特に「なぜやるのか」「スコープの範囲はどこか」）と `business_architecture.md` のバリューストリーム・ケイパビリティマップから、業務領域の分割（7 BC ＋共有カーネル）を導いた経路。中核／補完／汎用の分類（`domain-model.md` の quadrantChart：Booking [0.75, 0.82] が中核、Shared [0.18, 0.22] が汎用）と、それが投資配分の判断になったこと |
| 第 3 章 | 小さなリリースとイテレーション計画 | **ストーリーの出所**。`requirements_definition.md`（RDRA 2.0）→ `business_usecase.md` のアクター・目的リスト → `system_usecase.md`（UC01〜UC22）→ `user_story.md`（US01〜）という要件の降り方を追い、US01〜US36 をどう 20 イテレーションに割ったかを見る。**ストーリーの並び順が BC の立ち上がる順序を決めた**（M1）。v1.1.0 / v2.0.0 / v2.1.0 の 3 リリースとスコープ判断。**データモデルだけは「先に全体」だった**点（`V1__init.sql` で 20 テーブル）を、ドメインモデルの育ち方と対比して示す |
| 第 4 章 | 開発戦略 — 7 局面で TDD の入口を切り替える | `development_strategy.md` の 7 局面（序盤 IT1-2 ／中盤 IT3-6 ／終盤 IT7-12 ／精算 IT13-15 ／整流 IT16-17 ／見積 IT18 ／出荷と是正 IT19-20）。アウトサイドインとインサイドアウトの使い分けと選択理由。「Red-Green-Refactor は全局面で不変。変えるのはテストの入口とレイヤー貫通の順序だけ」。**「精算」「見積」のように局面名がそのまま業務領域名である点**を示し、局面の切り替えが業務領域の切り替えであることを裏づける |

### 第 2 部：テクニカルプラクティス — モデルを育てる（TDD・リファクタリング・シンプルな設計 × 戦術的設計）

| 章 | タイトル | 主題 |
| :--- | :--- | :--- |
| 第 5 章 | 受入テストから集約を立ち上げる | ウォーキングスケルトンで画面→ドメイン→DB を貫通させ、そこから集約を彫り出す。`scenario/` の業務シナリオ受入テスト（`CargoLifecycleScenarioTest` ほか 8 本）が集約の境界を決めた。**受入テストの名前が UC / US のどれに対応するかを対で示す**（例：UC13「荷役作業を記録する」と US15）。**受入の最後の関門としてユーザーマニュアル**を置き、「この画面の開き方」が書けないことが導線の欠落を暴いた例を扱う |
| 第 6 章 | 値オブジェクトと不変条件 — シンプルな設計が業務ルールを引き受ける | 不変条件を画面ではなく集約に置く判断。`HazardousDeclaration` の「存在しない値は申告が無いのと同じ結果になる」（IT12）。`EntityEncapsulationTest` による集約ルート経由の生成強制 |
| 第 7 章 | リファクタリングでモデルが割れる | `Cargo` が Checkstyle の 500 行上限に張り付いた状態から、ADR-024 の building block 分割（aggregates／entities／valueobjects）へ。**分割の代償を「（型・メソッド・呼んでよい相手）の 3 つ組」契約で封じた**判断（M5）。**分割前後を JIG の `domain.html`・`package.html` で対比**する |
| 第 8 章 | 境界を守る五つの手段 | 共有カーネルを `Location` と `ShipperId` の 2 要素に限定（ADR-005）、腐敗防止層（booking／estimation／handling／routing）、BC 横断ドメインイベント 10 種（ADR-009／012）。**いずれも越境の痛みが出てから決まった**（M2） |

### 第 3 部：チームプラクティス — 設計を腐らせない（CI・共同所有・ふりかえり）

| 章 | タイトル | 主題 |
| :--- | :--- | :--- |
| 第 9 章 | ユビキタス言語はどこで離れるか | **§2 の 6 層を端から端まで 1 つの語で辿る章**。`inception-deck.md` の言葉 → `requirements_definition.md` のアクター・情報 → UC / US の名前 → `domain-model.md` の対訳表 → 実装の型名という連鎖を具体例で示し、JIG の `glossary.html`（Javadoc 抽出）で実装側と突合する。`package-info.java` の存在を `PackageInfoPresenceTest` で強制する。**途中の層で名前が変わっている箇所・降りきらなかった箇所を同じ比重で扱う**（M3）。`manual/用語集.md` を**業務担当者に向けたもう一つの出口**として突き合わせる |
| 第 10 章 | 設計ドキュメントを実行可能にする | ADR・コメント・遷移表は守られない。ArchUnit（`PackageStructureTest`／`EntityEncapsulationTest`）、自作 lint（`ReadSideRuleLocationTest`／`PersistenceMarkerCallerTest`／`NarrowParseCatchTest`）、`DataModelDocumentSchemaTest` による設計ドキュメントと実スキーマの一致検査へ落とす（M4）。**データモデルの検査はここで扱う** — ADR-015 のテーブル所有 BC は `data-model.md` の表が正典で、`MapperTableOwnershipTest` がその表自身を読んで検査するため**片方だけ直すと赤くなる**（IT16 新設・IT17 で名簿の一致自体を検査対象に）。**検査そのものを検査する**テストの位置づけ。**JIG は乖離を見せるが赤くはしない**という、可視化と検査の役割分担 |
| 第 11 章 | 継続的インテグレーションが暴いたもの | 実行環境の差が業務ロジックの誤りとして表れる。H2 と PostgreSQL の方言差（`setval`／`INTERVAL`／`LATERAL`）から生まれた `H2DialectSmokeTest`、`TZ=UTC ./gradlew test` の運用（IT3 の CI 赤事故）、`RecentBusinessTime` による真夜中のテスト崩れ対策、Testcontainers の基底クラス自体をテストする判断 |
| 第 12 章 | ふりかえりが設計を変えた | KPT 20 回の Try が次イテレーションの検査になる連鎖を追う。「余力次第の返済枠」が 3 IT 連続で繰り越された ADR-008 と、返済枠を IT 序盤の独立コミットに固定した運用。SP 0 のイテレーション（IT16・IT17）を 2 回置いた判断。**マニュアル更新を DoD に組み込んだ運用**（イテレーション計画 20 本すべて・ふりかえり 15 本で言及）と、生成 AI が書く速さに対する歯止め |

### 総括

| 章 | タイトル | 主題 |
| :--- | :--- | :--- |
| 第 13 章 | XP は DDD に何を与えたか | 効いたプラクティスと効かなかったプラクティスの仕分け。DDD のパターンのうち「イテレーションで育てられたもの」と「最初に決めるしかなかったもの」の境目。残った未達と、次に同じ題材をやるなら変える点 |

## 4. 各章の構成（共通フォーマット）

第 1 部・第 3 部（第 2〜4 章、第 9〜12 章）は次の構成です。

1. **このプラクティスが解こうとした問題** — 設計上、何が壊れかけていたか
2. **どう実践したか** — 実ファイル・一次資料からの引用
3. **モデルがどう変わったか** — 前後の差分を具体的に
4. **働かなかったケース** — 同じ比重で扱う

第 2 部（第 5〜8 章）はコード中心のため、次の構成です。

1. **扱う業務ルール** — 対象の US と受入基準
2. **テストから始める** — Red の実物
3. **モデルに落とす** — 集約・値オブジェクト・ポートの実装
4. **後から効いた／効かなかった** — 以降のイテレーションでの帰結

### 図の活用方針

**本シリーズは設計で使った PlantUML 図をそのまま記事に載せ、視覚的に理解できる構成にします。**

参照元の設計ドキュメントには PlantUML が **169 個**（`@startuml` 126・`@startsalt` 34・`@startmindmap` 6・`@startgantt` 2・`@startditaa` 1）あります。境界づけられたコンテキスト・集約・レイヤー・イベントフローといった本シリーズの主題は、いずれも**関係の構造**であり、文章と表だけでは読者の頭の中で像を結びません。既存 3 シリーズがコードと表を主体にしているのに対し、本シリーズは**図を一次的な説明手段**として扱います。

| 方針 | 内容 |
| :--- | :--- |
| 転記元 | `source/java-2/docs/design/`・`development/`・`strategy/` の実ファイルから転記する。記事のために描き起こさない |
| 記法 | ` ```plantuml ` フェンス（`mkdocs.yml` の `plantuml_markdown` 拡張で描画される）。既存記事の ` ```mermaid ` と併用可 |
| 出典明記 | 図の直前または直後に転記元（ファイル名・図タイトル）を 1 行で示す。コードブロックと同じ規律 |
| 加工 | **原則そのまま。** 章の主題に対して要素が過多な場合のみ抜粋し、その旨を明記する（省略した図を「全体図」と偽らない） |
| 配置 | 主要な章に **1〜2 枚**。「この章で何が変わったか」を示す図を優先し、網羅的なカタログにはしない。**図で説明することがない章には置かない**（第 3・6・12・13 章） |
| 差分の表現 | 構造が変わった章（第 7 章）は、**変更の対象がどこか**を層の図で示す。分割前後のドメインモデル図は一次資料に存在しないため、並置はしない |

章と図の対応は次の通りです。

| 章 | 使う図（転記元） |
| :--- | :--- |
| 第 1 章 | 拡張サークルオブライフ（`docs/reference/よいソフトウェアとは.md`）、コンテキストマップ（`design/domain-model.md`） |
| 第 2 章 | ビジネスモデル・バリューストリーム・ケイパビリティマップの mindmap（`strategy/business_architecture.md`）、BC 分類の quadrantChart（`design/domain-model.md`） |
| 第 3 章 | 概念データモデル（`design/data-model.md`。初期構築の全体像として）、ビジネスユースケース全体図（`requirements/business_usecase.md`）、RDRA 4 層の図（`requirements/requirements_definition.md`）、リリーススケジュールの gantt（`strategy/inception-deck.md`）、ビジネスシナリオ図（`strategy/business_architecture.md`） |
| 第 4 章 | 局面別ワークフロー図「序盤: アウトサイドイン」「中盤: インサイドアウト」（`development/development_strategy.md`）、TDD 開発ワークフロー（`design/test_strategy.md`） |
| 第 5 章 | テストピラミッド（`design/architecture_backend.md`）、テスト形状の ditaa 図（`design/test_strategy.md`）、Booking Context ドメインモデル図（`design/domain-model.md`） |
| 第 6 章 | 各 BC のドメインモデル図から値オブジェクト周辺を抜粋（`design/domain-model.md`）、画面イメージの salt 図（`design/ui_design.md`）と `manual/assets/` の実画面キャプチャの対比（**設計した画面と実際の画面**） |
| 第 7 章 | Booking Context ドメインモデル図の**分割前後**、ヘキサゴナルアーキテクチャ図（`design/architecture_backend.md`） |
| 第 8 章 | コンテキストマップ（`design/domain-model.md` / `architecture_backend.md`）、共有カーネル図、ドメインイベントフロー図、ドメインイベント伝播図（ADR-009） |
| 第 9 章 | 情報マップ（`strategy/business_architecture.md`）→ RDRA の情報モデル（`requirements/requirements_definition.md`）→ 概念データモデル → 論理データモデル（`design/data-model.md`）を**同じ語を追える順に並べる**。層をまたいで用語が一致しているか（していないか）を図で示す。**JIG の `glossary.html`・`domain.html` の画面キャプチャ**を実装側の相方として並べる |
| 第 10 章 | CQRS のコマンド・クエリ分離図（`design/architecture_backend.md`）。ADR の規則がどの矢印を禁じているかを重ねる |
| 第 11 章 | CI/CD とテスト連携の図（`design/test_strategy.md`）、インフラ構成図（`design/architecture_infrastructure.md`） |
| 第 12 章 | 図なし（一次資料の引用が主体） |
| 第 13 章 | 第 1 章のコンテキストマップと**最終状態**の対比。20 イテレーションで何が動いたかを 1 枚で示す |

## 5. 引用元の対応

記事中のコード・設定の引用はすべて実ファイルから転記します。

| 章 | 主な引用元 |
| :--- | :--- |
| 第 1 章 | `docs/reference/よいソフトウェアとは.md`（拡張サークルオブライフ）、`java-2/docs/design/domain-model.md`、`java-2/docs/strategy/`・`requirements/` の各 index |
| 第 2 章 | `java-2/docs/strategy/inception-deck.md`、`business_architecture.md`、`design/domain-model.md` |
| 第 3 章 | `java-2/docs/design/data-model.md`、`apps/.../db/migration/`（`V1__init.sql` ほか 46 本）、`java-2/docs/requirements/requirements_definition.md`・`business_usecase.md`・`system_usecase.md`・`user_story.md`、`development/release_plan.md`・`release_scope.md`・`iteration_plan-1..20.md` |
| 第 4 章 | `java-2/docs/development/development_strategy.md`、`design/test_strategy.md` |
| 第 5 章 | `apps/cargo-tracker/src/test/.../scenario/`、`booking/` の集約とコマンドサービス、`java-2/docs/manual/`（受け入れの最後の関門として）、`java-2/e2e/manual/manual-screenshots.spec.js` |
| 第 6 章 | `booking/domain/model/{aggregates,valueobjects}/`、`EntityEncapsulationTest` |
| 第 7 章 | `java-2/docs/adr/024-*.md`、`apps/.../config/checkstyle/checkstyle.xml`、`PackageStructureTest` |
| 第 8 章 | `shared/`、各 BC の `infrastructure/acl/`・`interfaces/events/`、ADR-005／009／012 |
| 第 9 章 | `java-2/docs/strategy/inception-deck.md`、`requirements/requirements_definition.md`・`system_usecase.md`・`user_story.md`、`design/domain-model.md`（対訳表）、`java-2/docs/manual/用語集.md`、`apps/.../package-info.java`、`PackageInfoPresenceTest`、`build.gradle`（JIG 設定のコメント） |
| 第 10 章 | `apps/.../src/test/.../support/`（`DataModelDocumentSchemaTest`・`MapperTableOwnershipTest`）、`java-2/docs/design/data-model.md`、`java-2/docs/adr/015・021・022` |
| 第 11 章 | `support/H2DialectSmokeTest`・`PostgreSQLIntegrationTestBase`・`RecentBusinessTime`、`.github/workflows/`、`build.gradle` のコメント |
| 第 12 章 | `java-2/docs/development/retrospective-1..20.md`、`journal/`、`java-2/docs/manual/index.md`（執筆テンプレートと DoD） |
| 第 13 章 | 全ふりかえり、`java-2/docs/adr/index.md` |

**参照元ソースはほぼ収録済みです。** `docs/article/source/java-2/`（実装 850 ファイル・一次資料 144 ファイル）が実践 AI 駆動開発シリーズのために収録されており、本シリーズはこれをそのまま引用します。**本シリーズのために追加収録したのは `e2e/manual/manual-screenshots.spec.js` の 1 ファイルだけ**です（マニュアルの画面キャプチャ自動再生成を第 5 章・第 6 章で引用するため）。

引用箇所は出典（ファイル名・IT 番号・ADR 番号）を本文に明記します。ふりかえりからの引用は `> — retrospective-12.md P2` の形式で、既存シリーズの書式に合わせます。

## 6. 執筆順序

| 順 | 対象 | 理由 |
| :--- | :--- | :--- |
| 1 | 第 1 章＋シリーズ index | 全体像と 5 主題を先に固定する |
| 2 | 第 5〜8 章（テクニカルプラクティス） | コードが一次資料であり、事実の確定が最も確実 |
| 3 | 第 2〜4 章（計画ゲーム） | モデルの実物を確定させてから、そこへ至った計画を書く |
| 4 | 第 9〜12 章（チームプラクティス） | 第 2 部で扱ったモデルを対象に、どう腐敗を止めたかを書く |
| 5 | 第 13 章（総括） | 全章の確定後 |
| 6 | `docs/article/index.md` への反映・MkDocs nav | 章が揃ってから一括 |

**第 2 部を先に書きます。** 計画の章を先に書くと「こう計画したからこうなった」という後知恵の物語になります。実際には計画のほうが実装に修正された箇所が多いため、モデルの実物から先に固めます。

## 7. 前提整備

| 項目 | 状態 |
| :--- | :--- |
| 参照元実装 `source/java-2/apps/` | **完了**（850 ファイル・5.6M／実践 AI 駆動開発シリーズで収録済み） |
| 参照元一次資料 `source/java-2/docs/` | **完了**（144 ファイル・2.7M／同上） |
| 記事ディレクトリ `xp-domain-driven-design/` | **完了**（index ＋全 13 章） |
| `docs/article/source/README.md` への引用元シリーズ追記 | **完了**（`java-2/` の行に本シリーズを併記） |
| `docs/article/index.md` のシリーズ一覧追記 | **完了**（3 → 4 シリーズ。冒頭の件数表記・読む順序も更新） |
| `docs/index.md` への反映 | **完了**（入口・一覧表・章立て表・参照元） |
| MkDocs nav | **完了**（`mkdocs.yml` の `- 記事:` に全 14 ページ） |
| マニュアル `source/java-2/docs/manual/` | **完了**（16 md ＋キャプチャ 62 枚・5.7M） |
| キャプチャ生成スペック `source/java-2/e2e/manual/manual-screenshots.spec.js` | **完了**（1,119 行・51K）。第 5 章・第 6 章で引用するため、`apps/` と `docs/` に加えて本ファイルのみを収録した |
| JIG 生成物 | **未収録**（`build/` は `.gitignore` 対象。46 ファイル・7.5M のビルド成果物であり、`./gradlew jigReports` で再生成できる）。記事はドキュメントポータルへ誘導する |
| PlantUML 描画環境 | **完了**（`mkdocs.yml` に `plantuml_markdown` 拡張と `superfences` の `plantuml` カスタムフェンスが設定済み。転記した図はそのまま描画される） |

執筆は §6 のとおり第 2 部（第 5〜8 章）から着手し、第 1 部・第 3 部・第 13 章の順で完了しました。内部リンクの破損は 0 件、Markdown Lint の指摘は MD013（行長）のみで既存記事と同水準です。

サンプル実装は既存（`java/take-6` の実績）であり、**新規に TDD で作る対象はありません**。したがって本シリーズでは `creating-article` の「3. 環境準備」「6. CI」は適用対象外です。代わりに **引用したコードが `source/java-2/apps/` の実ファイルと一致していること**の確認を各章の完了条件とします。

## 8. 決定事項

| # | 論点 | 決定（2026-08-13） |
| :--- | :--- | :--- |
| Q1 | シリーズ名 | **「XP によるドメイン駆動設計の実践」で確定**。「XP と DDD」ではなく「XP による」とし、**XP のプラクティスが DDD を駆動する**という主従関係を題名で示す。末尾を「開発」ではなく「**設計の実践**」としたのは、本シリーズが扱うのが開発手法の紹介ではなく**20 イテレーションで設計をどう実践したか**の記録であるため。ディレクトリも `xp-domain-driven-design/` に揃える |
| Q2 | 実践 AI 駆動開発との重複 | **軸で切り分ける**。同じ実績を参照するが、AI 駆動開発シリーズは「誰が書いたか」、本シリーズは「どういう規律で書いたか」。重複しうる第 10 章（規則の検査化）・第 12 章（ふりかえり）は、AI の話を持ち込まず**設計への帰結だけ**を書く |
| Q3 | 章の分割単位 | **プラクティス単位で確定**。既存 2 シリーズの IT 単位（20 章）は分量が過大で、かつ「一つのプラクティスが複数の IT にまたがって効いた」という本シリーズの構造を消してしまう |
| Q4 | 一次資料の追加収録 | **1 ファイルのみ追加**。`source/java-2/` は既に本シリーズの必要範囲をほぼ満たすが、マニュアルのキャプチャ自動再生成を引用するため `e2e/manual/manual-screenshots.spec.js` を収録した。`e2e/app/` の受入テスト 11 本は引用予定が無いため収録しない |
| Q5 | 図の扱い | **設計で使った PlantUML を転記して視覚的に理解できる構成にする**（§4「図の活用方針」）。既存 3 シリーズはコードと表が主体だが、本シリーズの主題は BC・集約・レイヤー・イベントフローという**関係の構造**であり、図でしか伝わらない。記事のために描き起こさず、設計ドキュメントの実物を出典つきで載せる |
| Q6 | ユビキタス言語の扱い | **XP プラクティスの語彙を `strategy/` と `requirements/` の設計に関連付けて解説する**（§2「ユビキタス言語の出所」）。プラクティス名を単独で使うと業務から切り離された作業手順の紹介になる。受入テストは UC の ID と名前で、ストーリーは US の ID と名前で書き、戦略→要件→UC→US→ドメインモデル→実装の 6 層を全章の共通の座標軸とする |
| Q7 | イテレーションの単位 | **`design/domain-model.md` の業務領域の分割を元にイテレーションを進めたことを明記する**（§2「業務領域の分割がイテレーションの単位になった」）。差別化と複雑さによる中核／補完／汎用の分類が、着手順・繰り返し回数・「作らない」判断のすべてに現れている。イテレーションを技術タスクの束としてではなく**業務領域の切り替え**として記述する |
| Q8 | データモデリングの扱い | **「最初に全体を設計・構築、各イテレーションで追加・変更」というアプローチを明示的に解説する**（§2「データモデリングは『先に全体、あとから追加』で進んだ」）。ドメインモデルが育ったのに対しデータモデルは先に全体を張った、という**非対称そのものが主題**。実績（`V1__init.sql` で 20 テーブル、以後 `ALTER` 109 文・新規テーブル 5 本）を根拠に、DDD の教条ではなく「変更コストが高い箇所を先に決める」XP の判断として説明する |
| Q9 | マニュアルの扱い | **受け入れツールとして明記する**（§2「マニュアルを受け入れの道具として使った」）。成果物としてのドキュメントではなく、**書けないことが実装の欠陥を暴く検査**として位置づける。生成 AI による執筆・更新と Playwright によるキャプチャ自動再生成をセットで扱い、「文章が実態を先回りする」ことへの歯止めとして説明する |
| Q10 | 設計の可視化の扱い | **JIG の活用を明記する**（§2「JIG で設計を可視化した」）。`docs/design` は「こう設計した」、JIG の出力は「こう実装されている」という対の構図を本シリーズの主題として立てる。ただし**可視化は検査ではない**（乖離を見せるが赤くしない）ことを併記し、第 10 章で検査との役割分担を書く |
