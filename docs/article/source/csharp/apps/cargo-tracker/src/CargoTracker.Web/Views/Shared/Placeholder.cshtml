@model CargoTracker.Shared.Infrastructure.Web.PlaceholderViewModel
@{
    ViewData["Title"] = Model.Title;
}

<h1 class="h3 mb-3">@Model.Title</h1>
<div class="alert alert-info" role="alert">
    準備中（@Model.Note）。この画面は該当イテレーションで実画面に差し替えます。
</div>
