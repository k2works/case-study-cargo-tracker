/**
 * Gateway と各サービスの間の認証契約（ADR-004）。
 *
 * <p>Gateway が署名を検証し、検証済みのクレーム（利用者 ID とロール）をヘッダで渡す。
 * 各サービスは署名を再検証せず、このヘッダに基づいて認可だけを判断する。
 *
 * <p><strong>この 1 本の契約に ADR-004 の分担全体が乗っている。</strong> サービスごとに
 * ヘッダ名を書き写すと、Gateway 側で名前を変えても誰も落ちない。だから共有カーネルに置く。
 */
package com.example.shared.auth;
