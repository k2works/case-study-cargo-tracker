# 2026-08-12 IT19（出荷と是正）

## この日やったこと

Release 2.1 を出荷として確定させ、`ui_design.md` に書いてあって実装に無かった 2 件を返した。あわせて IT18 の Try 5 件を検査に落とし、利用者の依頼で 2 件（開発環境のシード・ドメインモデルの分割）を追加で行った。

**全 36 ユーザーストーリーが実装完了した後の、最初のイテレーション。**

## 判断の記録

### 「整流 3 回目」と呼ばなかった

開発戦略は整流について「この局面を繰り返さないことが目的である」と書き、IT17 のふりかえりは「2 回目・**最終**」と宣言している。**その宣言を取り消さないことにした。**

中身が違う。整流の対象は自分たちが数え上げた技術的負債だが、今回の対象は**正典に届いていない実装**である。局面名を「出荷と是正」とした。

### 名簿方式をやめる、ではなく「叩き方は人が書き、載せ忘れだけを機械が見る」

H2 スモークの載せ忘れを止める方法として、計画では「Bean を自動で集めて全件叩く」としていた。だが引数の要るメソッドには値をこしらえる必要があり、**叩けなかったものを黙って飛ばす形になりやすい**。それは名簿方式に戻るのと同じである。

叩き方は人が書き、載せ忘れだけを機械が見る形にした。

### SQL では作れないものがある

開発環境をマニュアルと同じ状態にする依頼を受けて調べると、追跡の記録も請求書も**荷役を登録した結果としてドメインイベントが作る派生データ**だった。`handling_activity` だけを INSERT すると、一覧には出るのに追跡画面と状態バッジが食い違う。採番もシーケンスと衝突する。

本番と同じ経路（アプリケーションサービス）で投入する形にした。

## 詰まったこと

### 投入済みの判定を 3 回間違えた

- 「見積があれば投入済み」——見積を作る別のテストが先に走ると、投入が丸ごと飛ぶ
- 「動作確認用の荷主が居れば投入済み」——その荷主は SQL が作る。local を起動すると必ず先に居るため、**一度も走らなかった**
- 「その荷主に予約があれば投入済み」——予約も SQL が作る

**印にできるのは、投入する側しか作らないものだけである。**

### 検査そのものが走っていなかった

壊れた `@link` を入れて破壊検証をしたら、赤にならなかった。Javadoc だけの変更ではバイトコードが変わらず `compileJava` が UP-TO-DATE になり、**テストが実行されなかった**。

ソースを読む検査が、ソースを入力に宣言していなかった。**破壊検証をしていなければ気づかなかった。**

### 参照実装の構成を、意味を確かめずに写した

ドメインモデルの分割で、参照実装が `BookingId` を `aggregates` に置いているのをそのまま真似た。結果、**ADR が「集約ルートの数が目に見える」と書いた利得を、識別子の同居が最初の 1 回で壊した**（本文の「Booking は 4」も誤りだった）。`entities` も 8 件中 6 件が record（同一性を持たない）だった。

レビューで指摘され、同じイテレーションの中で直した。直した結果、`entities` に残ったのは 2 クラスだけである。

### 「名前で記録した」と書きながら、記録が無かった

ADR-024 は 2 か所で「次のイテレーションで ArchUnit ルールに落とす（名前で記録した）」と書いていた。`docs/development/` を grep しても **1 件もヒットしなかった**。

**ADR 本文が最も強い言葉で戒めている形を、その ADR 自身が踏んでいた。**

## 学び

**検査を足すイテレーションで、その検査自身に穴を 3 つ作った。**

| 検査 | 穴 |
| :--- | :--- |
| H2 スモークの載せ忘れ | 型単位でしか見ておらず、**メソッド 6 件を素通り** |
| ADR-022 の規則の置き場 | 数値リテラルしか拾わず、**移設先がまさに定数比較の形** |
| 型名の照合 | 部分一致で、接尾辞が重なる型を見分けられない |

IT17・IT18 で外したのは「新しいサービス」だった。だから検査もサービス単位で書いた。**だが次に増えるのはメソッドである。**

「2 回続けて外したなら方式の問題」と書いて方式を変えたのに、**新しい方式の粒度が事故の粒度と合っていなかった。** Red は「過去に起きた症状」ではなく「次に起きる症状」で書く必要がある。

---

## クローズ（追記）

### 5 視点レビューで高 11 件。**検査を足す回に、検査自身の穴が 3 つあった**

| 検査 | 穴 | 実測 |
| :--- | :--- | ---: |
| H2 スモークの載せ忘れ | 型単位でしか見ていない | **6 メソッドが素通り** |
| ADR-022 の規則の置き場 | 数値リテラルしか拾わない | **移設先の `DeadlineUrgency` がまさにその形** |
| 型名の照合 | 部分一致 | 接尾辞が重なる型を見分けられない |

IT17・IT18 で外したのは「新しいサービス」だった。**だから検査もサービス単位で書いた。** だが次に増えるのはメソッドである。

### 利用者視点が、権限設定まで読んで手順の破綻を見つけた

マニュアル 8.5.1 に書いた手順は、**荷役作業員が実行すると 2 回続けて 403 に当たる**（`/tracking` も `/voyages` も別ロール専用）。さらに「陸揚げ地の変更は承認をやり直す」と書いたが、**7.10 が「同じ申請を二度決めることはできない」と定めている** —— できないことを約束していた。

**書いた側は全ロールの権限を持って読んでいた。**

### E2E の不安定さは、レビューをきっかけに原因が割れた

ログイン画面は動作確認用の利用者一覧が長く、**ログアウトの案内が出た状態ではボタンの位置が動き続ける**。Playwright の「安定するまで待つ」が 30 秒を使い切っていた。変更前のコードでも落ちることは先に確認していたが、**原因までは詰めていなかった**。Enter で送る形にして 21 件すべて緑。

---

## IT20 の開始準備で分かったこと（追記）

### **自分が ADR を読み替えていた**

IT20 計画の第 1 版に「IT19 の記録は誤り。D5 の違反は 0 件ではなく 2 件」と書いた。**これが間違いだった。**

ADR-024 の表は、呼んでよい相手を**メソッドごとに書き分けている**。

| クラス | 開いた操作 | 本来呼んでよいもの |
| :--- | :--- | :--- |
| `ProposedRoute` | `of` / `withPriority` | **`RouteSearchService`**（ドメインサービス） |
| `TrackingExceptionEvent` | `raise` / `resolve` | 集約ルート |

この契約に照らせば `RouteSearchService` からの 2 件は違反ではない。**IT19 の「0 件」は正しく、私が規則を「集約ルート以外から呼べない」と一般化して読み替えていた。**

**数え間違いではなく、規則の書き換えだった。** そして書き換えたまま進めれば、`ProposedRoute.of` を集約ルートへ移すことになる。あのメソッドの 6 引数はすべて探索中にしか作れない（経由港の走査結果・所要日数・概算費用・貨物種別の可否・期限判定）。**探索と提案の分離が壊れるところだった。**

### 基準にする図が欠けていると、欠けたものは検証されない

状態遷移図を「寄せ替えの前後で遷移が変わらないことを確かめる基準」として計画に載せた。その図が**遷移を 3 本落とし、表に無い遷移を 1 本描いていた**（`TRACKING_ISSUED → CANCELLED` の欠落など）。

**基準が欠けていれば、欠けた遷移は照合をすり抜ける。**

### DoD が引用する先が空だった

計画の DoD は「条件は書き写さず、正典を引用します。正典: `ui_design.md` の『見積一覧』」と書いた。だが `ui_design.md` の見積一覧の仕様に、**今回やろうとしている 5 項目（既定・0 件の種類・件数・並び順・一覧からの再見積）はどれも無い。**

**引用の作法は守っていたが、引用先が空だった。**

---

## 学び（追記）

**「規則を書き換えた」ことに気づける仕組みが無い。**

数字のずれは 9 度目になり、そのたびに「その場で数える」を積み重ねてきた。だが今回のは種類が違う。**数えた対象そのものが、規則を読み替えた結果だった。**

数え直しは「同じ規則で数え直す」ことしか保証しない。**規則を写すときは、写した先で 1 件ずつ「この行はどこから来たか」を言えるか確かめる**必要がある——これは IT19 の Try T3（参照実装を写すときは 1 クラスずつ「なぜそこか」を言えるか確かめる）と、まったく同じ形だった。**自分で書いた Try を、翌日に自分で踏んだ。**
