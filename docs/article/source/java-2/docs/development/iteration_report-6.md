# イテレーション 6 完了報告書

| 項目 | 内容 |
| :--- | :--- |
| イテレーション | IT6 |
| リリース | Release 1.0（追跡） |
| 局面 | 中盤（インサイドアウト） |
| 期間 | 2026-08-08 |
| 計画 SP | 8 |
| 完了 SP | **8**（達成率 100%） |
| コミット数 | 19（クローズ後の設計変更を含む） |

---

## エグゼクティブサマリー

**Release 1.0 が完成した。** 「予約 → 経路 → 確定 → 追跡番号 → 荷役 → 輸送中」が一本つながり、
E2E がその 1 本を 4 つのロールをまたいで通している。ウォーキングスケルトンが端から端まで到達した。

本 IT は **Tracking Context の初出**であり、集約 2 件・リポジトリ 2 件・BC 間 ACL ポート 3 件・
画面 3 つを新設した。これまでで最もトランザクション境界が広いイテレーションになった
（荷役の登録は当初 1 トランザクションで 2 BC・3 集約を書いていた）。

**その広がりが、クローズ後の設計変更につながった。** レビュー指摘を起点に
BC 構成と連携方式の 2 つを変更している（後述）。

**その広がりが、そのままレビューの指摘に現れた。** 5 視点のうち 3 視点が独立に同じ型の欠陥を
指摘している — 「安全装置を書いたが、その結果を誰も確かめていない／使っていない」。
高優先度 14 件のうち 8 件を本 IT 内で修正し、6 件は新規ストーリー相当として次 IT 以降へ送った。

---

## 達成状況

### ユーザーストーリー

| ID | ストーリー | SP | 状態 |
| :--- | :--- | :--- | :--- |
| US13 | 予約を確定する | 3 | **完了** |
| US14 | 追跡番号を発行する | 2 | **完了** |
| US15 | 荷役作業を記録する | 3 | **完了** |
| | **合計** | **8** | **100%** |

### 受入基準のうち満たさなかったもの

計画で明示的にスコープ外としたものと、実装中に判断したものを分けて記録する。

| 内容 | 理由 | 送り先 |
| :--- | :--- | :--- |
| US13 / US14 / US15 の**通知・メール送信** | ADR-006（外部連携は実装しない）。待ち行列で代替した | — |
| US15 の**スキャン入力** | 端末依存であり検証対象外。手入力のみ実装 | IT7（スキャナ入力への配慮は返済枠） |
| US13「経路設計中に戻せる」 | **状態遷移表（正典）に無い遷移である。** 受入基準を優先して実装すると正典が壊れる | IT8（US10） |
| US13「キャンセルできる」 | 遷移 #9 は 4 状態から到達し、US30 と地続き。確定のストーリーに混ぜると検証が薄まる | Release 1.1 以降 |
| US15 の**引取（CLAIM）** | US16 のスコープ。選択肢に出していない | IT7（US16） |

---

## 技術的成果

### 実装

| 分類 | 内容 |
| :--- | :--- |
| Booking | 確定・追跡番号発行・輸送開始（遷移表 #4・#5・#6）。`CargoProgress` / `BookingTrackingNumber` |
| Tracking | `TrackingActivity` 集約・`TransportStatus`・`TrackingNumber`（業務 TZ で採番） |
| Handling | `HandlingActivity` 集約・妥当性検証（デシジョンテーブル）・誤配の反映 |
| ACL | `TrackingPort` / `CargoSnapshots` / `VoyageCapacityPort`（**クローズ後の変更で伝播系のポートはイベントに置き換わった**） |
| 画面 | 予約詳細のボタン 2 つ・`/tracking/queue`・`/handling`・`/handling/new`・navbar・ダッシュボード |
| スキーマ | V10（区間の添字）・V11（追跡番号の採番と一意性）・V12（追跡レコードの一意性） |
| E2E | Playwright を有効化（開発戦略の品質ゲート表が定める IT6）。クリティカルパス 1 本 |

### 設計上の判断

| 判断 | 理由 |
| :--- | :--- |
| **確定の瞬間に空き容量を数え直す** | 算出時の判定を信じると、算出から確定までの間に満船になった便を確定できる（IT5 レビュー M3）。実際にその順序を再現するテストで固定した |
| **ACL が運ぶ値はポートのパッケージに置く** | 相手の `domain.model` に置くと、実装する BC がそこを参照する。ArchUnit ルール 4 の除外は ACL ポートのパッケージだけであり、**そこが唯一の越境点**である |
| **確定の可否を `CargoProgress.confirmable` に 1 か所だけ置く** | 集約と画面が同じ判断を呼ぶ。2 か所に書くと「押せるのに実行すると失敗するボタン」が生まれる |
| **追跡番号の桁あふれは丸めない** | 剰余で丸めると、発行済みの番号と重複した番号を黙って返す。形式検査で落とす |
| **予定と違う作業も登録を拒否しない** | 拒否すると、実際に起きた作業がどこにも残らない。伝えるのは警告である |
| **BC 間の状態伝播はドメインイベント（結果整合）** | クローズ後に判断を変更した（ADR-009 改訂 1）。**最も頻度が高く落としてはならない荷役の記録が、追跡や予約の都合で失敗してはならない**。詳細は後述「クローズ後の設計変更」 |
| **Handling は独立した BC** | クローズ後に昇格した（ADR-010。ADR-002 を置き換え）。実装すると言語が分岐しており、**境界が引かれていないだけの状態**だった |

---

## 品質指標

| 指標 | 値 | 目標 | 判定 |
| :--- | :--- | :--- | :--- |
| `./gradlew check` | 緑 | 緑 | ○ |
| `TZ=UTC ./gradlew test` | 緑 | 緑 | ○ |
| Backend CI | **緑**（2 IT 連続） | 緑 | ○ |
| E2E（クリティカルパス） | 緑。**壊すと赤になることを実測** | 緑 | ○ |
| SonarQube Quality Gate | **PASS** | PASS | ○ |
| カバレッジ（新規コード） | 87.9% | 80% 以上 | ○ |
| カバレッジ（全体・命令） | 92% | — | — |
| 重複率 | 0.6% | 3% 未満 | ○ |
| Bug / Vulnerability / Code Smell | **0 / 0 / 0** | 0 | ○ |
| Trivy HIGH / CRITICAL | 0 件（開始時に確認） | 0 | ○ |
| ArchUnit | 6 ルールすべて緑 | 全緑 | ○ |
| 本番コード（ファイル） | 239 | — | — |
| テスト（ファイル） | 52 | — | — |
| 壊して赤を確認した装置 | **19 件** | — | — |

---

## レビュー結果

[IT6 実装レビュー](../review/IT6実装_review_20260808.md)（5 視点並列）

| 分類 | 件数 | 対応 |
| :--- | :--- | :--- |
| 高優先度 | 14 | **8 件を本 IT 内で修正。** 6 件は新規ストーリー相当として次 IT 以降へ |
| 中・低優先度 | 13 | 4 件を本 IT 内で修正。残りは IT7 の返済枠・US 着手時 |

### 本 IT 内で修正した高優先度（8 件）

1. **楽観的ロックの戻り値を 3 か所で捨てていた**（追跡の更新・誤配の反映・輸送開始）
2. **追跡番号の発行が原子的でなかった**（V12 で DB 側の一意性も追加）
3. **誤配の反映を検証していなかった**（消しても全テストが緑だった）
4. **作業日時のタイムゾーン変換を検証していなかった**（UTC に戻しても緑だった）
5. `architecture_backend.md` が未更新（**イベント駆動設計が実装と食い違っていた**）
6. マニュアルの索引 3 点同期が未完
7. 追跡番号発行待ちの項目表が実画面とずれ
8. 付録 B に IT6 の症状が無かった

### 次 IT 以降へ送った高優先度（6 件）

いずれも**新規ストーリー相当**であり、本 IT のスコープでは扱えない。

| 内容 | 送り先 |
| :--- | :--- |
| 追跡番号で予約を引き当てる | IT7（US 化） |
| 荷役記録の訂正・取り消し | IT7（US 化） |
| 荷役一覧に追跡番号の列・スキャナ入力・作業日時の初期値 | IT7（返済枠） |
| 確定した経路の差し戻し | IT8（US10） |
| 誤配の追跡担当者への通知 | IT11（US28） |
| 満船時に営業担当者ができることが無い | IT8（US10） |

---

## ユーザーマニュアル

**UI に変更があったため更新した**（クローズのステップ 6.5）。

| 内容 | 変更 |
| :--- | :--- |
| 04. 貨物予約 | 「4.5 予約を確定する」を追加。冒頭の「今後の提供」を実態に修正 |
| 07. 追跡管理 | **新設** |
| 08. 荷役管理 | **新設**。あわせて**結果整合の注記**を追加（登録直後は追跡にまだ出ないこと・記録は必ず残ること） |
| 01. 業務フロー | 工程表と PlantUML 図を実態に更新 |
| 00. はじめに | 全体構成表・ロール表を同期（索引 3 点同期） |
| 付録 B | Q4〜Q6 とエラーメッセージ早見表 12 件、Q6-6（登録したのに追跡の状態が変わらない）を追加 |
| キャプチャ | 26 件を再生成（**日本語ロケール・日本時間**で撮り直し） |

---

## クローズ後の設計変更

**レビューを踏まえ、アーキテクチャの判断を 2 つ変更した。** ストーリーの範囲は変えていない
（SP は 8 のまま）。変わったのは実装の構造である。

| ADR | 内容 | 理由 |
| :--- | :--- | :--- |
| **ADR-010** | Handling を独立した BC へ昇格（**ADR-002 を置き換え**） | 実装すると言語が分岐していた。対応する型を 3 組も別々に定義しており、統合されていたのではなく**境界が引かれていなかった** |
| **ADR-009（改訂 1）** | BC 間の状態伝播を**ドメインイベントによる結果整合**へ | 1 操作が 3 集約を 1 トランザクションで更新する形が重すぎた。**最も頻度が高く落としてはならない荷役の記録が、追跡や予約の都合で失敗する構造だった** |

### この変更で守った線引き

すべての境界越えをイベントにはしていない。

| 種別 | 扱い |
| :--- | :--- |
| 状態の伝播（荷役 → 追跡・予約） | ドメインイベント（`AFTER_COMMIT`） |
| 問い合わせ（予定ルート・空き容量） | 同期（ACL ポート） |
| コマンド（経路割り当て・追跡番号発行） | 同期（ACL ポート） |

コマンドをイベントにすると、**拒否の理由を利用者にその場で返せなくなる**ためである。

### 意図的に変わった振る舞い

**追跡への反映が失敗しても、荷役の記録は残る。** 変更前は荷役ごと巻き戻していた。
テストもこの仕様に書き直している。

### 検証

- ArchUnit：**荷役から追跡の型を直接参照する変更を入れると落ちる**ことを実測
- 購読ハンドラを無効化すると 2 件が赤になることを実測
- `./gradlew check` 緑・E2E 緑・SonarQube Quality Gate PASS（再スキャン済み）

---

## 課題と残作業

| # | 内容 | 対応 |
| :--- | :--- | :--- |
| 1 | **購読側の失敗が利用者の画面に返らない**（結果整合の代償）。ログにしか残らず、見る運用が無い | IT7 |
| 1-2 | **イベントの取りこぼし**（プロセス障害）。Outbox は ADR-009 の次の改訂候補 | BC が別プロセスに分かれた時点 |
| 2 | `TrackingSequence` の置き場（`acl` パッケージにある MyBatis Mapper）と H2 方言スモークへの追加 | IT7 の返済枠 |
| 3 | 確定の `CONFLICTED` テスト・荷役種別ごとの結果状態の網羅テスト | IT7 の返済枠 |
| 4 | 用語集への IT6 の語（追跡番号・誤配・荷役種別）の追加 | IT7 の返済枠 |
| 5 | 作業日時の上限・下限（未来日時が通る） | IT7 で業務判断のうえ |
| 6 | `CLAIM` を POST すれば通る（画面の選択肢には無い） | IT7（US16 で正式に開く） |

---

## 次イテレーションへの引き継ぎ

引き継ぎ事項は [IT6 ふりかえり](retrospective-6.md) の「次イテレーションへの引き継ぎ」（C1〜C11）を正典とする。

**最も重い Try は T1（「壊して赤」を入口と出口の両方で回す）である。** 本 IT では 14 件の装置を
壊して赤を確認したうえで、レビューでさらに 4 件の空振りが見つかった。**回した対象が
「判定の入口」に偏り、「判定の結果がどこへ行くか」を回していなかった。**

---

## 参照

- [IT6 計画](iteration_plan-6.md)
- [IT6 ふりかえり](retrospective-6.md)
- [IT6 実装レビュー](../review/IT6実装_review_20260808.md)
- [リリース計画](release_plan.md)
