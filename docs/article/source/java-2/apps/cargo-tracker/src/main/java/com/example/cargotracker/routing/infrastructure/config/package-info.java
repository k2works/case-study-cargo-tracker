/**
 * 経路コンテキストのドメインサービスを組み立てる設定。
 *
 * <p><strong>ドメインは Spring を知らない</strong>（ArchUnit ルール 2）。
 * 依存を注入する側をインフラ層に置く。概算費用の単価は設定値である（ADR-008）。
 */
package com.example.cargotracker.routing.infrastructure.config;
