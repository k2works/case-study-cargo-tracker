/**
 * 荷役モジュールのコマンドサービス（ユースケース実行）。
 *
 * <p>ACL ポートが運ぶ素の値を荷役のことばへ翻訳し、集約に判断を委ね、
 * 結果を永続化する。<strong>判断そのものは集約が行う。</strong>
 */
package com.example.cargotracker.handling.application.internal.commandservices;
