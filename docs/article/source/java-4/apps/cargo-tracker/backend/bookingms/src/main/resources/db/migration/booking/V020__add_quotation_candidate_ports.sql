-- 候補ごとの経由港（US01 §受入基準 3・IT14）。
--
-- **候補ごとに違う。** 出発地と目的地だけを画面で繋ぐと、どの候補も同じ経路に
-- 見え、営業担当者は案を選び分けられない。正典の S13 は候補ごとに
-- 「JPTYO > SGSIN > USNYC」と並べている。
--
-- **既存行は空にする。** 見積は作られたあと変わらないので、列が無かったころの
-- 行は経由港を持てない。NOT NULL にせず、読み口が「分からない」を扱える形にする
-- （不変条件の追加は既存行を壊す・IT12 の教訓）。
ALTER TABLE quotation_candidate ADD COLUMN ports VARCHAR(200);
