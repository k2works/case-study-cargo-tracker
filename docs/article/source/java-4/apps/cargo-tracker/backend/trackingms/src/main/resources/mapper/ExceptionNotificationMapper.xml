<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cargotracker.tracking.infrastructure.persistence.ExceptionNotificationMapper">

    <!--
      追記の表なので、リプレイで増えないのは主キーの働きによる。主キーは元イベントの
      識別子で、再配送も読み直しも同じ行に落ちる（DO NOTHING）。内容は書き換えない
      ——伝えた事実は後から変わらない。
    -->
    <insert id="insert">
        INSERT INTO exception_notification (
            event_id, tracking_number, exception_id, means, summary,
            notified_by, notified_at, projected_at
        ) VALUES (
            #{eventId}, #{trackingNumber}, #{exceptionId}, #{means}, #{summary},
            #{notifiedBy}, #{notifiedAt}, #{projectedAt}
        )
        ON CONFLICT (event_id) DO NOTHING
    </insert>

</mapper>
