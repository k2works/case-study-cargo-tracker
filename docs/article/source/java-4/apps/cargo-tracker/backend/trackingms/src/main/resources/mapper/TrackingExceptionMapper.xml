<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cargotracker.tracking.infrastructure.persistence.TrackingExceptionMapper">

    <!--
      追記の表なので、リプレイで増えないのは主キーの働きによる。主キーは例外の
      識別子で、再配送も読み直しも同じ行に落ちる（DO NOTHING）。内容は書き換えない
      ——起票の内容は変わらないし、対応の進みは別の UPDATE が書く。
    -->
    <insert id="insert">
        INSERT INTO tracking_exception (
            exception_id, tracking_number, exception_type, response_status, urgent,
            unlocode, description, resolution, booking_id, occurred_at, resolved_at,
            projected_at
        ) VALUES (
            #{exceptionId}, #{trackingNumber}, #{exceptionType}, #{responseStatus}, #{urgent},
            #{unlocode}, #{description}, #{resolution}, #{bookingId}, #{occurredAt},
            #{resolvedAt}, #{projectedAt}
        )
        ON CONFLICT (exception_id) DO NOTHING
    </insert>

</mapper>
