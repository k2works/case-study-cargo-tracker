---
type: Plan
title: "イテレーション 6 ふりかえり"
tags: [plan]
status: draft
generated: { by: claude-code/claude-opus-5, at: 2026-09-06T10:31:28Z }
---

# イテレーション 6 ふりかえり

| 項目 | 内容 |
| :--- | :--- |
| イテレーション | IT6（Release 0.2 経路設計と予約確定・中盤） |
| 期間 | 2026-09-06 |
| 計画 SP / 実績 SP | 8 / 8（達成率 100%） |
| コミット数 | 38 |
| 変更 | 約 125 ファイル |

## Keep（続けること）

### K1. 引き継ぎ枠を Day 1 の独立コミットに閉じる（2 回目）

IT5 に続き、引き継ぎ 3 件（H.1〜H.3）を Day 1 に置いて繰越ゼロで返しました。**「余力次第」にしないことが 2 回続けて効いています。** IT5 のふりかえりで書いた「落とした負債は育つ」がそのまま守れています。

しかも **H.1（キャプチャの撮り直し）が欠陥を 1 件連れてきました**。負債の返済は、返済そのもの以上のものを返すことがあります。

### K2. 決定の数だけ検査を用意する（ADR-0009）

ADR-0009 は決定が 3 つあり、検査も 3 つ対応させました。決定 2（誤配は差し戻せない）は集約のイベントでは `MISROUTED` に到達できないので、`RoutingStatus` の全値を回す形にしています。**述語を緩めて赤を見ました。**

IT5 の architect 指摘（「決定が 2 つで検査が 1 つなら、片方は文章のまま」）を計画段階から織り込めています。

### K3. 壊して赤を見る

本 IT で足した検査のうち、次を実際に壊して赤を確かめました。認可の宣言（2 回）・通知の冪等性・イベントの再利用・状態の呼び名・件数の表示・条件の記録。**「書いたつもり」で終わった検査がありません。**

### K4. クラスタ E2E とキャプチャを「検査」として回す

Day 9 のクラスタ E2E と Day 1・Day 10 のキャプチャ生成が、**単体・投影・HTTP のどれも緑のまま通り抜けた欠陥を 4 件**捕まえました。IT5 の学び（「クラスタ E2E はモックが通す欠陥を捕まえる」）が再現し、そこにキャプチャが加わりました。

## Problem（問題だったこと）

### P1. 「設計やコメントに書いた保証」が 2 件破れていた

- `ui_design.md` に「確定済みの旅程は S22 に残ります」と**自分で書いた**のに、画面は状態で出し分けていて戻した瞬間に消えていた（クラスタ E2E で発覚）
- 経路設定状態の呼び名が、実装だけ正典から逸れていた（未設計/設計依頼済み/設計済 ↔ 正典は 未設定/設計依頼中/設定済）。**IT5 から誰も気づいていませんでした**

どちらも「書いた」ことで満足し、赤で固定していませんでした。IT5 の Try（「書いた保証は赤で固定する」）を**自分が書いた設計ドキュメントにも適用できていませんでした**。

### P2. 値が層をまたぐところで 2 回落とした

- 調整した除外港と起点が投影に置き場が無く、集約 → 投影 → 探索の途中で捨てられていた（V011 で追加）
- `notified_by` を NOT NULL にしたため、ヘッダの無い呼び出しが 500 になっていた（V013 で修正）

**どちらも配線して初めて出ました。** 集約の検査も投影の検査も緑のままで、どちらも自分の層しか見ていませんでした。

### P3. モックが本物より甘い形を 3 回作った

S34・ダッシュボード・S22 で、1 つの応答本体をすべての問い合わせに返すモックが「本物が返さない形」で緑になっていました。**ダッシュボードは真っ白になっていた**のに、テストは通っていました。

IT5 のメモリ（「モックを本物より甘くしない」）があったのに、既存のモックの形をそのまま踏襲して 3 回繰り返しました。

### P4. 「US ごとに品質ゲートを回す」を 2 IT 続けて守れなかった

成功基準に「層別カバレッジと SonarQube を US ごとに 1 度回す」と置き、**Day 4・Day 5・Day 8 のタスクにまで落としたのに踏みませんでした**。IT5 の Try（T4）をそのまま持ち越したのに、2 回連続で守れていません。

**結果として、層別カバレッジは「CI が落ちてから」回すことになり 2 回赤にし、SonarQube はクローズで初めて回して 3 件直しました。** 置き方（タスク表に書く）では守られないことが 2 回で分かったので、**踏まないと次へ進めない形**にする必要があります。

### P5. JaCoCo のクラス単位の閾値で CI が 2 回落ちた

タスクごとに `--tests` で絞って回し、「緑」と判断していました。分岐を足すたびに閾値へ近づくのに、`build` を回すのはタスクの区切りだけでした。落ちた分岐（null を送る・片方だけ設定する）は**実際に起こりうる入力**だったので、埋めた検査自体には意味がありました。

### P6. 計画の記述に誤りが 2 件あった

- 認可の順序（`AntPathMatcher` の `*` は `/` をまたがない）。**実装して実測するまで誤りに気づけませんでした**
- 画面遷移の注が、着手前の検証で覆っていたのに残っていた

前者は「実装を読んで確かめた」と計画に書いていたのに、読み違えていました。

### P7. 受入基準の「等」を読み飛ばしていた

US10 §受入基準 2 は「期限延長・**経由地追加**・**貨物種別変更**等」と並べています。計画では経由地追加だけを未達として拾い、**貨物種別変更を未達として数えていませんでした**（設計判断で US32 に振ったつもりでしたが、US32 は仮受付にしか使えないので、実際には手段がありません）。クローズの自前点検で見つけました。

**受入基準に並ぶ項目は、1 つずつ数える。** 「等」でまとめられた列挙は、読み飛ばしやすい形をしています。

### P8. 並列レビューを「返らない」と誤判断し、誤った記録を 3 つの文書に書いた

クローズの最初に 5 視点を起動し、**15 分ほどで「返らない」と決めつけました**。ふりかえり・完了報告書・レビュー記録に「1 件も返らなかった」と書き、自前の点検で代替する形で締めようとしました。

**実際には約 40 分後に全 5 視点が返り、高 5 件・中 9 件の指摘がありました。** IT5 の 4〜5 分を基準にしたのが誤りで、エージェントの `idle` 表示は結果の有無を表しません（結果が長いと分割して届きます）。

**自前の点検は代わりになりませんでした。** 自前で見つけた高 2 件はどちらも「自分が書いた文書と実装の食い違い」で、エージェントが見つけた**読み口の欠落・実装と ADR の逆転・検査が配線を踏んでいないこと・港コードの型を通していないこと・旧経路を伝えたまま気づけないこと**は 1 件も出ていません。

**視点をまたいで一致した指摘が 3 件あり、いずれも実在の欠陥でした。** 1 視点だけの指摘より優先して見る価値があります。

### P9. 「記録と読み口は対で出す」を同じ IT の中で守り切れなかった

`cargo_notification` は読み口まで作ったのに、**`return_reason` は書きっぱなしで SELECT する箇所が 0 件**でした。理由の入力を必須にしておいて誰にも届かない状態です。既存の学びとして持っていたのに、列を足すときに適用できていませんでした。

### P10. 層ごとの検査が全部緑でも、層をつなぐ配線を誰も踏んでいなかった

条件を投影から読んで探索へ渡す組み立てを、**どの検査も踏んでいませんでした**（スタブが引数を捨てていた）。集約・投影・ACL・探索器の検査はすべて自分の層だけを見ています。潰しても全部緑になる状態でした。

### P11. 計画の「成功基準」を埋め忘れ、指摘されるまで気づかなかった

クローズのステップ 3 で **DoD だけを埋めて、その上にある「成功基準」16 件を空のまま残していました**。埋めてみると 1 件が未達（P4）で、達成した 15 件のうち 3 件にも注記が要りました。

**同じ文書の中に確認すべきチェックリストが 2 つあり、片方だけ見ていました。** クローズのチェックリスト（スキル側）は「`iteration_plan-N.md` を実績で更新」としか言わないので、文書側に何個チェックリストがあるかは自分で数える必要があります。

### P12. Pod を作り直した直後のクラスタ E2E が 1 件落ちた

`k8s:wait` が Ready を返した直後の初回実行で 1 件落ち、再実行で通りました（2 回目・3 回目とも 14/14）。**再実行で通ることを理由に流さず**、記録に残しています。Ready と「投影が暖まっている」は別です。

## Try（次に試すこと）

| # | 内容 | いつ | 期待する効果 |
| :--- | :--- | :--- | :--- |
| T1 | **設計ドキュメントに書いた「〜は残る」「〜になる」を、その変更の中で赤にする。** 自分が書いた保証ほど疑いにくい | IT7 の各タスク | P1 の再発防止 |
| T2 | **利用者に見せる文字列は、設計の要素表を読む canon テストで固定する。** IT6 で `RoutingStatus` に置いたものを、`BookingStatus`・`TransportStatus` にも広げる | IT7 序盤 | P1 の同型（他の列挙）を先に潰す |
| T3 | **値を足したら「集約 → イベント → 投影 → 読み口 → 画面」を 1 本読み直す。** 途中で落ちても層ごとの検査は緑になる | IT7 の各タスク | P2 の再発防止 |
| T4 | **モックは URL で出し分ける形を既定にする。** 1 つの本体を全部に返すモックを新しく書かない | IT7 の各タスク | P3 の再発防止 |
| T5 | **分岐を足したタスクの終わりに `jacocoTestCoverageVerification` を回す**（`build` 全体でなくてよい） | IT7 の各タスク | P5 の再発防止・CI の往復を減らす |
| T5b | **US を終えたコミットのメッセージに、回した品質ゲートの結果を 1 行書く。** 書けないなら回していない。**3 IT 目なので、置き方を変える** | IT7 の各 US | P4 の再発防止 |
| T6 | **計画に「実装を読んで確かめた」と書くときは、確かめ方（実行したコマンドや壊して赤を見たこと）も書く** | IT7 の計画 | P5 の再発防止 |
| T7 | **受入基準の列挙は 1 項目ずつ表にして、満たす／未達を個別に書く。** 「等」でまとめない | IT7 の計画 | P6 の再発防止 |
| T7b | **DoD の回数を伴う項目は、回した記録（日付・結果）を計画に残す** | IT7 の計画 | 記録の精度 |
| T8 | **並列レビューは 45 分待つ。** 15 分で見切らない。待つ間は他の工程（品質ゲート・進捗反映）を進め、**返着するまでレビュー結果に関する記述を書かない** | IT7 のクローズ | P7 の再発防止 |
| T8b | **列を足したら、同じ変更の中で読み口まで作る。** 書き込みだけのコミットを作らない | IT7 の各タスク | P8 の再発防止 |
| T8d | **クラスタ E2E は Pod を作り直したあと 1 度空回ししてから測る。** Ready と暖まっていることは別 | IT7 Day 9 | P12 の再発防止 |
| T8e | **クローズで計画を更新するときは、文書内の `- [ ]` を全部数えてから埋める。** チェックリストが 1 つとは限らない | IT7 のクローズ | P11 の再発防止 |
| T8c | **層をつなぐ組み立てを 1 本踏む検査を置く。** スタブは受け取った引数を捕捉し、捨てない | IT7 の各タスク | P9 の再発防止 |
| T9 | **引き継ぎ枠を Day 1 の独立コミットに置く形を 3 回目も続ける** | IT7 Day 1 | K1 の継続 |

## 次イテレーションへの引き継ぎ

| # | 内容 | 優先度 |
| :--- | :--- | :--- |
| 1 | **US10 §受入基準 2 の「経由地追加」が未達。** 探索の入力に「必ず通る港」が無い（除外はある）。`RouteSearchSpecification` に追加が要る | 中 |
| 1b | **US10 §受入基準 2 の「貨物種別変更」が未達。** 経路設計に入った予約の貨物種別を変える手段がどこにも無い（US32 は仮受付だけ）。**ADR が要る**——危険物申告・温度条件の検査が付いて回る | 中 |
| 2 | **US12 §受入基準 2 の「料金概算」が未達。** US21（IT13）が前提。通知内容に欄を置いていない | 低（IT13 まで待つ） |
| 3 | **`BookingStatus`・`TransportStatus` の呼び名にも canon テストを広げる**（T2） | 高 |
| 4 | **`cargo_summary` の列が増え続けている**（IT6 で 6 列追加）。読み口ごとに必要な列だけを返す形を検討する | 低 |
| 5 | 通知の宛先を荷主のメールアドレスから初期表示する（いまは毎回手入力）。荷主の連絡先は `shipper` にある | 中 |
| 6 | ADR の承認と `verify`（0004〜0009）は人の署名。代筆しない | — |
| 7 | Sonar の Code Smell 46 件（既存分）。新規コードは 0 だが、既存の指摘は減っていない | 低 |
| 8 | **差し戻された営業に、システム上の打ち手が 1 つも無い**（レビュー 高）。営業が押せる操作（協議の結果を返す）が要る。新しいコマンドとイベントが要るので ADR も検討する | 高 |
| 8b | **探索が落ちていると、条件も差し戻しも使えない**（レビュー 高・2 視点が独立に指摘）。US10 §4 が最も必要な場面で使えない。条件を予約から組む形に変える | 高 |
| 8c | `assignRoute`／`notifyShipper`／`returnToRouting` が生の `!=` 比較のまま。canon テストが正規表現に依存していて、述語化のたびに追随が要る | 中 |
| 9 | 経路設計者が「自分がもう差し戻したか」を S30 で読めない（レビュー 中） | 中 |
| 10 | 探索が落ちていると条件も差し戻しも使えない（レビュー 中）。条件は予約から組めるので分離できる | 中 |
| 11 | 確定済みの経路が外れる警告が、押す前に画面に出ない（レビュー 中） | 中 |
| 12 | 航海キャンセル後に組み直しが要る予約が、誰の一覧にも立たない（レビュー 中。H.2 の続き） | 中 |
| 13 | 所要日数の起点が荷主に伝わらない。計算式が画面とサーバの 2 か所にある（レビュー 中） | 中 |
| 14 | 到着期限に過去日を選べる（`min` 未指定）・「所要日数」と「所要時間」の呼び方が揺れている（レビュー 低） | 低 |

## 関連ドキュメント

- [IT6 計画](iteration_plan-6.md)
- [IT6 完了報告書](iteration_report-6.md)
- [IT5 ふりかえり](retrospective-5.md)
- [ADR-0009 営業への差し戻しを状態遷移にしない](../../adr/cargo-tracker/0009-condition-review-is-not-a-state-transition.md)
