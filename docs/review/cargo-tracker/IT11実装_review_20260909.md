---
type: Review
title: "IT11 実装レビュー"
tags: [review]
status: draft
generated: { by: claude-code/claude-opus-5, at: 2026-09-09T06:34:30Z }
---

# IT11 実装レビュー（2026-09-09）

## 対象

| 項目 | 内容 |
| :--- | :--- |
| 対象 | IT11 の実装（US20 破損・紛失例外を処理する・US28 誤配を検知して経路を再設計する） |
| 範囲 | 8 コミット・約 110 ファイル |
| 実施 | XP 5 視点（programmer / tester / architect / technical-writer / user-representative）を**クローズの最初に並列起動**。4 通が 2 分以内に返着、**user 視点は返らず再送を依頼** |
| 依頼の形 | IT10 と同じく出力の形を指定（高と中だけ・1 件 1 行の表・3000 字以内）。**返着した 4 通とも指定どおりの形**で、切れたものは無い |

## 結果

| 重要度 | 件数 | 対応 |
| :--- | :--: | :--- |
| 高 | 9 | **すべてクローズ前に修正** |
| 中 | 14 | 8 件を同じ変更で修正、6 件は IT12 へ（下表） |

**「片方にだけ書いた」形の欠陥が 3 件**出ました。同じ業務判断が 2 か所に必要なのに、後から足した経路にだけ書いていない——預かった荷役の適用（契約イベントを出さない）、誤配の印（立てるが降ろさない）、取り消し（預かりを外さない）です。

## 高（すべて修正済み）

| # | 視点 | 指摘 | 対応 |
| :--- | :--- | :--- | :--- |
| H1 | programmer | 預かった荷役の適用が、その場で適用したときの後段（`CargoDeliveredEvent`・誤配の自動起票）を出さない | 「状態を進めたあとにやること」を `afterAdvancing` に括り出し、両方の経路から呼ぶ。**預かった引取が精算へ伝わる**ことを赤で固定 |
| H2 | programmer | 取り消しが預かり中の荷役を外さない（預かりは状態を進めていないので早期 return する） | `revert` の先頭で預かりを外す。**取り消した荷役が解決時に適用される**ことを赤で固定 |
| H3 | architect | `isSatisfiedByRedesign` が出発地の検査を**置き換えではなく削除**しており、貨物のいない港から出る旅程を集約が受ける | `BookingMisroutedEvent.unLocode` を集約が覚え、起点＝誤配地を検査する。**列が無かったころの行では検査しない**（不変条件の追加は既存行を壊す） |
| H4 | tester | `misrouteIdFor` に検査が無い（**クラスタで実際に投影を止めた欠陥**） | 長さ 36・UUID の形・同じ荷役から同じ ID を赤で固定 |
| H5 | tester | `BookingController.departFrom` の誤配分岐に検査が無い（US28 §4・§5 の中核がここだけにある） | `departFrom` を package-private にし、4 ケースの検査を新設（誤配・通常・未設定・誤配だが現在地不明） |
| H6 | tester | `findOpen(true)` に検査が無い（US28 §8 の唯一の読み口） | 投影の IT で「既定では出さない・切り替えれば読める」を固定 |
| H7 | writer | 15 章「出発港は現在地に固定されます」が実装と違う（画面は**編集可能な絞り込み欄**で、サーバだけが現在地を優先していた） | **実装を正した**——誤配のときは「出発港（現在地）」として読み取り専用にする。押せない・効かない操作を並べない |
| H8 | writer | 14 章が説明する「未連絡」がキャプチャに写っていない | キャプチャのモックを直し（緊急の行を未 escalation に）撮り直した |
| H9 | （クラスタ E2E で発見） | 対応内容を入力している最中に 30 秒ごとの読み直しが走り、**入力欄が消える** | フォームを開いているあいだはポーリングを止める。検査を新設 |

## 同じ変更で直した中

| # | 視点 | 指摘 | 対応 |
| :--- | :--- | :--- | :--- |
| M1 | programmer | 誤配の印を立てる経路だけで降ろす経路が無い（組み直してもバナーが残る） | 新しい経路で動き出したとき（誤配から荷役で先へ進んだとき）に降ろす。**例外の対応中は降ろさない** |
| M2 | programmer | 超過日数を 2 か所で数えている（ACL のコメントは「1 か所」と書いている） | コメントを事実に合わせた。**答えるべき問いが違う**（候補は探索条件、確定は予約の期限）ことを書いた |
| M3 | programmer | `List.copyOf` の理由を赤にできる検査が無い | 預かり 2 件を順に適用する検査を新設 |
| M4 | tester | escalation の集約・投影の検査が無い | 「緊急なら出す・緊急でなければ出さない」と投影の `escalated_at` を新設 |
| M5 | tester | S22 バナーの「日時」が未アサート | 業務タイムゾーンの表示を赤で固定 |
| M6 | tester | `overdueDays` の境界に 1 日超過が無い | 追加 |
| M7 | writer | 例外一覧の到着期限が UTC の ISO 文字列で写っていた | **キャプチャのモックが本物より甘かった**（サーバは DATE を返す）。モックを直した |
| M8 | architect | `basis_exception_id` が VARCHAR(36) のまま（V010 で 64 に広げた列への論理参照） | `data-model.md` を 64 に揃えた（実装は US21・IT13） |

## IT12 へ送る中（6 件）

| # | 視点 | 指摘 | 送る理由 |
| :--- | :--- | :--- | :--- |
| N1 | architect | **Dead Letter Queue が無い。** 1 件の不正イベントで Event Processor が止まり後続が全部届かない構造は残っている | 設計の判断が要る（退避先・再投入の手順・`attention_item` との関係）。**IT11 で実際に起きた事象**なので優先度は高い |
| N2 | architect | 旧ペイロード（`overdueDays` 欠落）を新レコードへ読み戻す検査が無い | ゴールデンは新しい形の書き出しを固定するだけ。旧 JSON を資産として置く仕組みが要る |
| N3 | programmer | `findOpen` の `WHERE (#{includeResolved} OR ...)` は boolean を述語に置く形で、エンジンによって解釈が分かれうる | 第二方言を持たないので実害は出ていない（全統合テストが本番と同じ PostgreSQL 16） |
| N4 | programmer | 「0 を入れる互換コンストラクタ」が 3 本（`CargoRoutedEvent`・`RouteCandidateDto`・`RouteCandidate`） | 呼び出し側が新形に揃った時点で外す。いま外すと変更が広がる |
| N5 | writer | 確認ダイアログのキャプチャが無い（章内で唯一画像の無い操作） | 撮るには候補を選んだ状態を作る必要があり、キャプチャ spec の組み立てが増える |
| N6 | （通しの E2E で発見） | **未解決の誤配が経路設計作業一覧（S30）の表示上限を食う。** 誤配は並びの先頭に来る設計なので、滞留すると通常の設計依頼が沈む | 画面は「N 件のうち M 件」と知らせているが、**絞り込みが無い**。S30 に「誤配だけ / 設計待ちだけ」の切替が要る |

## 対応せず（理由を明記）

| # | 指摘 | 理由 |
| :--- | :--- | :--- |
| — | architect: V002 の残存コメント（`misrouted` は「後の IT で足す」）を消す | **適用済みマイグレーションは編集しない**（IT9 で実測。CI は緑のまま動いているクラスタだけが起動しなくなる）。V009 のコメントが読者を導く |
| — | writer: バナーと候補のキャプチャが別の貨物 | 同じ貨物で撮るにはキャプチャ spec の前提を共有する仕組みが要る。**文章は「同じ貨物を追う手順」として読めるが、画像は例示**である旨は本文から読み取れる |

## 良かったと指摘された点

- 預かり（`HandlingDeferredEvent`）と反映不可（`HandlingNotAppliedEvent`）を分けた判断が、**追跡管理者の次の行動の違い**という理由まで書かれている（programmer）
- 緩めた守りの反対側を毎回対で置いている（tester）——超過候補・打ち消しイベント・誤配バナー・確認ダイアログのいずれも否定側あり
- クラスタ E2E の 6 か所の直しが、**待ち方に寄せた修正でアサートを弱める症状隠しになっていない**（tester）
- `CargoDeliveryRevertedEvent` を `TransportStatusRevertedEvent` と分けて出し、IT10 で「塞いだ」判断を補償の設計として開き直している（architect）
- ADR-0013 が決定 4 つに検査 4 つを対応させ、対象を名簿でなく走査で導いている（architect）
- 15 章が「なぜその作りなのか」を業務の言葉で説明し、章立て・索引・01 章・`mkdocs.yml` の 4 点が一致している（writer）
