---
type: Review
title: "IT12 実装レビュー（2026-09-10）"
tags: [review]
status: draft
generated: { by: claude-code/claude-opus-5, at: 2026-09-10T13:17:52Z }
---

# IT12 実装レビュー（2026-09-10）

## レビュー対象

- IT12（US29 通関申告 6 SP + 負債枠 2 + 引き継ぎ枠 2）の実装
- `1655b2a1c..fe502decf` の 43 コミット・約 200 ファイル（追加 11996 行）
- 対象ドキュメント: `docs/development/cargo-tracker/iteration_plan-12.md`・`docs/manual/16-通関を通す.md`・`docs/adr/cargo-tracker/0014-poison-events-are-parked-not-blocking.md`

## 進め方（記録）

並列 5 視点（`xp-programmer` / `xp-tester` / `xp-architect` / `xp-technical-writer` / `xp-user-representative`）をクローズの最初に起動した。**48 分待っても 1 通も返着せず**、催促を 5 通送っても応答が無かったため、`developing-review` の逐次フォールバックに切り替えて 5 視点を自分でたどった。

**その後、5 通すべてが届いた**（起動から約 40 分後に完了していたが、通知が届いたのはさらに後。長さ制限で切れた 4 通には続きを個別に求めた）。**遅着分に実欠陥が入っていた**——逐次で回した分では出なかったものが 5 件ある。待たずに確定させると、これらは IT13 で別の症状として現れていた。

**Try T7「5 通そろうまでクローズしない」は、待ち方を間違えた形で未達である。** 48 分で「返らない」と判断したが、実際には返っていた。IT13 では「上限を決めて逐次に切り替え、**返着したら統合する**」を Try にする（返着を捨てないことが要点である）。

以下の「改善提案」は逐次分と遅着分を統合したものである。

## 総合評価

US29 は 3 サービスの連鎖（handlingms → trackingms → 引取のガード）まで通り、クラスタ E2E の通し 21/21・受け入れ 19/19・フロント 418 件・SonarQube の品質ゲート（backend / frontend とも PASS・Bug 0・Vulnerability 0）が緑である。一方で、**層ごとに緑でも実務では足りない箇所が 2 件**あった。どちらも「記録はあるのに読み口が繋がっていない」形で、単体・受け入れ・E2E のいずれも判別しなかった。

## 改善提案（重要度順）

### 高（クローズ前に対応すべき）

逐次分 3 件（#1〜#3）に、遅着分 5 件（#H4〜#H8）を加えた 8 件。すべてクローズ前に直した。

| # | 提案 | 箇所 | 指摘元 | 理由 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 留置から出たあとの営業日数が 0 に落ちる。`held_business_days` 列を返していたが**列は常に 0**（確定値を持つのは契約イベントで、投影は内部イベントだけを読む） | `CustomsQueryHandler#heldBusinessDaysOf` | tester | 5 営業日留置されて通関済になった申告が「留置 0 営業日」に見える。US21（IT13）の料金調整の根拠がそこで消える。javadoc は「留置から出たあとは列の確定値を返す」と書いており、**書いた保証が守られていない** |
| 2 | S02 の「督促してください」が全件の一覧に着く。`?overdueOnly=true` を渡していたのに S52 が URL を読んでいなかった | `CustomsListPage`・`DashboardPage` | user-representative | 件数から来た人が、どれが督促の対象かをもう一度自分で絞ることになる。**気づく手段が次の行動へ繋がっていない**。`DashboardPage.test.tsx` は href を検査しているが、着いた先の挙動は誰も見ていなかった |
| 3 | 引取待ち（H.8）が「通関の状態はこの画面では分かりません。荷主または通関業者に確かめてください」と言い続けていた | `AwaitingClaimPage`・`docs/manual/14-引取と例外を扱う.md:60` | user-representative | US29 で読めるようになり、通関済でなければ引取そのものが断られる。**現場が確かめる先を間違えたまま窓口で待たせる**。IT11 の文言が残っていた |

| #H4 | **決着した例外と同じ識別子で再起票できない。** 留置 → 審査中 → 再度留置（どちらも未決着なので集約が許す遷移）で同じ識別子に戻るが、`containsKey` で黙って捨てていた | `TrackingActivity#registerException` | architect | **再留置が誰にも見えない**：状態は例外発生に戻らず、未解決の一覧にも督促にも出ない。引取は投影の最新状態で止まるので、気づく手段だけが消える |
| #H5 | **「不可」でも税関保留を解決していた** | `CustomsHoldStatus#resolvesHold` | user-representative | 留置からは出ているが、返送・再申告・廃棄が決まるまで貨物は港に残り保管料が積み上がる。引取は永久に断られるのに未解決の一覧からも督促からも消えて**誰も追わない貨物**になる |
| #H6 | **申告日時がブラウザの時間帯で送られていた**（`new Date(...).toISOString()`。コメントは「業務タイムゾーンで送る」と書いていた） | `CustomsRegisterPage` | user-representative | 海外港や UTC の端末から入れると数時間ずれて記録され、エラーは出ない。留置営業日数の起点に効くので督促の判定が 1 日ずれる |
| #H7 | **決着した申告にも更新フォームが出ていた。** `UPDATABLE_STATUSES` が現在の状態に依らない固定リストで、いまと同じ状態も選べた | `CustomsDetailPage`・`api.ts` | tester | 押すとサーバが断る。テストはフィクスチャが常に `PENDING` なので緑だった（**モックが本物より甘い**形） |
| #H8 | **宣言した安全網が実在しなかった 2 件。** `portTimeZone.canon.test.ts`（存在しない）と、通関の呼び名の canon テスト（`api.ts` が「画面で対応表を持たない」と書いた 100 行下でハードコード） | `portTimeZone.ts`・`features/customs/api.ts` | programmer | 港を足すと黙って JST 扱いになる。呼び名は正典と二重定義 |

### 中（対応推奨）

| # | 提案 | 箇所 | 指摘元 | 理由 |
| :--- | :--- | :--- | :--- | :--- |
| #M10 | **`@EventHandler` を持つパッケージの列挙漏れを拾えない検査。** `infrastructure.projection` だけを見ており、`application.reaction` のような新しいパッケージは対象外だった（bookingms・trackingms は手で列挙して偶然揃っていた）。`application.yml` が挙げていた検査名（`EveryProcessorHasADeadLetterQueueTest`）も実在しなかった | `EventSourcedServicesHaveTheSameShapeTest` | architect | ADR-0014 決定 2「書き忘れを人の注意で防がない」を満たしていない。走査側から検査する形に直し、**列挙を 1 つ外して赤になることを確認**した |
| #M11 | **canon を読むテスト 6 本が Gradle の入力として宣言されていない。** 要素表だけを書き換えるとテストは UP-TO-DATE で走らず、**正典と実装が食い違ったまま緑**になる | `backend/build.gradle.kts` | tester | `tasks.withType<Test>` に 1 か所置いて 6 本まとめて解決。**正典を書き換えると走ることを確認**した |
| #M12 | **S52 の一覧が 500 件で無言に打ち切られる。** 読むときに数えて並べる設計なので SQL では絞れない | `CustomsQueryHandler`・`CustomsListPage` | architect | IT11 の H.2（S30 は上限到達を知らせる）と同じ形が新画面で再発。督促の対象が上限の外に沈む |
| #M13 | **例外一覧の税関保留から通関へ行けない。** 対応は S53 で通関状態を更新することで、追跡詳細では何もできない | `ExceptionListPage` | user-representative | 発生状況に申告番号は入っているがリンクでないので、書き写して探し直すことになる |
| #M14 | **列挙の一部しか回していない検査**（`requiresCustomsClearance` は `CLAIM` と `LOAD` だけ）、**履歴のリプレイ冪等性が未検査**、**必須項目 3 件が落としても赤にならない**、テスト名が実装と食い違い（`readsHistoryFromTheEventStore`） | handlingms の各テスト | tester | いずれも「壊しても緑」の形。すべて直した |
| 4 | 「履歴はイベント列から読む（追記専用の表を作らない）」が 4 か所に残っていた | `HandlingQueries#FindCustomsHistoryQuery` の javadoc・`architecture_backend.md:893`・受け入れ feature のコメント・計画の ER 図の note | technical-writer / programmer | 正典（`data-model.md`）は注 N3 で直したのに、書いた側の文が古いまま。**コメントは仕様として読まれる**ので、次の IT が古い設計を信じる |
| 5 | ドメイン層の未検査分岐 3 件（範囲の null・未登録申告の更新・状態なしの更新） | `HolidayCalendar`・`CustomsDeclaration` | tester | JaCoCo のドメイン層ゲート（BRANCH 0.9）が分割フルビルドで落ちた。守りを書いたが破るテストが無かった |
| 6 | `heldOver` が定義済み未使用 | `CustomsDeclaration`（削除） | programmer | **読む側が無い。**「3 営業日超」を判定しているのは投影から多数を読む `CustomsQueryHandler` で、両方に閾値を置くと本番と検査が別の判定を持つ |
| 7 | `DayOfWeek` を `==` で比較（SonarQube S8696 / Bug） | `HolidayCalendar#isBusinessDay` | programmer | 週末の集合への `contains` に置き換え、意図（どの国でも土日は休み）が読めるようにした |
| 8 | `updateStatus` の 8 引数 | `CustomsDeclarationMapper` | programmer | `changedAt` と `projectedAt` はどちらも `Instant` で、順序を 1 つ違えても型が合う。名前で渡す record にした |
| 9 | 通関の画面 2 枚が非推奨の `React.FormEvent` を使用（他の画面は React 19 の `SubmitEvent`） | `CustomsDetailPage`・`CustomsRegisterPage` | programmer | 新しく書いた 2 枚だけが古い型を使っていた |

### 低（記録して IT13 へ）

| # | 提案 | 箇所 | 指摘元 | 理由 |
| :--- | :--- | :--- | :--- | :--- |
| 10 | `customs_declaration.held_business_days` の削除 | V006（適用済み） | architect | 誰も読まない列が残る。**V006 はコメントも含めて編集できない**（checksum が変わると既存クラスタが起動しない）ので、追加マイグレーションで落とす |
| 11 | 引取待ちの一覧に通関状態の**列**を出す | `FindAwaitingClaimQuery` / `CargoOnVoyageView` | user-representative | いまは行から S52 へ送っている。列に出すには貨物ごとの最新申告を引く読み口が要る（N+1 を避ける形で） |
| 12 | 退避先から処理し直す入口・処理の列を分ける | ADR-0014「引き受けていないこと」 | architect | 計画の IT13 送りとして既に記録済み |
| 14 | 受け入れフィクスチャの固定日付（handling・tracking に残る分） | `HandlingSteps`・`CustomsSteps`・`TrackingSteps` | tester | **routing で実際に赤になった時限式**と同じ形。routing は直したが、残る 2 suite は同じ罠を抱えている |
| #L15 | **不変条件 3 の守りが競合と遅延に開いている**（投影を読むので同時 2 リクエストは両方通る）。部分ユニークインデックスを最後の砦に置く | `CustomsController`・`customs_declaration` | architect | **スキーマ変更なので確認が要る**。IT13 の追加マイグレーションで行う |
| #L16 | **`CountOverdueCustomsHoldsQuery` が誰からも dispatch されていない**（画面は一覧の `total` から数える）。配線すると母集合が違う（`findHeld()` は無制限・`search` は上限あり） | `CustomsQueryHandler` | programmer | 定義済み未使用。IT13 で消すか、同じ母集合に揃えて配線する |
| #L17 | **輸入港の国を見ずに日本の休日で数えている**（`CountryCode("JP")` 固定が 2 か所）。US29 の貨物は `USNYC` なので、実際は米国の休日で数えるべき | `CustomsDeclaration`・`CustomsQueryHandler` | tester | 理由は javadoc にあるが、`HolidayCalendarTest` の国別分岐が**本番経路で一度も踏まれない**。IT13 で `CargoSnapshot` の目的港から渡す |
| #L18 | **同じ判定が 2 か所**（記録時と読み取り時の営業日数）。用途は違うが、両者が同じ値を返す検査が無い | 同上 | tester | US21 で請求と画面の日数が食い違う余地 |
| #L19 | `AdrHasChecksTest` は「`## 検査` 節の有無」しか見ない（決定 4 つ・検査 2 つでも緑）、`ui_design.md` の ER 図・S52 図の細部、`release_plan.md` の重複リンク | 各所 | architect | 図と表の細部は直した。ADR の対応数の検査は IT13 |
| 13 | 残った Code Smell 34 件 | backend（新規コードの外） | programmer | `ApiExceptionHandler` の三項・`RouteSearchService` の複雑度など、本 IT で触っていない箇所。触っていないものを直すと本 IT の変更が読みにくくなる |

## 矛盾事項

| # | 視点 A | 視点 B | 論点 | 推奨判断 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | tester「決着後の営業日数は列に持つべき（読むたびに数えるのは無駄）」 | architect「判定を 2 か所に置かない」 | 確定値の置き場 | **読むときに数える**を採った。列に持つには投影が契約イベントを購読することになり、自分の BC の投影が自分で出した契約を読み返す形になる。数え方は 1 つのまま、留置中／決着後で終端だけを替える |

## 視点別の所見

### programmer（高 0 / 中 5 / 低 1）

コメントと javadoc が濃く、**なぜそうしたか**が追える。とくに `appender.append()` が即座に適用されることを踏まえて留置日数を先に数えている箇所（`CustomsDeclaration:111`）は、IT11 で作った同型の欠陥を踏まえた記述になっている。一方で「〜する」と書いた保証のうち 2 件（列の確定値・履歴の読み元）が守られておらず、**自分が書いた保証形ほど疑いにくい**という IT11 の教訓が再現していた。

### tester（高 1 / 中 1 / 低 0）

デモ項目 D1〜D11 はすべて固定先がある（D7 はクラスタ E2E、D8 は `TrackingControllerIT`、D9 のダッシュボード件数は `DashboardPage.test.tsx`、残りは受け入れ）。受け入れの feature に「ここに置かない理由」が書かれており、落ちているのではなく**置き場を選んでいる**ことが読める。3 営業日超の判定を受け入れで確かめられない理由（時計を進める手段が無く、本番の時計を差し替える口を開けるほうが危ない）も明記されている。足りなかったのは、決着後の営業日数と、守りを破る 3 本の赤。

### architect（高 0 / 中 0 / 低 2）

`HolidayCalendar` を handlingms に置いた判断は ADR-0001 の名簿と整合し、移す条件（billingms が保管料を数え始めたら）も書かれている。契約イベントは購読側が作れる分（`bookingId`・`heldBusinessDays`）を運んでおり、追記専用であることを踏まえた形。ADR-0014 は 3 つの決定にそれぞれ検査があり、引き受けていないこと（列が 1 本・再処理の入口が無い）が IT13 送りとして記録されている。

### technical-writer（高 1 / 中 1 / 低 0）

マニュアル 16 章は前提（どのロールで、どこから入るか）から書かれ、断られたときの対処表もある。14 章との相互リンクも通っている。ただし 14 章の 60 行目が IT11 の文言のままで、**実装と正反対のことを言っていた**。キャプチャは本文が説明する要素を写しているかを生成 spec の検査で固定する形になっており、今回そこに 3 つの検査を足した。

### user-representative（高 2 / 中 0 / 低 1）

通関担当者（登録）・追跡管理者（状態更新・督促）の 2 つの入口は、ダッシュボードと navbar の両方から到達できる。督促は S02 の件数 → S52 の絞り込み → 行 → S53 と繋がる設計になっていたが、**その繋ぎ目が 1 つ切れていた**（提案 2）。現場（引取）については、断られたときのメッセージが理由と現在の通関状態を伝えており、`ClaimCustomsGuardIT` が固定している。しかし断られる**前**に確かめる手段が画面に無く、しかも「分かりません」と書いてあった（提案 3）。

## 対応

**高 8 件・中 11 件をクローズ前に修正した。** 低 9 件は IT13 へ送る（計画の「IT13 へ送るもの」に記録済み）。うち #L15（部分ユニークインデックス）はスキーマ変更なので、確認を経てから行う。

修正のコミット: `f6e9d45ff`・`ff39cbd72`・`fe502decf`・`e80597006`・`b15f332f6`・`6d27b46db`（および本レポートの統合）。

修正後の確認: 分割フルビルド 5 群緑・フロント 418 件緑・キャプチャ 35 件緑・クラスタ E2E 通し 21/21 緑・SonarQube backend / frontend とも PASS。

## 追記: CI が見つけた時限式のフィクスチャ

クローズ確定前の push で CI が赤になった（routing の受け入れ 13/20）。**コードの後退ではない**——CI が緑だったコミットに戻しても同じ 13 件が落ちた。原因は**フィクスチャの日付が固定**だったことである。

航海の出発日時が `2026-09-10T09:00:00Z` 固定で、一覧は既定で出港済みを外すため、**現実の時刻がその時刻を追い越した瞬間に登録した航海が一覧から消える**。同じ日の 08:00 UTC の CI は緑、09:51 UTC の CI は赤。テストの意図（登録が投影に届くか）とは関係のない赤である。

日付を「今」から導くように直した（`SharedRoutingSteps.DEPARTURE` = 業務日付の翌週 09:00）。期間の絞り込みも固定日付をやめ、出発日から数える。**同型が handling・tracking の suite に残っている**ので IT13 へ送る（提案 14）。
