---
type: Review
title: "IT6 実装レビュー"
tags: [review]
status: draft
generated: { by: claude-code/claude-opus-5, at: 2026-09-06T08:15:53Z }
---

# IT6 実装レビュー

| 項目 | 内容 |
| :--- | :--- |
| 対象 | IT6（US10 条件調整・US11 経路紐付け・US12 荷主通知・引き継ぎ枠 H.1〜H.3） |
| 範囲 | `git diff 256e7277d..HEAD`（38 コミット・約 125 ファイル） |
| 実施日 | 2026-09-06 |
| 形 | XP 5 視点の並列レビューを起動 → **結果が返らず**、自前の点検で代替（下記） |

## 実施の形（正直に記録します）

**クローズの最初に 5 視点（programmer / tester / architect / technical-writer / user-representative）の並列レビューを起動しました。** IT5 のふりかえりで「クローズの最初に起動する」「無応答は『返らない』ではない」と決めたとおりです。

**しかし 40 分待ち、2 回の催促を送っても、結果が 1 件も返りませんでした。** IT5 では**約 4〜5 分で 5 視点すべてが返り**、提出 41 件・重複を除き 35 件の指摘がありました（[IT5 実装レビュー](IT5実装_review_20260905.md)）。今回は**エージェントの結果を 1 件も統合できていません**。同じ起動の仕方で結果がここまで違うので、**返らないことがある**と考えて手順を用意する必要があります。

そこで **IT5 と同じく自前の点検で代替しました**。ただし IT5 の教訓（「自前の確認は『書いたことが正しいか』を見て、『落としたものはないか』を見ていなかった」）を踏まえ、**受入基準の側から実装へ**、**設計ドキュメントの側から実装へ**の 2 方向で突き合わせています。

**この形は並列レビューの代わりになりません。** 見つけた 2 件はどちらも「自分が書いたものと実装の食い違い」で、**設計そのものへの疑いや、業務としての使い勝手の指摘は含まれていません**。IT7 のふりかえりに持ち越します。

## 自前の点検で見つけたもの（2 件）

### 高 1: マニュアルが「営業が条件を直せる」と書いていたが、直せない

| 項目 | 内容 |
| :--- | :--- |
| 場所 | `docs/manual/09-経路を設計する.md:73` |
| なぜ問題か | 「営業担当者が条件を直したら、そのまま設計を続けられます」と書いていたが、**営業は条件を直せない**。予約修正（S24）は仮受付の予約にしか使えず、経路設計に入った予約の条件を変えられるのは経路設計者だけである。差し戻された営業が画面を探し回ることになる |
| 直し方 | 「条件を実際に変えるのは経路設計者。営業は荷主と協議して結果を伝える」と書き直した。**営業の画面から直せないことも明記した** |
| 状態 | **対応済み** |

### 高 2: US10 §受入基準 2 の「貨物種別変更」を未達として数えていなかった

| 項目 | 内容 |
| :--- | :--- |
| 場所 | `docs/development/cargo-tracker/iteration_plan-6.md`・報告書 |
| なぜ問題か | 受入基準は「期限延長・**経由地追加**・**貨物種別変更**等」と並ぶが、未達として拾ったのは経由地追加だけだった。貨物種別は US32 が持つと設計判断したものの、**US32 は仮受付の予約にしか使えない**ので、経路設計に入った予約の貨物種別はどこからも変えられない |
| 直し方 | 未達として報告書・計画・ふりかえりに記録した。行き先（`AdjustRouteSpecificationCommand` に足すか、US32 を開くか）は **ADR が要る**と明記した |
| 状態 | **対応済み**（記録として） |

## 開発中に見つけて直したもの（6 件）

**クローズのレビューではなく、開発中の検査で出たものです。** 内訳は[完了報告書](../../development/cargo-tracker/iteration_report-6.md)の表を参照してください。**4 件は画面から踏むテスト（マニュアルのキャプチャ・クラスタ E2E）でしか出ませんでした。**

| # | 内容 | 見つかった場所 |
| :--- | :--- | :--- |
| 1 | 引き渡し済みの予約に引き渡しの導線が出て、押すと確定済みの経路が巻き戻る | キャプチャ |
| 2 | 調整した除外港と起点が投影に届いていない | 配線 |
| 3 | `notified_by` の NOT NULL でヘッダ無しの呼び出しが 500 | 配線 |
| 4 | 経路設定状態の呼び名が実装だけ設計から逸れていた | 受け入れテスト |
| 5 | 経路設計へ戻すと確定済みの旅程が画面から消える | クラスタ E2E |
| 6 | 読み口がモックに吸われてダッシュボードが真っ白 | キャプチャ |

## 対応しないもの

| 内容 | 方針 |
| :--- | :--- |
| 未通知の件数から `/bookings` へ行くが、一覧が未通知で絞られていない | **許容**。IT3 で置いた「引き渡していない予約」の行と同じ形。絞り込みを足すなら両方まとめて（IT7 の引き継ぎ 5 と併せて検討） |
| Sonar の Code Smell 46 件（既存分） | **保留**。新規コードは 0。既存分は触るときに直す |
| 通知の宛先を毎回手入力する | **次イテレーション**（ふりかえりの引き継ぎ 5） |

## 教訓

**並列レビューが返らないことがある。** IT5 では 4〜5 分で 5 視点すべてが返り、IT6 では 40 分待って 2 回催促しても 1 件も返りませんでした。**「クローズの最初に起動する」だけでは足りません。** 返らなかったときに何をするか（待つ上限・代替の点検の形・記録の仕方）を決めておく必要があります。

自前の点検は代わりになりません。今回見つけた 2 件はどちらも「自分が書いた文書と実装の食い違い」で、**自分の設計判断そのものを疑う指摘は 1 件も出ていません**。IT5 でエージェントが見つけた高 4 件は、まさにそこ（正典から黙って落としたコマンド 2 本）でした。

## 関連ドキュメント

- [IT6 計画](../../development/cargo-tracker/iteration_plan-6.md)
- [IT6 ふりかえり](../../development/cargo-tracker/retrospective-6.md)
- [IT6 完了報告書](../../development/cargo-tracker/iteration_report-6.md)
- [IT5 実装レビュー](IT5実装_review_20260905.md)
