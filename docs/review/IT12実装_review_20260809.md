# IT12 実装レビュー

## レビュー対象

- **イテレーション**: IT12（US35 引取確認コードの採番と照合 / US36 引取記録の訂正・取り消し）
- **コミット範囲**: `b51ae0b3d..8dec89226`（26 コミット）
- **規模**: 153 ファイル・約 7,000 行の追加
- **レビュー日**: 2026-08-09

## レビューの実施方法

**自己レビュー（逐次シミュレーション）で実施し、クローズ後に本来の並列レビューが届いた。**

5 つの XP 視点（programmer / tester / architect / technical-writer / user-representative）で並列レビューを起動したが、**25 分待っても 1 件も応答が返らなかった**。クローズを止め続けるより、`developing-review` が定めるフォールバック（「Agent ツールが利用できない場合、5 つの視点を逐次的にシミュレートする」）に切り替えた。

**この判断自体が本 IT の記録である。** 並列レビューが機能しなかった以上、見落としの可能性は並列実施時より高い。

**そして実際に見落としていた。** クローズ後に届いた本来のレビュー（技術文書 / 利用者視点）が **11 件を指摘し、全件が事実だった**（下記）。自己レビューの検出力は、並列レビューの代替にならない。

## 総合評価

US35 / US36 の受入基準は満たしており、返済枠 8 タスクをすべて返した。品質ゲート（テスト 1,160 件・E2E 14 本・CI・SonarQube）はすべて緑である。

一方で、**クローズ前の自己レビューが高優先度 3 件を見つけた**。とくに H1 は「マニュアルが実装より多くを主張する」形であり、IT11 の Problem P1（宣言と実装の食い違い）とまったく同じ型が、コメントではなくマニュアルで再発していた。**全緑の状態から欠陥が出るのは 4 IT 連続である。**

## 改善提案（重要度順）

### 高（クローズ前に対応 — **3 件すべて IT 内で対応済み**）

| # | 指摘 | 箇所 | 視点 | 対応 |
| :--- | :--- | :--- | :--- | :--- |
| **H1** | **訂正（CORRECT）を申請・承認できるが、記録の中身が何も変わらない。** ドメインには `correcting()` があったが、画面が直す内容を集めず、承認も適用していなかった。**マニュアルは「訂正は記録の中身を直します」と書いていた** | `CorrectionController` / `CorrectionCommandService` / `correction-form.html` / `docs/manual/08` | programmer / user / writer | **対応済み**（`cba1df35f`）。入力欄・適用・3 テスト（部分更新を含む）・未来日の検査・マニュアル・キャプチャ |
| **H2** | **記録を直す前に「承認済み」を書いていた。** 書き込みが 0 件でも承認済みになり、直っていないのに直ったと表示される。**取り消しの副作用が書けなくてもイベントを発行していた**（ADR-009 が運ぶのは「起きた事実」） | `CorrectionCommandService.approve` | architect / programmer | **対応済み**（`6d3249b57`）。適用 → 決定 → 発行の順に変更。楽観的ロックの敗北は例外で巻き戻す |
| **H3** | **引取確認コードが経路設計者・追跡管理者にも見えていた。** 予約詳細は両者に開いているが、どちらも引き渡しに関わらない。**コードを知る人が増えるほど、引き渡しの証明としての価値が下がる** | `booking/detail.html` | user / architect | **対応済み**（`cdead67f4`）。営業担当者・荷主だけに絞り、**見えないことと見えることを対で固定** |

### 中（IT12 内で対応）

| # | 指摘 | 箇所 | 視点 | 対応 |
| :--- | :--- | :--- | :--- | :--- |
| M1 | 節番号の繰り下げで、他章・付録からのリンク 7 件が古い番号を指したまま | `docs/manual/07` / `08` / `付録B` | writer | **対応済み**（`b7968cad7`）。`mkdocs build` のアンカー警告が IT12 起因 0 件に |
| M2 | 中黒を含む見出しへのアンカーを**自分の推測で**書いていた（MkDocs は中黒を落とす） | 同上 | writer | **対応済み**。**生成する道具に聞く**という形で解決 |
| M3 | 受入基準とテストの対応表が、US36 の「訂正」の道を名指ししていなかった | `ClaimCorrectionScenarioTest` | tester | **対応済み**（`8dec89226`）。訂正の受入テストを名指し |
| M4 | E2E が訂正の道を通っていなかった（取り消しのみ） | `e2e/app/claim-code-and-correction.spec.js` | tester | **対応済み**。「承認前は直っていない」「貨物状態は動かない」を対で確認 |
| M5 | 荷主の認証情報を作る補助が 2 つのテストクラスに重複 | `ShipperSelfServiceTest` / `ClaimCodeScenarioTest` | programmer | **対応済み**。`support/ShipperScopedTestUser` に 1 つだけ置いた |
| M6 | SpotBugs をローカルで `spotbugsMain` しか見ておらず CI だけが赤になった | ビルド運用 | programmer | **対応済み**（`889818f49`）。以降は `check` で確認 |

### 低（次イテレーション以降・方針を明記）

| # | 指摘 | 箇所 | 方針 |
| :--- | :--- | :--- | :--- |
| L1 | `ClaimCode.matches`（Booking）と `ClaimCodeMatch.matches`（Handling）に照合の規則が 2 か所ある | `booking/domain/model/ClaimCode` / `handling/domain/model/ClaimCodeMatch` | **許容**。BC をまたいで運べるのは素の文字列だけであり（ADR-005）、値オブジェクトそのものは渡せない。両方の Javadoc で相互に参照している。**規則が変わったときに片方だけ直る危険は残る** — Release 2.0 の BC 切り出し時に再検討 |
| L2 | `MapperTableOwnershipTest.ALLOWED` に 8 件の越境が残る | 同テスト | **理由つきで登録済み**（ADR-015）。「写しにできるもの」2 件（`BookingQueryMapper` → `voyage` / `carrier_movement`）を**次に返す候補**として名指し |
| L3 | 訂正で直せるのは作業日時とメモだけ。場所・航海番号は直せない | `CorrectionRequest.Details` | **意図的**。場所を直すと誤配判定の前提が変わり、航海番号を直すと経路との整合が崩れる。**取り消して登録し直す**のが業務として正しい。マニュアルには書いていない → 次 IT で追記 |
| L4 | 承認待ちが滞る運用リスク（追跡管理者が不在の週末） | 運用 | ダッシュボードのカードで気づける形にはした。**期限や通知は Release 2.0 の運用要件**として扱う |
| L5 | 小規模事業所で追跡管理者が 1 人の場合、本人承認の禁止により取り消しができない | `CorrectionRequest.approve` | **許容**。承認の意味は別人であって初めて生まれる。運用は「別の追跡管理者アカウントを用意する」。ADR 化はしない（US36 の受入基準そのもの） |

## 遅れて届いた IT7 レビューの突合（2026-08-09 追記）

クローズ後に、**IT7 を対象としたレビュー（利用者視点）が届いた**。並列レビューを起動したとき、**同名のエージェントが既に存在したため新しいものが `-2` 付きになり、催促が旧エージェント（IT7 担当）へ届いていた**。レビュー対象の取り違えである。

**内容は当時すべて正しく、10 件中 9 件は後続イテレーションで返済済みだった。**

| # | 指摘（IT7 時点） | 現状 |
| :--- | :--- | :--- |
| 高 1 | メモ欄が無いのに「メモに理由を記入」と案内 | 解消（メモ欄あり） |
| 高 2 | 確認コードが発行も照合もされない | **US35（本 IT）で解消** |
| 高 3 | 引取確認の記録が画面に出ない | 解消（荷役一覧の「受取人 / メモ」列） |
| 高 4 | 荷受人不一致の警告が未実装 | 解消（`matchesConsignee` を集約が呼ぶ） |
| 高 5 | 読み手が実行できない案内 | 解消 |
| 中 6 | UN/LOCODE のみ（港湾名が無い） | 解消（`JPOSA（大阪）`） |
| 中 7 | 遅れているかが読めない | 解消（IT11 / C18。新しい到着予定日が追跡照会に反映） |
| 中 8 | 荷主に自社の貨物一覧が無い | 解消（US34 / IT9） |
| 中 9 | 個人荷主の契約が黙って捨てられる | 解消（集約が拒否・HTTP レベルのテストあり） |
| **低 10** | **公開追跡の問い合わせがメールのみ** | **未対応**（下記） |

### 未対応の 1 件

**公開追跡（`public/tracking.html:52`）の問い合わせ先がメールのみである。** 荷役当日の夕方に来る電話に対応できない、という指摘。

**本 IT では対応しない。** マニュアル（`00-はじめに.md`）が「連絡先は運用開始時にここへ記載します」と定めており、**電話番号を実装側で先に決めると、運用が決めるべきことをコードが先取りする**。運用要件（Release 2.0）として扱う。

### この出来事から得たもの

指摘のうち 2 件が、**そのまま後続の設計になっていた**。

- 「案内だけ出して受け口が無いのは、これまで繰り返してきた形です」→ **US36（訂正・取り消し）の受け口**
- 「公開追跡に荷主名・品名・重量を足す提案が出たら止めてください」→ `TrackingInquiryView` が個人情報を**型として持たない**設計と、応答本文を検査するテスト。**US35 の引取確認コードも公開追跡には渡していない**

## クローズ後に届いた本来のレビュー（2026-08-09 追記）

**技術文書視点と利用者視点から 11 件の指摘があり、すべて事実だった。** 自己レビューでは見つけられなかったものである。全件を修正した（`68012ec66`）。

### 実装の欠陥（現場が踏む）

| # | 指摘 | なぜ自己レビューで見つけられなかったか |
| :--- | :--- | :--- |
| A1 | **荷役登録の画面が「システムでの照合は行いません」のまま**（US35 で照合するようになったのに） | **マニュアルだけを直して画面を確かめなかった。** H1 で「マニュアルが実装より多くを主張する」を見つけながら、**逆方向（画面が実装より古い）を探さなかった** |
| A2 | **承認・却下のボタンが荷役作業員にも出ていた**（押すと 403） | 認可（SecurityConfig）は検査したが、**画面の出し分けを検査していなかった**。「共有した画面のリンクもロールで出し分ける」の再発 |
| A3 | **決定した申請が一覧から消え、却下の理由が申請者に届かない** | ナビのコメントとマニュアルに「自分の申請の行方を読める」と書きながら、**その道を実行していなかった**（T6 の対象がストーリーの受入基準に限られていた） |
| A4 | **承認メッセージが種別によらず「貨物の状態を戻しています」** | 訂正の承認を**画面から踏んでいなかった**（テストは DB を見ていた） |
| A5 | 却下の理由欄が承認ボタンと同じセルにあり、誤って承認を押しかねない | 操作の**危険**を見ていなかった。承認は取り消せない |

### ドキュメント

| # | 指摘 |
| :--- | :--- |
| B1 | **8 章に「登録後の訂正・取り消しはできません」が残存**（新設した 8.4 の 4 行上） |
| B2 | リンク切れ 3 件。**8.4→8.5 のパターンだけ漏れた** — 8.4 という番号自体が別の節名で存在し続けたため、機械的な突合で見えなかった |
| B3 | 09 章の項目番号が重複（⑥ が 2 つ） |
| B4 | 04 章の 3 列テーブルに 2 セルの行を追加（レンダリングが崩れる） |
| B5 | `ui_design.md` のナビ表の途中に注記を挟み、テーブルが分断 |
| B6 | 「今後の提供」が実装済み機能について 3 件残存 |
| B7 | 「8 桁」が不正確（実装は英字を含む）。**電話で聞き取る現場が迷う** |
| B8 | **09-tracking-detail のキャプチャに通関の行が写っていない**のに、⑥ として説明していた |

### この出来事から得たもの

- **自己レビューは「自分が考えた観点」しか見ない。** A1 は「マニュアルを直したから画面も合っているはず」という思い込みで、H1 と同じ型の欠陥を**逆方向で**残していた
- **番号の繰り下げは、機械的な突合をすり抜けることがある。** 8.4 は新しい節名で存在し続けたため、「アンカーが実在するか」の検査を通ってしまった。**節名まで突き合わせる必要がある**
- **キャプチャは「この IT で触ったテンプレート」と突き合わせる。** 09 章の本文を直しながら、対応する画像を撮り直していなかった

## 品質指標

| 指標 | 実績 | 目標 | 判定 |
| :--- | :--- | :--- | :--- |
| テスト | 1,160 件（146 クラス）緑 | 全緑 | OK |
| E2E | 14 本緑（本 IT で 2 本追加・3 本修正） | 全緑 | OK |
| ArchUnit | 11 ルール緑 + **SQL 所有検査**（ADR-015） | 全緑 | OK |
| 行カバレッジ | 92.5%（分岐 73.3%） | — | — |
| SonarQube | **PASS**（Bug 0 / Vulnerability 0 / Code Smell 0 / 重複 0.3% / 新規カバレッジ 87.4%） | PASS | OK |
| CI | success | 緑 | OK |
| Checkstyle / SpotBugs | 0 件 | 0 | OK |

## 特筆すべき点

### 良い点

- **返済枠 8 タスクを IT 序盤の 8 コミットで完済した**（T8。6 IT 連続）。US35 の着手は 9 コミット目である
- **安全装置 21 件を数え上げて全件破壊検証し、空振り 0 件**。IT11（22 件中 3 件空振り）と異なり、**作った直後に壊す**を各コミットの中で行った
- **C28 の検査が、レビューの指摘（2 本）より多い 9 本の越境を見つけた**。「指摘の件数は見つかった数であって、ある数ではない」
- **Checkstyle が 3 回、設計の合図を出した**（`CorrectionRequest` の 13 引数 → 値オブジェクト分割、`SecurityConfig` の 161 行 → BC ごとのメソッド抽出）
- **`critical-path` の E2E が C29 で落ちていたことを発見した**。ユニット・統合が緑でも、画面を通した流れは別に確かめる必要がある

### 懸念

- **自己レビューが高 3 件を見つけた。** 実装中の破壊検証（21 件全件赤）をすり抜けている。**破壊検証は「作った守り」しか見ない** — 作らなかった守り（訂正の適用そのもの）は数え上げの対象にならない
- **並列レビューが機能しなかった。** 本 IT の検出は 1 人の視点に依存している

## 関連ドキュメント

- [IT12 イテレーション計画](../development/iteration_plan-12.md)
- [ADR-015 BC 間の結合は SQL の層でも検査する](../adr/015-mapper-sql-table-ownership.md)
- [IT11 実装レビュー](IT11実装_review_20260809.md)
