## コードレビュー結果

### レビュー対象
- IT5 実装（コミット df4bdbd5..58a78013）
- TI02（追跡番号一意性・例外分離・CQRS）、US18（追跡照会）、US06（経路設計者引渡し）、US12（荷主通知stub）

### 総合評価

CQRS の分離、ポート・アダプタパターン、ドメインイベントの導入はアーキテクチャとして適切な方向性です。SonarQube Quality Gate も PASS しており、テスト件数も増加（ユニット +9件、E2E +10件）しています。一方で、バックエンドサービス層のユニットテスト欠如（TrackingQueryService）、イベント履歴テーブルの表示バグ（全行に現在状態が表示）、「経路通知送信済み」バッジの誤表示、ナビゲーション定義の重複という改善点が複数のエージェントから共通して指摘されました。

### 改善提案（重要度順）

#### 高（対応推奨）

| # | 提案 | 箇所 | 指摘元 | 理由 |
|---|------|------|--------|------|
| 1 | `TrackingQueryService` のユニットテスト追加（正常・NotFound・不正フォーマット境界値） | `TrackingQueryServiceTest.java`（新規） | xp-tester / xp-programmer | サービス層の分岐がE2Eのみカバーされており、テストピラミッド上の欠落 |
| 2 | `confirmBooking` 時の `CargoAssignedForRoutingEvent` 発行テスト追加 | `CargoCommandServiceTest.java` | xp-tester / xp-programmer | 新しいイベント発行が一切テストされていない |
| 3 | `RecordingCargoEventPublisher` に `publishCargoAssignedForRouting` の記録を追加 | `CargoCommandServiceTest.java:139` | xp-tester | 振る舞いの検証ができていない |
| 4 | イベント履歴「状態」列の修正（全行が `activity.transportStatus` を表示） | `TrackingPage.tsx:106` | xp-programmer / xp-user-representative | バグの可能性。全行同じ値になる |
| 5 | 「経路通知送信済み」バッジの表示条件見直し（CONFIRMED以降に限定、またはstub未実装なら非表示） | `BookingDetailPage.tsx:74-78` | xp-user-representative | stub段階で「送信済み」と表示するのは誤解を招く |

#### 中（対応推奨）

| # | 提案 | 箇所 | 指摘元 | 理由 |
|---|------|------|--------|------|
| 6 | `DashboardPage.MENU_ITEMS` と `AppLayout.NAV_ITEMS` のロール定義を共通化 | `DashboardPage.tsx`, `AppLayout.tsx` | xp-architect / xp-programmer | 同一ロールフィルタが2箇所で重複。変更時に不整合リスク |
| 7 | `@ControllerAdvice` による例外ハンドラ共通化（GETとPUTのcatchブロック重複） | `TrackingStatusController.java` | xp-architect / xp-programmer | DRY原則違反。将来のControllerでも同じ重複が生まれる |
| 8 | `ShipperNotificationPort.notifyRouteAssigned` の引数をCargo集約からDTOに変更 | `ShipperNotificationPort.java:15` | xp-programmer / xp-architect | infrastructure層がdomain集約の内部に依存。インターフェース分離の原則 |
| 9 | `assignRoute` の通知失敗がトランザクションをロールバックするリスクの検討 | `CargoCommandService.java:88` | xp-architect / xp-programmer | stub→本番化時に通知障害が経路割当を阻害する |
| 10 | `RouteAssignedNotificationService` の `[STUB]` ログをwarnレベルへ変更または本番対策明示 | `RouteAssignedNotificationService.java:19` | xp-technical-writer | 本番ログに混在すると通知実施有無の判断不能 |
| 11 | 400エラーと404エラーで異なるユーザー向けメッセージを表示 | `TrackingPage.tsx:42-47` | xp-technical-writer / xp-user-representative | フォーマット不正と未発見を同一メッセージにするのはUX問題 |
| 12 | フロントエンド入力時のフォーマットバリデーション追加 | `TrackingPage.tsx` | xp-user-representative | 手入力ミスを即座にフィードバックできない |
| 13 | 「最終更新: HH:MM:SS」表示追加（30秒自動更新の通知） | `TrackingPage.tsx` | xp-user-representative | 画面がいつ更新されたか不明 |
| 14 | `ErrorResponse` にエラーコードフィールド追加 | `ErrorResponse.java`（trackingms） | xp-technical-writer | フロントエンドがHTTPステータスのみでエラー種別を判断せざるを得ない |

#### 低（改善の余地あり）

| # | 提案 | 箇所 | 指摘元 | 理由 |
|---|------|------|--------|------|
| 15 | `CargoAssignedForRoutingEvent` の命名を `CargoConfirmedForRoutingEvent` に変更検討 | `CargoAssignedForRoutingEvent.java` | xp-architect | 「経路設計待ち」の意図に命名が合っていない |
| 16 | イベント履歴テーブルのkey属性をインデックスから変更 | `TrackingPage.tsx:99` | xp-programmer | 並び替え時のレンダリング問題リスク |
| 17 | E2Eテストの重複削除（navigation.spec.tsとtracking.spec.tsのナビ遷移テスト重複） | `tracking.spec.ts:11-16` | xp-tester | E2Eメンテナンスコスト削減 |
| 18 | バリデーションエラーメッセージの日本語化 | `TrackingNumber.java:16` | xp-technical-writer | 英語メッセージがユーザーに露出する可能性 |

### 矛盾事項

矛盾する指摘はありませんでした。

### エージェント別フィードバック詳細

<details>
<summary>xp-programmer（高: 2 / 中: 3 / 低: 2）</summary>

**評価サマリー**: 全体として設計の方向性は良好。CQRS、ヘキサゴナルアーキテクチャ、例外の型による分岐が一貫している。

**良い点**:
- CQRS 分離が適切（`readOnly = true`）
- Port/Adapter パターンが依存性逆転の原則に忠実
- 例外の型による HTTP ステータス出し分けが正しい
- フロントエンドのテスト網羅性（正常系・エラー系・例外バッジ）

**改善提案**（主要）:
- 【高】`TrackingQueryService` の単体テストが存在しない
- 【高】`confirmBooking` 時のイベント発行テストが存在しない
- 【中】`assignRoute` の通知失敗がトランザクションをロールバックするリスク
- 【中】`ShipperNotificationPort` の引数が Cargo 集約全体

</details>

<details>
<summary>xp-tester（高: 3 / 中: 2 / 低: 2）</summary>

**評価サマリー**: テストピラミッドは概ね良好だが、バックエンドのサービス層ユニットテストに明確な欠落がある。

**良い点**:
- テスト名が仕様として機能している
- `main`/`nav` ロケータでの絞り込みによるテストの安定性
- `RecordingCargoEventPublisher` / `NoOpEventPublisher` の目的別使い分け

**改善提案**（主要）:
- 【高】`TrackingQueryService` ユニットテスト欠如
- 【高】`RecordingCargoEventPublisher.publishCargoAssignedForRouting` が no-op
- 【中】E2E で追跡成功時の結果表示テストがない
- 【低】navigation.spec.ts と tracking.spec.ts のナビ遷移テスト重複

</details>

<details>
<summary>xp-architect（高: 1 / 中: 2 / 低: 1）</summary>

**評価サマリー**: アーキテクチャの方向性は健全。CQRS、DIP、ドメインイベントが一貫している。

**良い点**:
- CQRS 分離が明確（QueryService / UpdateService）
- 依存関係の方向が正しい（ドメイン層ポート → インフラ層アダプタ）
- `NoOpCargoEventPublisher` による環境差異への堅牢性

**改善提案**（主要）:
- 【高】イベント発行と通知のトランザクション境界問題（Outbox パターン検討）
- 【中】`DashboardPage.MENU_ITEMS` と `AppLayout.NAV_ITEMS` のロール定義重複
- 【中】`@ControllerAdvice` による例外ハンドラ共通化

</details>

<details>
<summary>xp-technical-writer（高: 0 / 中: 3 / 低: 3）</summary>

**評価サマリー**: Javadoc や日本語ラベルの整備は良好。stub ログと ErrorResponse スキーマに改善余地あり。

**良い点**:
- `TrackingQueryService` の `@throws` 付き Javadoc
- `STATUS_LABELS` / `EVENT_TYPE_LABELS` の集約管理

**改善提案**（主要）:
- 【中】`[STUB]` ログが本番で通知実施有無を判断不能にする
- 【中】`ErrorResponse` にエラーコード追加（機械可読識別子）
- 【中】400/404 で異なるユーザー向けメッセージ

</details>

<details>
<summary>xp-user-representative（高: 2 / 中: 3 / 低: 1）</summary>

**評価サマリー**: 業務フローに沿った実装だが、イベント履歴の表示バグと「経路通知送信済み」バッジの誤表示が現場での混乱を招く。

**良い点**:
- placeholder による入力補助
- EXCEPTION バッジの赤色強調
- ロール別ダッシュボードカード

**改善提案**（主要）:
- 【高】イベント履歴「状態」列が全行同じ値
- 【高】「経路通知送信済み」バッジが stub段階で誤表示
- 【中】30秒自動更新の「最終更新」タイムスタンプ表示
- 【中】フロントエンド入力バリデーション

</details>
