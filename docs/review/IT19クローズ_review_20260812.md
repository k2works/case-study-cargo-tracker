# IT19 クローズ前レビュー

**実施日**: 2026-08-12
**対象**: IT19 の実装全体（`java/take-6/v2.1.0..6d472ba4e`。12 コミット・479 ファイル）
**方式**: XP 5 視点の並列レビュー（programmer / tester / architect / technical-writer / user-representative）

## 総合評価

**指摘は 5 視点あわせて高 11 件・中 20 件・低 8 件。** うち**高 11 件すべてと中の主要分をクローズ前に対応**した。

**このイテレーションの成果物は「検査を足すこと」だったが、その検査自身に穴があった。** H2 スモークの載せ忘れ検査は型単位でしか見ておらず、実測で 6 メソッドを素通りさせていた。ADR-022 の検査は数値リテラルしか拾わず、**移設先の `DeadlineUrgency` がまさに定数比較の形**だった（infrastructure に残していたら緑のままだった）。

**「作った本人には見えない」ものが、今回も利用者視点から出た。** マニュアル 8.5.1 の手順は、荷役作業員が実行すると 2 回続けて 403 に当たる。書いた側は全ロールの権限を持って読んでいた。

---

## 高優先度の指摘と対応（11 件）

| # | 視点 | 指摘 | 対応 |
| :--- | :--- | :--- | :--- |
| U1 | 利用者 | **8.5.1 の手順 1・2 は荷役作業員には実行できない**（`/tracking` `/voyages` は別ロール専用で 403） | **対応**。公開追跡（ログイン不要）で確かめる形に書き直し、到着予定は経路担当者への依頼として書いた |
| U2 | 利用者 | **手順 3 は追跡管理者にできないことを約束している**（7.10「同じ申請を二度決めることはできない」） | **対応**。8.8.2 として「システムでは変更できない」と明記し、実際の窓口を書いた |
| W1 | writer | マニュアル 01 の図が 8.4（訂正の申請）を指す。通関申告は 8.6 | **対応** |
| W2 | writer | 8.5.1 は 8.8 配下にある（番号と所在が別） | **対応**（8.8.1 / 8.8.2 に改番） |
| W4 | writer | `booking/aggregates` の package-info が「集約ルートは Cargo」と単数。実際は 3 つで、ADR 自身が「4」と書いていた | **対応**（実測に合わせ、`aggregates` 以外からは記述を外した） |
| W5 | writer | **ADR-024 の「名前で記録した」が、どこにも記録されていない** | **対応**（`iteration_plan-19.md` に **D5** として実測付きで起票） |
| A1 | architect | `architecture_backend.md` が「フラットに一本化した」と書いており、ADR-024 と正面から矛盾 | **対応**（正典を更新し、ADR に「一本化を取り消す判断でもある」と明記） |
| A2 | architect | 次 IT へ送ったルール名（`entities` 対象）では **7 メソッド中 4 が検査の外** | **対応**（分類を是正し、`entities` に残るのは対象の 2 クラスだけにした。D5 の記述も実測に合わせた） |
| A3/P3 | architect / programmer | **demo の 8 Bean が本番でも生成される**（条件付きは Runner だけ） | **対応**（すべて条件付きに。テストでは Runner を `@Profile("!test")` で外す） |
| P1 | programmer | **部分的に失敗した投入が「投入済み」として永久に固定される** | **対応**（途中で止めて ERROR を出す） |
| P2/T3 | programmer / tester | H2 スモークの検査が**メソッド単位の載せ忘れを素通り**（実測 6 件） | **対応**（メソッド単位＋語境界。未収載 6 件をスモークに追加） |

## 中優先度の主な対応

| # | 視点 | 指摘 | 対応 |
| :--- | :--- | :--- | :--- |
| T1 | tester | 往復テストの `contains("120")` 等は HTML 全体でほぼ常に真 | **対応**（描画された形と個数欄の抽出に） |
| T2 | tester | 再見積リンクのクエリ文字列を誰も見ていない（href を書き換えても緑） | **対応** |
| T4 | tester | 貨物種別はセレクトのため `value=` では見えず、引き継ぎが壊れても緑 | **対応** |
| D1 | tester | `EstimateListFilterTest` の `DELETE` 全消しが他クラスの行まで消す | **対応**（自分が作った行だけに） |
| M1 | programmer | ADR-022 の検査が**定数比較を素通り**（移設先がその形） | **対応**（大文字定数との比較も拾う） |
| M3 | tester | `EstimateFilter` に単体テストが無い（統合テスト 6 本だけ） | **対応**（単体 6 本。境界・大小文字・未知の値） |
| — | tester | **未知の状態を渡すと「有効で絞る」に化ける** | **対応**（知らない値では絞らない） |
| A-M1 | architect | 実費は 7 メソッドでなく 4（`record` は元から公開） | **対応**（ADR の記述を実測に訂正） |
| A-M3/M4 | architect | `entities` の 8 件中 6 件が record。識別子の同居でルート数が読めない | **対応**（識別子と record を `valueobjects` へ） |
| A-M5 | architect | ADR-022 の守り手表に新しい検査が載っていない | **対応** |
| U3 | 利用者 | 状態が「有効」（絞り込み）と「作成済」（一覧）で割れている | **対応**（`EstimateStatus` の表示名を「有効」に統一） |
| W6 | writer | package-info 27 件で Markdown の `**` が Javadoc に混入 | **対応**（`<strong>` へ。全 41 ファイル） |

## 次イテレーションへ送るもの

| # | 指摘 | 理由 |
| :--- | :--- | :--- |
| **D5** | 集約ルート以外から `entities` の生成・変更を呼ばない ArchUnit ルール | ADR-024 の代償。**実測の違反は 0 件**であり、0 件のうちに固定する |
| U4 | 見積一覧の既定を「有効」にする（いまは運用ルールで補っている） | 既定を変えると「初めて開いた人」の 0 件文言と衝突する。設計の判断が要る |
| U5 | 絞り込みの 0 件で理由を分ける（港コードの誤り・作成日の逆転） | 実装は小さいが、文言の設計が要る |
| U6 | 一覧に件数と並び順を出す | 月末の集計で Excel に写す人が出る |
| U7 | 「有効期限」の語が実際は「希望到着期限」を指している | 同業のシステムでは「価格の保証期限」を意味する。**語を直すか、別の概念を足すかの判断が要る** |
| U8 | 期限切れの一覧から一件ずつ開かないと作り直せない | 一覧の行に導線を置く |
| P-M2 | `EstimateFilter` が全件取得してメモリで絞る | 日付と地名は SQL に落とせる。件数が増えるまで痛まない |
| A-L3/P-M3 | `Cargo` が 499 行（上限 500） | D1 を返した結果。**猶予を 1 行買った状態**であり、次の切り出し候補を記名する |

## 品質ゲート（対応後）

| 指標 | 値 |
| :--- | :--- |
| `TZ=UTC ./gradlew check --rerun-tasks` | **緑** |
| E2E | **21 件 緑**（不安定だった 1 件も原因を特定して解消） |
| マニュアルのキャプチャ | **63 件 緑** |
| SonarQube Quality Gate | **PASS**（new_violations 0 / new_coverage 87.7% / 重複 0.34%） |

## 特筆

**E2E の 1 件が長く不安定だった原因を、レビューをきっかけに特定した。** ログイン画面は動作確認用の利用者一覧が長く、**ログアウトの案内が出た状態ではボタンの位置が動き続ける**。Playwright の「安定するまで待つ」が 30 秒を使い切っていた。変更前のコードでも同じように落ちることを確認済みで、モデル分割とは無関係だった。Enter で送る形にして解消した。
