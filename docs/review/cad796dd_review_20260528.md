# developing-review: cad796dd（テストギャップ補完）マルチパースペクティブレビュー

## レビュー対象

- コミット: `cad796dd` "test: developing-review 高指摘のテストギャップを補完"
- 範囲: 5 ファイル / +60 行（テスト 3 件・コメント 2 件）
- 実施日: 2026-05-28
- レビュー実施: Ralph Loop iter 1（developing-review 自動実行）

### 変更概要

| 区分 | ファイル | 内容 |
|------|----------|------|
| Test (H1) | `apps/backend/bookingms/.../RouteConfirmedEventHandlerTest.java` (+17) | 冪等スキップ対象外の例外が伝播するネガティブテスト |
| Test (H2) | `apps/backend/routingms/.../OptimalRouteServiceTest.java` (+18) | 接続境界（到着 == 出発）の特性化テスト |
| Test (H3) | `apps/frontend/src/features/booking/pages/BookingFormPage.test.tsx` (+18) | weightKg=null プリセットでも壊れない |
| Doc (M1) | `BookingFormPage.tsx` (+6) | QuotationPreset 双方向契約コメント（受信側） |
| Doc (M1) | `QuotationDetailPage.tsx` (+2) | 同（送信側） |

## 総合評価

**通過（重要度「高」の指摘ゼロ）**。前回 IT4 セッションレビュー（`docs/review/it4_session_review_20260526.md`）の H1/H2/H3/M1 指摘に対する即対応として、現行挙動を最小コストで固定する特性化テストを正しく追加できている。プロダクションロジックの変更を伴わずテストネットの張り直しに徹した点が好印象で、IT5（追跡・荷役）の cross-service 拡張前に「冪等スキップ条件を不用意に広げない契約」を先に固められた時間軸も適切。

ただし**中優先度の構造的負債が複数指摘された**：相互参照コメントによる契約管理は腐敗しやすく型による拘束への昇格が望ましい点（M1）、H2 の業務妥当性（最低接続時間 = MCT 未考慮）が IT8 / 新ストーリー候補である点、H1 の伝播先（DLQ・再試行ポリシー）と一体設計が IT5 着手前に必要な点。

## 改善提案（重要度順）

### 高（マージ前に対応すべき）

なし。本コミットは既にマージ済みであり、High 指摘もなし。

### 中（対応推奨・IT8 ないし新ストーリーへ）

| # | 提案 | 箇所 | 指摘元 | 理由 |
|---|------|------|--------|------|
| M1 | `QuotationPreset` 型を共有モジュール（例 `features/quote/types.ts` または中立 `contracts/`）へ抽出し、`QuotationDetailPage` の navigate payload を `satisfies QuotationPreset` で型拘束する。コメントによる相互参照は腐敗するため、構造的契約に昇格させる | `BookingFormPage.tsx:44-48` / `QuotationDetailPage.tsx:115-116` | architect, technical-writer | コメントが「両側を必ず同時更新せよ」を人手規律に依存させており、項目追加で片側だけ silently に拡張されても型検査・テストで検出できない |
| M2 | H1 ネガティブテストを `IllegalStateException` 等の `RuntimeException` 直系業務例外でもう 1 ケース追加し、catch 句が `catch (Exception)` に広がる退行を二重検知する | `RouteConfirmedEventHandlerTest.java:97` | tester, architect | 現状の `new RuntimeException(...)` 単発では「型による選別」の意図がテスト名でしか表現されていない |
| M3 | H2 境界対を完成させる：「到着 == 出発」（追加済）に対し「到着の 1 分前出発（接続不可）」をパラメタライズドテストで追加し、`isBefore` を `isAfter` に取り違える回帰を境界 ±1 で完全に縛る | `OptimalRouteServiceTest.java:103-118` | tester | 現状「到着 == 出発」と「数日前出発（既存）」のギャップが大きく、`!isBefore` 反転回帰の半分しか捕まえられない |
| M4 | **業務妥当性確認の新ストーリー**：H2 で固定した「同時刻接続を有効候補とする」挙動は、港湾・空港での荷役・通関の現実的所要時間（MCT: Minimum Connection Time）を無視している。US08 の受け入れ条件に MCT を追加する新ストーリーを起票し、現テストはその確定までは「現行挙動」を縛る暫定特性化テストとして扱う | `OptimalRouteService` 業務ルール | user-representative | テストで固定する前に業務妥当性が問われる典型例。経路設計者が選択しても実接続できない経路が候補に残るリスク |
| M5 | H1 で「伝播する」が固定された以上、伝播先の処理（DLQ・再試行ポリシー・運用可視化）を **IT5 着手前に ADR で方針化**する。trackingms 追加で同一 `cargo-events` トピックの consumer が増えるため、サイレントスキップ vs 顕在エラーのハンドリング統一が遅れると negative テストが緑のまま意味を失う。**→ ADR-0011 として起票済み（2026-05-28）**。ホワイトリスト方式の継続を明文化し、DLQ・本格的な再試行は IT8 へ明示的に延期。IT5 計画書（iteration_plan-5.md §ADR / §リスク / §設計トピック）からも参照 | Kafka tracking プロセッサ全体 | architect, user-representative | メモリ `reference_adr0010_orphan_event_skip.md` と `cross-service-tracking-processor-profile-config.md` の延長線 |

### 低（改善の余地あり）

| # | 提案 | 箇所 | 指摘元 | 理由 |
|---|------|------|--------|------|
| L1 | H2 テストに `@DisplayName("境界条件: 到着 == 出発")` を追加し、IDE のテストツリーでペア境界であることを一目で示す | `OptimalRouteServiceTest.java:104` | programmer |  |
| L2 | H3 テストの `value=null` コメント「number input の空表現」を JSDoc または `it.each` の補足へ移し、テスト本文の意図表現を残す | `BookingFormPage.test.tsx:117` | programmer, tester |  |
| L3 | テスト名 `H4:` プレフィックスはレビュー文書アーカイブ後に意味を失う恐れ。US 番号または恒久仕様 ID への置換を推奨 | `BookingFormPage.test.tsx:102` | technical-writer |  |
| L4 | `QuotationPreset.weightKg?: number \| null` の `null` と `undefined` の同値クラス（未確定 vs プリセット項目省略）を JSDoc 1 行で明示 | `BookingFormPage.tsx:55` | technical-writer |  |
| L5 | `RouteConfirmedEventHandler.java` 本体 Javadoc に H1 で固めた「ホワイトリスト方式」を 1 行追記（テストと実装の独立劣化リスク低減） | スコープ外 | architect | **iter 2 で対応済**（クラス Javadoc に「例外ハンドリング方針」段落を追加） |
| L6 | H3 の `cargoType` 連動必須項目（HAZARDOUS の危険物申告、REFRIGERATED の温度条件）が未確定でプリセットされた場合のテストを将来追加 | `BookingFormPage.test.tsx` | user-representative | 営業の業務シナリオ（見積時点で種別未確定）の安全網 |

## 矛盾事項

| # | 視点 A | 視点 B | 論点 | 推奨判断 |
|---|--------|--------|------|----------|
| C1 | **xp-programmer / tester**：H2 接続境界（到着 == 出発）テストは `!isBefore` の等値含意を正しく特性化し三角測量として理想 | **xp-user-representative**：H2 同時刻接続は MCT 未考慮で業務的に妥当か疑問。実務では数時間〜半日の連絡余裕が必要 | テストの技術的正しさ vs 業務的正しさ | **両立**：本コミットの特性化テストは「現行実装の挙動」を固定する目的では正しい（マージ妥当）。並行して M4（MCT を US08 受け入れ条件に追加する新ストーリー）を起票し、業務妥当化はそちらで対処。現テストは MCT 導入時に修正対象として明示する |

## エージェント別フィードバック詳細

<details>
<summary>xp-programmer（高: 0 / 中: 0 / 低: 3）</summary>

### 評価サマリー
H1/H2/H3/M1 への対応は的確で、テストファースト規律に沿った高品質な特性化テスト。指摘内容を最小コストで固定化し、回帰防止の安全網としても契約の可視化としても良いコミット。

### 良い点
- H1: catch 範囲の意図がテストコメントで明文化、TDD の「意図表現テスト」
- H2: `!isBefore` の等値含意を最も読みやすい形で固定
- H3: React number input の `null` 挙動を可視化
- M1: 双方向コメントは `navigation-state-type-safety` メモリの懸念への具体的軽減策
- テスト名がレビュー追跡 ID 付きで日本語意図記述・トレーサビリティ良好

### 改善提案（すべて Low）
- L1, L2 と同義（H2 表示名、H3 コメント整理、H1 例外型具体化）

### 懸念事項
- なし

### スコープ外の発見
- H1 サブクラスの除外検証は未保証（H4 IT8 時に補強）

</details>

<details>
<summary>xp-tester（高: 0 / 中: 2 / 低: 1）</summary>

### 評価サマリー
意図が明確な特性化テストとして適切。コメントで「なぜ固定するか」が明記されている点が特に良い。

### 良い点
- H1: Positive 2 件と Negative の三角測量で catch 範囲の境界を仕様化
- H2: 既存「到着前出発不可」とセットで境界値分析として理想
- H3: `presetToFormValues` の `?? ''` を契約として固定
- M1: 双方向相互参照で暗黙契約を可視化

### 改善提案
- M2（H1 ラップ検知、上記参照）
- M3（H2 ±1 境界完成、上記参照）
- L2

### 懸念事項
- Axon 例外ラップが変わると Negative が緑のまま意味を失う恐れ。バージョン更新時の再点検フラグを CHANGELOG に
- テストピラミッドはユニットに集中で良好（アイスクリームコーン化の兆候なし）

### スコープ外の発見
- `OptimalRouteService` が IT8 で Dijkstra/A* に差し替え予定 → 多段経由の特性化テスト追加観点
- `weightKg='0'` 境界テスト未整備

</details>

<details>
<summary>xp-architect（高: 0 / 中: 2 / 低: 1）</summary>

### 評価サマリー
特性化テストの典型コミットで変更容易性への直接投資。文書コメントによる契約明文化はテキスト二重管理になりやすく将来負債のシード。

### 良い点
- H1 が catch 範囲拡大の構造的検知網になっており、IT5 cross-service ハンドラの参照実装として価値が高い
- H2 は `!isBefore` の等値含意を実装/テスト双方に残せている
- IT5 で trackingms/handlingms が同一トピックに乗る計画に対し、H1 の方針固定は時間軸として正しい

### 改善提案
- M1（QuotationPreset 型抽出）
- M2（H1 例外型多様化）
- L1（H2 表示名）

### 懸念事項
- **ドメイン契約をテストで縛る方針の限界**：H1/H2 はドメイン側にルールが宣言的に存在しない。`VoyageConnection` のような値オブジェクト昇格を ADR 検討
- **`QuotationPreset` 型の所在**が依存方向と逆（booking が quote の出力契約を所有）。境界づけられたコンテキストとして再構成
- **H1 伝播先の未定義**：DLQ・再試行ポリシーと一体で ADR を IT5 前に

### スコープ外の発見
- `RouteConfirmedEventHandler.java` Javadoc にホワイトリスト方式の意図を 1 行追記
- メモリ `project_route_confirm_select_recalc_debt.md`：非トランザクショナル publish が H1 伝播経路で顕在化リスク

</details>

<details>
<summary>xp-technical-writer（高: 0 / 中: 1 / 低: 2）</summary>

### 評価サマリー
M1 は前回指摘への正しい初手。テスト名も振る舞いを自然な日本語で語っており合格点。ただし「契約」を本気で守るなら型による拘束への昇格を IT8 で検討すべき。

### 良い点
- 双方向コメントが対になり、片側からもう片側へ辿れる
- テスト名が仕様文として読める
- ADR-0010 と H1 テストの紐付けが追跡可能

### 改善提案
- M1（型抽出）
- L4（`null` vs `undefined` 同値クラス）
- L3（`H4:` プレフィックス恒久化）

### 懸念事項
- 相互参照コメントは人手規律依存。送信側の navigate payload を契約として検証するテストが不在

### スコープ外の発見
- なし

</details>

<details>
<summary>xp-user-representative（高: 0 / 中: 1 / 低: 1）</summary>

### 評価サマリー
特性化テスト中心の堅実な変更。直接の機能追加はないが将来回帰から現行動作を守る。業務側に残る運用課題（サイレントスキップの可視化、H4 のプリセット欠落項目）は別途継続が必要。

### 良い点
- H1：握り潰さない動作を固定し運用ログでの発見容易性が壊れにくくなる
- H3：見積時点で重量未確定の業務シナリオ（LCL、荷主からの確報未着）に合致
- M1：H4 のフォーム項目改廃で片側更新漏れリスクが下がる

### 改善提案
- M4（MCT 業務妥当性確認の新ストーリー）
- L6（cargoType 連動必須項目テスト）

### 懸念事項
- US11 冪等スキップが「黙ってスキップする」を仕様化した副作用：IT8 で経路設計者・営業向けの検知導線（待ちリスト残留表示等）必須

### スコープ外の発見
- QuotationDetailPage 詳細表示に「貨物種別」が出ない既知の軽微な不整合（H4 プリセット項目を確認できないまま予約化に進む）

</details>

## 対応方針

### 即対応（本セッション・iter 2 で実施）

- **L5 対応**：`RouteConfirmedEventHandler.java` のクラス Javadoc に「例外ハンドリング方針（ホワイトリスト）」段落を追記。冪等スキップ許容例外を 2 種に限定する設計意図と、ネガティブテスト（`RouteConfirmedEventHandlerTest#冪等スキップ対象外の例外は握り潰さず伝播する`）の双方を更新する規律を明文化。テストと実装が独立に劣化するリスクを最小化。

> High 指摘ゼロ・本コミット（cad796dd）はマージ済み。本セッションで追加で対応したのは L5 のみ（Javadoc のみで実害なし・テスト変更不要）。

### IT8 / 新ストーリー候補（バックログ化推奨）

- **M1**：`QuotationPreset` 型の共有モジュール抽出（フロントエンド設計改善）
- **M2 + M3**：H1/H2 のテスト深化（パラメタライズド・例外型多様化）
- **M4**：MCT 業務妥当性確認の新ストーリー（US08 受け入れ条件追加）
- **M5**：trackingms 追加前の Kafka エラーハンドリング方針 ADR **→ ADR-0011 起票済み（2026-05-28）**
- **L1〜L6**：軽微改善（IT8 のドキュメント・テスト整理セッションへ）

### スコープ外として記録

- `project_route_confirm_select_recalc_debt.md` の非トランザクショナル publish 負債：H1 伝播経路で顕在化リスクを認識
- `OptimalRouteService` IT8 Dijkstra/A* 差し替え時の多段経由テスト追加観点
- QuotationDetailPage 貨物種別非表示の既知不整合

## レビュー完了条件チェック

- [x] 全エージェントのフィードバックが収集済み（5/5）
- [x] 統合レポートが作成済み（本ドキュメント）
- [x] 重要度「高」の指摘について対応方針 → **該当なし**
- [x] レビュー結果がドキュメントとして保存済み

## Ralph Loop コンテキスト

- iteration: 1 → 2 / 3
- max-iteration: 3
- completion-promise: `simple made easy.`
- iter 1 結果：High 指摘ゼロ・コード修正不要。中優先度はバックログ化方針で確定。
- iter 2 結果：L5（`RouteConfirmedEventHandler` Javadoc 追記）を即対応。中優先度はバックログ化方針を維持。
- iter 3 結果：完了確認。残課題なし（未コミット変更はユーザー判断待ち）。

## iter 3 完了確認

### 本セッションの成果物（未コミット）

| ファイル | 種別 | 内容 |
|----------|------|------|
| `apps/backend/bookingms/.../RouteConfirmedEventHandler.java` | 変更 (+7) | クラス Javadoc にホワイトリスト方式の例外ハンドリング方針を追記（L5 対応） |
| `docs/review/cad796dd_review_20260528.md` | 新規 | 本マルチパースペクティブレビュー統合レポート |

### user 向け next step

1. **コミット**（任意）: `git-commit` スキル経由で上記 2 件をまとめてコミット
   - 推奨メッセージ: `docs(review): cad796dd マルチパースペクティブレビューと L5 Javadoc 追記`
2. **GitHub Issue 起票**（推奨・IT8 / 新ストーリー向け）:
   - M1: QuotationPreset 型抽出（フロントエンド設計改善）
   - M2 + M3: H1/H2 テスト深化（パラメタライズド・例外型多様化）
   - M4: MCT を US08 受け入れ条件に追加する新ストーリー
   - M5: trackingms 追加前の Kafka エラーハンドリング方針 ADR
3. **IT5 着手前**: 特に M5（Kafka エラーハンドリング ADR）は IT5 で新サービス追加前に着手することを推奨
