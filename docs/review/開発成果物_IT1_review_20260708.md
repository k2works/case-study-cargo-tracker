# 開発成果物レビュー（IT1）

- 実施日: 2026-07-08
- レビュー対象: IT1 実装（コミット範囲 `b1e75441..HEAD`・87 ファイル / +3598 行）
  - 技術基盤（AggregateRoot / UnitOfWork / DbUp 二方言 / DapperConfiguration）
  - US26 認証 + ウォーキングスケルトン
  - US02/US03 荷主登録（Shipper）
  - US01 輸送見積（Estimation）
  - ユニット / 統合 / Web / E2E テスト
- レビュー方式: XP 5 エージェント並列（developing-review）

## 総合評価

DDD + ヘキサゴナル + 軽量 CQRS の意図がコード全体に一貫して表現され、値オブジェクトによる不変条件の前倒し、post-commit イベントの構造的安全性、二方言差異の局所化（TypeHandler）など**規律の高い良質な実装**である。テストも Given/When/Then + 日本語名で意図が明確。一方、(1) **アーキテクチャの規律をテストで守る仕組み（ArchUnitNET）が実質未実装**、(2) **設計ドキュメント（domain-model.md）と実装のドリフト**、(3) **US01 の一部受入条件（危険物申告・期限なしルート通知）の未実装**、が主要な課題として複数エージェントから共通指摘された。

## 改善提案（重要度順）

### 高（マージ/IT 完了前に対応すべき）

| # | 提案 | 箇所 | 指摘元 | 理由 |
|---|------|------|--------|------|
| H1 | ArchUnitNET の 4 ルールを実装（Domain→Infra 非依存 / Domain にフレームワーク属性なし / App→Infra はポート経由 / BC 間直接参照禁止） | `tests/CargoTracker.Architecture.Tests/UnitTest1.cs`（空） | architect, tester | ADR-0005・tech_stack.md がテスト化を宣言済みだが未実装。参照実装が正しい今、緑を基準線に固定しないと以降 依存方向が静かに壊れても気づけない |
| H2 | domain-model.md ↔ 実装 ↔ data-model.md の **住所（Address）不整合**を解消 | `domain-model.md`(56/428/476/515), `iteration_plan-1.md`(67/71/243/266/368/392) | technical-writer, user | 実装・data-model に住所カラム/VO が無いのに domain-model と US02 受入・UI モックに住所が残存。ユビキタス言語の SoT が信頼できない状態 |
| H3 | US01 危険物申告フォーム（受入条件 6）の対応 | Estimate 作成画面 | user, tester | 危険物は申告漏れが法規制違反・出港停止に直結。全体が難しくても「危険物選択時に申告必要」の警告表示は最低限入れる |
| H4 | 荷主登録の法人フィールドを htmx 動的切替に（現状常時表示） | `Views/Shipper/New.cshtml` | user | ui_design/iteration_plan は htmx 切替を規定。個人荷主が大半の窓口で毎回迷いの元 |
| H5 | US01 受入 5（期限に間に合うルートなし通知）・受入 2（航海スケジュール由来）の仕様整合確認 | Estimation | tester, user | スタブ実装のため受入条件と乖離の疑い。IT3 実データ投入前に計画へ載せる |

### 中（対応推奨）

| # | 提案 | 箇所 | 指摘元 | 理由 |
|---|------|------|--------|------|
| M1 | ドメイン永続化ポートが `IDbTransaction`（ADO.NET）に依存 → UoW 抽象の内側に隠す or ドリブンポート再配置 | `IShipperRepository`, `IEstimateRepository` | architect | 技術的関心事がドメイン出力ポートに漏れ、永続化機構差し替え時にポート署名が動く |
| M2 | コマンドサービスが具象 `UnitOfWork` を直接 new（DIP バイパス・定型重複） → `IUnitOfWorkFactory`/`Func<IUnitOfWork>` 注入 | `RegisterShipperCommandService`, `CreateEstimateCommandService` | architect | port を port として使えておらず、コマンド増加で重複増 |
| M3 | メール重複の TOCTOU → `email` に UNIQUE 制約追加 + 制約違反を `EmailAlreadyRegisteredException` に翻訳 | `0002_shipper.sql`, `RegisterShipperCommandService` | programmer, architect | check-then-act は並行時に二重登録を許す。DB 制約が最終防波堤 |
| M4 | `Estimate.Create` に到着期限のドメイン検証（過去日ガード等） | `Estimate.cs:37` | programmer, user | Controller の相関検証だけでは集約不変条件を保証できない |
| M5 | `AccessDeniedPath = "/login"` → 専用の 403「権限がありません」画面に分離 | `Program.cs` | programmer | 認証済み・ロール不足で再ログインを促す混乱挙動 |
| M6 | domain-model.md ShipperCode 桁数（8→6）、README の起動/ログイン手順・シードユーザー追記 | domain-model.md:528, README.md | technical-writer | 実装は 6 文字。README にウォーキングスケルトンの起動手順がない |

### 低（改善の余地あり）

| # | 提案 | 箇所 | 指摘元 |
|---|------|------|--------|
| L1 | 楽観的ロック未実装を「意図的な負債」として計画/ADR に明記（更新ユースケース着手時に返済） | Shipper/Estimate version | architect |
| L2 | `enum.ToString().ToUpperInvariant()` の DB 文字列化を一箇所に集約（DRY） | 各 Repository | programmer |
| L3 | メール/ユーザー名照合の大文字小文字（正規化検討） | Email/UserRepository | programmer |
| L4 | data-model.md の shipper DDL 例が `NOW()` 含み・`version` 欠落（実マイグレーションに追随） | data-model.md | architect, technical-writer |
| L5 | テンプレ残骸 `UnitTest1.cs` の削除/リネーム | Architecture.Tests, Web.Tests | programmer |
| L6 | domain-model.md EstimateStatus「表示名を保持」の記述削除（実装は素の enum） | domain-model.md:1063 | technical-writer |

## 矛盾事項

| # | 視点 A | 視点 B | 論点 | 推奨判断 |
|---|--------|--------|------|----------|
| 1 | programmer（法人必須検証の Controller/サービス二重化は防御的多重化として妥当） | architect（知識重複・DIP バイパス） | 相関バリデーションの配置 | UI 検証=Controller、入力ガード=サービス、不変条件=集約 の 3 層で役割を明示（コメント化）。重複は許容し意図を文書化 |

## 高優先項目の対応方針（IT1 完了時点）

| # | 方針 | 備考 |
|---|------|------|
| H1 ArchUnit | **IT2 冒頭で実装**（持ち越し 1.6 の CQRS ADR は済み。アーキテクチャテストは基盤の一部として最優先） | 参照実装が緑の今が最適 |
| H2 住所不整合 | **対応済み（2026-07-08）** | 「実装に住所を追加」方向で統一。Address VO・shipper.address（0004 マイグレーション）・登録フォーム・一覧・リポジトリを追加し、data-model にカラム追記。domain-model/US02/UI と全一致 |
| H3 危険物警告 | **IT1 内で最小対応**（危険物選択時の警告表示）または US05（IT2）で申告フォーム本実装として計画 | 受入条件 6 の扱いを PO 確認 |
| H4 法人 htmx | **IT1 内で対応可**（htmx 部分更新の小改修） | ui_design 準拠 |
| H5 期限/ルート通知 | **IT3 で実データ化時に対応**（現状スタブ）と計画へ明記 | 受入 5 の仕様確定を PO 確認 |

## エージェント別サマリー

- **xp-programmer**: 値オブジェクトによる不変条件集約・post-commit 構造的安全・方言禁止遵守を高評価。改善は Estimate 期限検証・法人検証の二重化整理・AccessDeniedPath・メール TOCTOU。
- **xp-tester**: Given/When/Then の意図明確さを評価。優先 3 点＝受入条件（US01-5/6・US02-2）の未検証/乖離、ArchUnit 未実装、E2E の診断性。
- **xp-architect**: ヘキサゴナル+CQRS の一貫性を高評価。高＝ArchUnit 未実装・ドメインポートの ADO.NET 依存。中＝UoW 直接 new・メール TOCTOU。
- **xp-technical-writer**: `<summary>` の why 記述・ユビキタス言語一致を高評価。高＝domain-model の住所ドリフト。中＝ShipperCode 桁数・README。
- **xp-user-representative**: 業務入口の骨格成立・PRG/フラッシュ/ロール別ナビを評価。高＝危険物申告フォーム・法人フィールド動的切替。中＝重複メール導線・期限過去日・見積↔荷主紐付け。

## レビュー完了条件

- [x] 全 5 エージェントのフィードバック収集
- [x] 統合レポート作成・保存
- [x] 重要度「高」の対応方針を決定（上表）
