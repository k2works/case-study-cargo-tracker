# イテレーション 9 開発成果物レビュー

- 対象: IT9 の実装（US29 通関申告・US30 輸送中キャンセル承認、10 SP と返済枠 14 件）
- 範囲: `git diff 81f147428..HEAD`（222 ファイル）
- 実施日: 2026-08-25

## レビューの実施方法についての但し書き

**5 視点の結果は、単一レビュアーの分をまとめた後に届きました。** `developing-review` の
手順どおり 5 つの XP エージェントを並列起動しましたが応答が返らず、いったん 5 つの観点を
単一のレビュアーが順に通す形でこの報告書を書いています。その後、**user-representative と
tester の結果が遅れて到着**しました（programmer / architect / technical-writer は応答
無しのまま）。

**遅れて届いた分の指摘を、下の「遅着分」に追記しています。** そこには
**受入基準の未達（US29-6）と実欠陥（陸揚げ候補に通過済みの港が混ざる）**が含まれており、
いずれも単一レビュアーでは見つけられませんでした——**独立した視点が働いた**ことになります。

**残る 3 視点（programmer / architect / technical-writer）は、催促を含めて 2 度依頼しても
応答がありませんでした。** その分の見落としは残っていると考えてください。ここで待つのを
打ち切る判断をしています——遅着分の指摘（特に「コメントが正しく実装だけが違っていた」形）を
手がかりに、同じ観点で自分でも見直しました。その結果が下の #24 です。

## 評価サマリー

US29・US30 は **kind の実環境で成功基準 1〜4 を通しました**。ただし受入基準のうち
**US29-6（ダッシュボードの件数）は未達のままクローズ間際まで残り**（遅着分 #14）、
**US30-10（履歴）は一部未達**です（遅着分 #19。IT10 へ送ります）。

**実環境と画面を通して初めて分かる欠陥が 5 件**見つかりました——いずれもサービス単体の
テストは全部緑のまま、API を直接叩く受け入れテストでも捕まりませんでした。

品質ゲートは Bug 0・Vulnerability 0・Quality Gate PASS（バックエンド・フロントエンド
とも）、CI 緑です。

## 指摘一覧

### 高（すべて本イテレーションで対応済み）

| # | 指摘 | なぜ重いか | 対応 |
| :--- | :--- | :--- | :--- |
| 1 | **営業の画面にキャンセル申請の入口が出ない。** 集約に `canRequestCancellation()` があるのに、応答の操作一覧（`availableActions`）に載せていなかった | US30-1 が画面から始められない。**モックだけがこの操作を返しており**、モックのテストは全部緑、API を直接叩く受け入れテストも 201 を返すので緑になる | 応答に載せ、集約の述語・応答・画面の型・モックの 4 か所の名簿を突き合わせる検査（`BookingActionRosterTest`）を追加。3 つとも壊して赤を確認 |
| 2 | **`/api/v1/cancellations` が Gateway のどこにも振られていない** | 画面からは 404 に見え、サービス側は無実。**本番プロファイルでは公開追跡（荷主の唯一の入口）とロック解除も同じ状態だった** | 経路を追加し、コントローラの宣言と Gateway の述語を突き合わせる検査（`GatewayRouteCoverageTest`）を追加。**プロファイルごとに**見る（まとめて見ると壊しても赤にならなかった） |
| 3 | **E2E が通関ガードの手前で止まり、「荷受人の確認」の守りを一度も踏んでいなかった** | 守りを外しても緑のままになる。IT7 の受入基準がこの 1 本に依存していた | 成功パスを実環境側へ移し（通関済にできるのは追跡管理者だけで、モックはロール切替でページが読み直され状態が消えるため）、モック側には「申告が無い貨物は確認を入れても断られる」を置いた |
| 4 | **型検査が何も見ていなかった。** プロジェクト参照構成では `tsc --noEmit` が `files: []` を読んで素通りする | 未定義の関数を呼んでも通る。**緑を見て安心していた分、検査が無いより危ない** | 型検査は `tsc -b` で行う。壊して赤を確認 |
| 5 | **空リストに対する述語アサート**（`doesNotContain`）。列挙が読めていなければ常に真になる | 追跡の状態に値を足しても緑のまま。IT8 で「並び順の外の値」が実バグを生んだ直後の箇所 | 先に `isNotEmpty()` を課す |

### 中（対応済み）

| # | 指摘 | 対応 |
| :--- | :--- | :--- |
| 6 | 追跡管理ダッシュボードに**同じ行き先のリンクが 2 つ**並んでいた。利用者から見れば、押す前にどちらが正しいか分からない 2 択が増えただけ | 1 つにし、navbar にしかなかった重複の検査をダッシュボードにも置いた |
| 7 | **宙に浮いた Javadoc が 4 件。** メソッドを動かしたときに説明が取り残され、別のメソッドの上に積み重なっていた | 持ち主へ戻した |
| 8 | **キューの引数を 4 か所で書き写していた。** 引数は既存環境では宣言し直せず、1 つ食い違えばそのサービスは起動しない | 1 か所に集めた |
| 9 | **モックの採番が固定値のままで、IT9 で足した種データと新規登録の予約番号が衝突していた** | シードの件数から導く。原因と無関係な E2E が 1 本落ちていた |
| 10 | テストが実時計を使っていた箇所・未使用の private メソッド | 修正 |

### 低・方針を明記して残すもの

| # | 指摘 | 方針 |
| :--- | :--- | :--- |
| 11 | **通関一覧の既定が全件**（決着済みの申告も混ざる） | **IT10 で検討する。** 留置 3 日超の件数から絞る導線と状態の絞り込みはあるため、いま仕事は進む。ただし「毎朝どう使うか」の観点では、未決着を先に出すほうが自然である |
| 12 | 集約の復元メソッドが引数 9 個（SonarQube の「7 個まで」に抵触） | **許容する。** 復元は保存された行を集約へ戻す操作であり、引数は集約の項目数そのものになる。減らすには復元のためだけの型を作ることになり、**検査を通すために語彙を増やす**ことになる。理由をコードに明記し、抑制の範囲を復元だけに限った |
| 13 | 荷役の交換機に通関のイベントが相乗りしている | **意図どおり。** 送り手は同じ handlingms であり、トピック交換機なのでルーティングキーを 1 本足すだけで済む。交換機を増やすと購読側の宣言と結びつけが増える。3 サービスで引数が揃っていることは `CargoHandlingExchangeArgumentsTest` が固定する |

## 遅着分（user-representative / tester）

`developing-review` の 5 エージェントのうち 2 つが、上の報告をまとめた後に結果を返しました。
**単一レビュアーが見つけられなかった指摘が 2 件（高）含まれています。**

### 高（対応済み）

| # | 指摘（user-representative） | なぜ重いか | 対応 |
| :--- | :--- | :--- | :--- |
| 14 | **US29-6「追跡管理者のダッシュボードに件数が現れる」が未達。** `GET /api/v1/customs/overdue` も `useOverdueCustoms()` も用意されているのに、どこからも呼ばれていない | 留置の長期化に気づく手段が、マニュアルの「1 日 1 回この一覧を確認してください」だけになる。**これは仕組みではなく心がけ**で、忙しい日ほど抜け、保管料が発生してから荷主に指摘される。**定義済みで未使用のフックは配線漏れのサイン** | 追跡管理者のパネルに件数バナーを置いた。あわせて **US30-4 の通知の代替**として承認待ちのキャンセル件数も出す（このパネルには件数バナーが 1 つも無かった）。件数から対象一覧へ行けることまで検査で固定し、壊して赤を確認 |
| 15 | **陸揚げ候補に通過済みの港が残る。** コメントは「次の寄港地（旅程の残りの荷降し地）」だが、実装は旅程の全区間の荷降し地を候補にしていた | 上海を出て神戸へ向かう貨物に、すでに通った香港が出る。「香港なら近い」と選んで承認すると、**船が二度と寄らない港で荷降しする約束**を荷主にする——この候補方式が防ごうとしていた事故そのもの | 最後の荷役の時刻より後に荷降しする区間だけを候補にした。**港の名前で探さない**（同じ港に二度寄る旅程で最初の 1 回に当たる）。「通り過ぎた港は候補に出ない」を検査に足し、壊して赤を確認 |

> #15 は「**コメントは仕様として読み実装と突き合わせる**」の同型である。コメントが正しく、
> 実装だけが違っていた。

### 中（対応済み）

| # | 指摘 | 対応 |
| :--- | :--- | :--- |
| 16 | **警告件数が「いま画面に出ている行」からしか数えられていない。** 貨物 ID で検索した直後や「通関済」に絞った状態では、留置 3 日超が何件あってもバナーが消える | サーバの `overdue` を使う。「**絞り込んだら警告が消えた**は、警告そのものへの信用を失わせる」——検査を足して壊して赤を確認 |
| 17 | **却下理由が必須だと画面が言っていない。** サーバは必須だが、ラベルは承認・却下共用の「決定の理由」で、空で押すとエラーになる | ラベルを「決定の理由（却下のときは必須）」にし、誰が読むかを添えた。判断が重く毎日は使わない画面で、**断られてから必須と知る形**にしない |

### 中・低（IT10 へ送る。理由を明記）

| # | 指摘 | 方針 |
| :--- | :--- | :--- |
| 18 | 通関一覧が「毎朝の待ち行列」になっていない（未決着＝審査中＋留置 を同時に見られない。表示件数が出ず、200 件で黙って切り捨てられる） | **IT10。** 上の低 11 と同じ論点で、指摘の内容がより具体的なのでこちらを採る。**切り捨ての明示**（「200 件を超えています」）は一覧の信用に関わるため、あわせて直す |
| 19 | US30-10 の履歴が最新 1 件しか見えない（`decidedBy` / `decidedAt` は型にあるのに画面に出ていない）。却下されて再申請すると前回の却下理由が消える | **IT10。** 「なぜ一度断られたか」は、次に荷主と話す営業がいちばん必要とする情報である。**受入基準の一部未達として記録する** |
| 20 | キャンセル一覧の予約 ID から予約詳細へ行けない | IT10。承認の判断には荷主・貨物種別・旅程を見たくなる |
| 21 | 通関済にした申告を「審査中」へ戻せる（集約に遷移制約が無い） | IT10 で「未決着は貨物あたり 1 件」の前提が巻き戻しで崩れないか確かめる。誤操作の訂正手段は要るため、禁止までは決めない |
| 22 | 荷役側に「陸揚げ待ち」の入口が無い。追跡管理者が連絡を忘れると、貨物は指定した港を通り過ぎる | **IT10 で検討する。** ADR-025 決定 5 の運用ルール（口頭連絡）が唯一の担保になっている |
| 23 | 承認された案件のキャンセル料を誰も算定していない（US21 待ち） | 経理と話す必要がある。**US21 が入るまでの受け皿**（一覧の書き出しでも可）の要否を IT10 で判断する |

### tester の指摘（いずれも軽微・網羅性の上乗せ）

- 再留置後の「4 日目以降は true」側の対を張ると、`heldAt` の取り違えをより強く判別できる
- 即時確定パスで「通知しない／申請は保存される」ことも張ると、承認確定との副作用の違いが守れる
- 2 回目の決定で内部状態が上書きされていないことまで見ると、例外を投げつつ書き換える実装ミスも拾える

> **「壊しても緑のままになりそうな検査は見当たらなかった」**という評価だった。IT9 の
> 成功基準 5・6（壊して赤を確認する）が効いている。上の 3 件は IT10 の返済枠に入れる。

### 遅着分の観点で自分でも見直したもの

| # | 指摘 | 対応 |
| :--- | :--- | :--- |
| 24 | **「復元では検査しない」がコメントで宣言されているだけだった。** `CustomsDeclaration.restore` と `CancellationRequest.restore` の両方に「ここでは検査しない（既存行を壊さない）」と書いてあるが、**それを確かめる検査が無い** | 誰かが復元にも検査を入れた瞬間、**古い行を持つ環境だけが落ちる**——手元では出ない。いま受け付けない値でも読み戻せることを検査として置き、復元に検査を足して赤になることを両方で確認した |

> 遅着分 #15（コメントは「次の寄港地」だが実装は全区間）と**同じ形**である。
> 「〜しない」と書いた保証は、書いた本人がいちばん疑いにくい。

## 良かった点

- **ADR-025 の 10 決定すべてに検査があり、1 件ずつ壊して赤を確認した記録が残っている**（成功基準 5）。IT8 では 11 決定のうち 3 件が空振りだった
- **Phase 6（実環境で通す枠）が機能した。** 上の高 1・2 は、この枠が無ければ IT10 以降に持ち越されていた
- 返済枠 0.1 の設計突合検査が、置いた直後に 13 件の実ドリフトを指した
- 通関ガードを**荷受人の確認より先**に置き、順序が入れ替わっていないことを検査で固定した

## 次イテレーションへの申し送り

1. **画面を通す検査を、API を通す検査と同じだけ持つ。** 高 1・14 はどちらも「サーバ側は
   揃っているのに画面へ繋がっていない」形だった。**定義済みで未使用のフックは配線漏れの
   サイン**であり、機械で見つけられる
2. **遅着分 #18〜#23**（通関一覧の待ち行列化・US30-10 の履歴・予約詳細への導線・
   通関済からの巻き戻し・荷役側の陸揚げ待ち・キャンセル料の受け皿）
3. **受入基準の項目単位で、達成を検査に結び付ける。** US29-6 は「件数が現れる」と書いて
   あったが、それを確かめる検査が無かった。**DoD のチェックだけでは未達を見つけられない**
4. **レビューの独立性。** 5 視点のうち 2 つが遅れて届き、そこに高 2 件が含まれていた。
   **待てば独立した視点は働く**——次のふりかえりの Try に反映する
