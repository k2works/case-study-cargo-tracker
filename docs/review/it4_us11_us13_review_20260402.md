# IT4 コードレビュー結果（US11〜US13）

- **レビュー日**: 2026-04-02
- **対象イテレーション**: IT4（US11〜US13）
- **対象コミット**: `c06491d`（feat(handling): US11 RECEIVE 重複防止ロジックを追加する）
- **変更規模**: 29 ファイル、+1,365 行
- **テスト結果**: 352 テスト、0 失敗、ライン coverage 93.0%、ブランチ coverage 71.7%
- **レビュアー**: xp-programmer / xp-tester / xp-architect / xp-technical-writer / xp-user-representative / xp-interaction-designer（7 エージェント並列）

---

## 総合評価

DDD・CQRS・ポートアダプタパターンを適切に適用した高品質な実装であり、テストピラミッド（ユニット→スライス→E2E）も揃っている。一方で、**US13 追跡ページが受入条件 2 を充足していない**（出発地・目的地・現在状態・予定到着日の欠落）点は今イテレーション内での修正が必要。また BC 境界違反・HTTP ステータス設計ミス・E2E テストのサイレント成功バグなど、技術品質の観点で対処すべき重要な指摘が複数存在する。CHANGELOG の更新漏れと OpenAPI ドキュメントの不整合もドキュメント品質として対応が求められる。

---

## 改善提案（重要度順）

### 高（今イテレーション内に対応すべき）

| # | 提案 | 箇所 | 指摘元 | 理由 |
|---|------|------|--------|------|
| H1 | `show.html` に出発地・目的地・現在状態・予定到着日を追加 | tracking/show.html | writer / user / uiux-user / uiux-designer | US13 受入条件 2 の直接的な未充足 |
| H2 | `TrackingWebController.show()` が 404 ページを HTTP 200 で返すのを修正 | TrackingWebController.java | programmer | SEO・クローラ・クライアントが誤った成功として扱う |
| H3 | US13 E2E の `if (trackingNumber == null) return;` を `assertNotNull` に修正 | US13TrackingInfoE2ETest.java | programmer / tester | セットアップ失敗を握りつぶしてテストが偽陽性になる |
| H4 | E2E 全クラスに未認証アクセスの検証テストを追加（US11/US12 は 302、US13 は 200） | E2E テスト群 | tester | Spring Security 設定ミスが本番まで検知されない |
| H5 | `HandlingRestControllerTest` に RECEIVE 重複 → 409 Conflict のテストを追加 | HandlingRestControllerTest.java | tester | REST 層の重複エラーハンドリングが未検証 |
| H6 | `TrackingRestController` に `@Tag`・`@Operation`・`@ApiResponses` を追加 | TrackingRestController.java | writer | 公開 API の Swagger ドキュメントが皆無 |
| H7 | `CHANGELOG.md` に IT3・IT4 の成果を記録 | CHANGELOG.md | writer | リリースノートとして機能しない状態 |
| H8 | `TrackingQueryService` の `HandlingEventType` import を除去（BC 境界違反解消） | TrackingQueryService.java | architect | tracking BC が handling BC に直接コンパイル依存 |
| H9 | ArchUnit テストに A10 ルール（tracking ↔ handling 境界）を追加 | ArchitectureTest.java | architect | 今回の境界違反が CI で検出されなかった |
| H10 | `handling/list.html` に引取記録・手動更新への導線ボタンを追加 | handling/list.html | uiux-user / uiux-designer | 荷役一覧から receive/manual-update へのナビゲーションがない |
| H11 | `handling/manual-update.html` の memo textarea に `required` 属性を追加 | manual-update.html | uiux-designer | UI 上は必須表示なのにブラウザバリデーションが効かない |
| H12 | `role="note"` を削除（不正な ARIA ロール・WCAG 2.1 違反） | handling/receive.html | uiux-designer | ARIA 1.1/1.2 仕様外のロール値でスクリーンリーダーに予期不明な挙動 |

### 中（対応推奨）

| # | 提案 | 箇所 | 指摘元 | 理由 |
|---|------|------|--------|------|
| M1 | `HandlingRestController` の `@ApiResponses` に 409 を追加 | HandlingRestController.java | writer | RECEIVE 重複エラーが OpenAPI 未定義でクライアントが対応できない |
| M2 | `DuplicateReceiveException` をドメイン層（domain/model/exceptions）に移動 | DuplicateReceiveException.java | architect / programmer | ドメイン不変条件違反の例外がアプリケーション層に置かれている |
| M3 | `TrackingMapper.xml` の `::uuid` を H2 互換構文（`CAST`）に修正 | TrackingMapper.xml | architect | PostgreSQL 固有構文で H2 テスト環境と非互換のリスク |
| M4 | `HandlingEventRecordedEvent` の消費者不在を ADR に記録し方針決定 | RecordHandlingEventCommandService.java | architect | 発行されるが誰も受け取らない状態が意図的かどうか未定義 |
| M5 | E2E テスト 3 クラスの `loginAsUser`・`createConfirmedBooking` を基底クラスに昇格 | E2E テスト群 | programmer / tester | 同一コードが 3 クラスにコピーされ保守コストが高い |
| M6 | `TrackingQueryService` の `findByBookingId` に欠落テストを追加 | TrackingQueryService.java | tester | サービス層メソッドが未テスト |
| M7 | `resolveDisplayName` の未知イベント種別フォールバックにログを追加 | TrackingQueryService.java | programmer / tester | サイレントフォールバックで DB 不正データが気付かれない |
| M8 | `HandlingWebController` の `@ExceptionHandler(BookingNotFoundException)` を整理 | HandlingWebController.java | programmer | 全 POST メソッドが try-catch しているため実質デッドコード |
| M9 | `TrackingResponse.from(TrackingEntry)` を削除または修正（常に空リストを返す） | TrackingResponse.java | programmer / writer | 使用中なら荷役履歴が常に空になるバグ、未使用なら YAGNI 違反 |
| M10 | `HandlingWebController` の RECEIVE/MANUAL_UPDATE ハンドラを対称な構造に統一 | HandlingWebController.java | programmer | eventType 強制セットのタイミングが非対称で可読性が低い |
| M11 | `RecordHandlingEventCommandServiceTest` の `atLeastOnce()` を `times(1)` に変更 | RecordHandlingEventCommandServiceTest.java | tester | 複数回発行されても Pass する精度不足 |
| M12 | 両フォームに成功フラッシュメッセージ表示領域を追加 | receive.html / manual-update.html | uiux-user / uiux-designer | 登録成功後のフィードバックが HTML に未定義 |
| M13 | `completionTime` に現在日時をデフォルト値として設定 | receive.html / manual-update.html | uiux-user / uiux-designer | 作業直後の記録が多いのに毎回カレンダー選択が必要 |
| M14 | 追跡ページのイベント種別バッジに荷役一覧と同じセマンティック配色を適用 | tracking/show.html | uiux-user / uiux-designer | list.html は色分けされているが show.html は全件グレー固定で不整合 |
| M15 | 手動更新ボタンに確認ダイアログを追加 | manual-update.html | uiux-designer | 取り消し不可の重大操作なのに誤送信防止機構がない |
| M16 | `TrackingQueryService` の `resolveDisplayName` キャッチブランチにテストを追加 | TrackingQueryService.java | tester | 分岐 coverage 71.7% の要因の一つ |

### 低（改善の余地あり）

| # | 提案 | 箇所 | 指摘元 | 理由 |
|---|------|------|--------|------|
| L1 | `HandlingEventView` の命名を `RecordedHandlingEvent` または `HandlingHistoryEntry` に変更 | HandlingEventView.java | architect | "View" サフィックスがプレゼンテーション層概念と混同される |
| L2 | E2E テストメソッドプレフィックスを `US11_`/`US12_`/`US13_` に統一 | E2E テスト群 | programmer / tester | クラス名（US11〜US13）とメソッドプレフィックス（E12〜E14）が不一致 |
| L3 | `HandlingEventTest` のヘルパーメソッドを `static` に変更 | HandlingEventTest.java | programmer | インスタンスフィールド不参照なので static が適切 |
| L4 | `receive.html`/`manual-update.html` に locationCode の大文字変換・pattern・aria-describedby を追加 | 両フォーム | uiux-user / uiux-designer | new.html には実装済みで不一致 |
| L5 | 追跡テーブルに `table-responsive` ラッパーを追加 | tracking/show.html | uiux-designer | モバイル幅で横スクロール不可になるリスク |
| L6 | ヘッダーナビに `active` クラスと `aria-current="page"` を追加 | fragments/header.html | uiux-designer | 現在ページの視覚的・スクリーンリーダー的な識別がない |
| L7 | `HandlingWebController.java` の UUID パース防御を追加 | HandlingWebController.java | programmer | バリデーション迂回経路で `IllegalArgumentException` が漏れる可能性 |

---

## 矛盾事項

| # | 視点 A | 視点 B | 論点 | 推奨判断 |
|---|--------|--------|------|----------|
| 1 | uiux-designer: `tracking/show.html` のヘッダーナビなしは意図的（公開ページ） | uiux-user: 少なくともサービス名・ロゴは表示すべき | 認証が必要なヘッダーコンポーネントを公開ページに出すべきか | 認証不要の軽量ヘッダー（ロゴ + サービス名のみ）を別フラグメントとして作成し適用する |
| 2 | programmer: `DuplicateReceiveException` をドメイン層に移動 | architect: 同じ方向性だが基底例外クラス設計も検討 | 移動先パッケージとクラス階層 | `handling.domain.model.exceptions` への移動を優先し、基底クラス設計は別 ADR で判断 |

---

## エージェント別フィードバック詳細

<details>
<summary>xp-programmer（高: 3 / 中: 5 / 低: 3）</summary>

### 評価サマリー
IT4 の実装は DDD・CQRS・ポートアダプタパターンを適切に適用し、テストピラミッドが揃った高品質な実装。一方で E2E テストの DRY 違反、`TrackingWebController` の HTTP ステータス設計ミス、例外クラスのパッケージ配置ミスなど修正が必要な問題が複数ある。

### 良い点
- `HandlingEvent` の `recordEvent()`/`reconstitute()` 静的ファクトリメソッド設計が優秀（ドメインイベント二重発行バグを構造的に防ぐ）
- `canReceive()` によるドメインルールの集約ルート内への集中
- `RecordHandlingEventCommandService` の責務が明確
- `TrackingQueryService` の `Optional` 連鎖（null 安全）
- `HandlingEventTest` の `@ParameterizedTest + @EnumSource` + ヘルパーメソッドで重複排除

### 重要指摘
- **【高】TrackingWebController.show()** が 404 ページを HTTP 200 で返す
- **【高】US13 E2E の `if (trackingNumber == null) return;`** → `assertNotNull` または `assumeTrue` に変更
- **【高】E2E 3 クラスの `loginAsUser`/`createConfirmedBooking` DRY 違反** → `PostgreSQLIntegrationTestBase` に昇格
- **【中】`DuplicateReceiveException` パッケージ配置ミス** → `handling.domain.model.exceptions` に移動
- **【中】`TrackingResponse.from(TrackingEntry)` が常に空リストを返す**
- **【中】`@ExceptionHandler(BookingNotFoundException)` が実質デッドコード**

</details>

<details>
<summary>xp-tester（高: 3 / 中: 4 / 低: 2）</summary>

### 評価サマリー
全体的に TDD サイクルに沿った良質なテストスイートが構築されており、主要な正常系・異常系・RECEIVE 重複防止ロジックは十分にカバーされている。ただし非認証パスの検証が一貫して欠落している点と E2E テストのサイレントスキップ問題が潜在的リスクとして残る。

### 良い点
- `canReceive` の 3 ケース網羅（空リスト/RECEIVE あり/RECEIVE なし）
- `@ParameterizedTest @EnumSource` による全 `HandlingEventType` の一括テスト
- `reconstitute` vs `recordEvent` のポリシーがテストで明示
- `verify(handlingEventRepository, never()).save(any())` による「例外時に保存されない」保証

### 重要指摘
- **【高】US13E2ETest の null ガード** → 例外握りつぶし解消
- **【高】E2E 全クラスの非認証パス検証欠落**
- **【高】HandlingRestControllerTest に 409 テストなし**
- **【中】`resolveDisplayName` の catch ブランチ未テスト**
- **【中】E2E の DRY 違反**
- **【中】`atLeastOnce()` を `times(1)` に変更**

</details>

<details>
<summary>xp-architect（高: 1 / 中: 3 / 低: 2）</summary>

### 評価サマリー
ヘキサゴナルアーキテクチャの基本構造は堅実に実装されており、ADR-002 の `@TransactionalEventListener(AFTER_COMMIT)` も適切に活用されている。ただし `tracking` BC が `handling` BC のドメインモデルを直接 import する BC 境界違反が 1 件存在し、既存の ArchUnit ルールに検出ギャップがある。

### 良い点
- `BookingExistencePort` による ACL 経由の BC 参照
- `HandlingEventView` を tracking BC 固有の値オブジェクトとして定義
- `TrackingNumberIssueService` の冪等性チェック
- `SecurityFilterChain` の `@Order` 分離

### 重要指摘
- **【高】BC 境界違反**: `TrackingQueryService` が `HandlingEventType` を直接 import
- **【スコープ外・要対応】ArchUnit A10 ルール欠落**
- **【中】`TrackingMapper.xml` の `::uuid`** PostgreSQL 固有構文
- **【中】`HandlingEventRecordedEvent` 消費者不在**
- **【低】`DuplicateReceiveException` パッケージ** → ドメイン層へ移動

</details>

<details>
<summary>xp-technical-writer（高: 4 / 中: 2 / 低: 1）</summary>

### 評価サマリー
Thymeleaf テンプレートの日本語化・バリデーション表示は全体的に丁寧に実装されており、US11・US12 のフォーム UI は良好。一方、追跡ページが US13 受入条件を部分的にしか満たしておらず、`TrackingRestController` の OpenAPI ドキュメントが皆無、かつ CHANGELOG が IT3・IT4 の成果を全く反映していない。

### 良い点
- `receive.html` の重複防止説明文
- `manual-update.html` の警告バナー
- `HandlingRestController` の充実した OpenAPI アノテーション
- `iteration_plan-4.md` の全タスク・受入条件のトレーサビリティ

### 重要指摘
- **【高】`TrackingRestController` に OpenAPI アノテーションなし**
- **【高】`CHANGELOG.md` に IT3・IT4 未記録**
- **【高】`show.html` が US13 受入条件 2 を部分充足のみ**
- **【高】`HandlingRestController @ApiResponses` に 409 未定義**
- **【懸念】`TrackingResponse.from(TrackingEntry)` が `List.of()` を返す**

</details>

<details>
<summary>xp-user-representative（高: 3 / 中: 4 / 低: 1）</summary>

### 評価サマリー
US11・US12 のフォームは業務の流れに沿った基本構造を持っている。ただし荷主が最も使う追跡ページ（US13）の受入条件が未達成であり、かつシステム内の画面間の導線に重大な不整合が残っているため、このままリリースすると現場で混乱が生じる可能性がある。

### 良い点
- RECEIVE 重複防止メッセージ
- MANUAL_UPDATE の警告バナー
- 荷役一覧のバッジ色分け
- detail.html の追跡番号カード（未発行時は非表示）

### 重要指摘
- **【高】US13 追跡ページに出発地・目的地・現在状態・予定到着日が欠落**
- **【高】予約詳細 → 引取記録の導線が /handling/new（汎用）**
- **【高】MANUAL_UPDATE への導線がない**
- **【中】追跡ページに UUID・UNLOCODE を表示**
- **【中】completionTime に現在日時のデフォルト値がない**

</details>

<details>
<summary>xp-interaction-designer（高: 4 / 中: 3 / 低: 2）</summary>

### 評価サマリー
IT3 までのフォームパターンの一貫性を引き継いでおり Bootstrap 5 の骨格は整っている。一方で 3 ストーリー全てに「受入条件と実装画面のギャップ」があり、追跡ページのナビゲーション欠如と両フォームへの導線断絶が業務フローに深刻な支障をきたす可能性がある。

### 良い点
- フォームレイアウトの一貫性（`col-sm-3 / col-sm-9` 列分割）
- インラインバリデーションパターンの統一
- ガイダンス alert の設置（alert-info / alert-warning）
- ハンバーガーメニューの aria 属性（`aria-controls`/`aria-expanded`/`aria-label`）

### 重要指摘
- **【高】US13 追跡ページに必須情報が欠落**
- **【高】追跡ページに検索フォームと次アクション導線がない**
- **【高】handling 一覧から receive・manual-update への導線がない**
- **【高】memo textarea に `required` 属性なし**（ブラウザバリデーション不機能）
- **【高】`role="note"` は不正 ARIA ロール**（WCAG 2.1 違反）
- **【中】成功フラッシュメッセージ表示領域がない**
- **【中】手動更新ボタンに確認ダイアログなし**
- **【中】completionTime のデフォルト値なし**

</details>

---

## 付録 A: ArchUnit A10 ルール（案）

```java
@ArchTest
static final ArchRule A10_tracking_does_not_depend_on_handling =
    noClasses()
        .that().resideInAnyPackage(
            "com.example.cargotracker.tracking.application..",
            "com.example.cargotracker.tracking.domain.."
        )
        .should().dependOnClassesThat()
        .resideInAPackage("com.example.cargotracker.handling..")
        .as("[A10] tracking の application/domain 層は handling コンテキストを直接参照してはならない");
```

> **注意**: このルールを追加する前に H8（`TrackingQueryService` の `HandlingEventType` import 除去）を完了すること。

---

## 付録 B: CHANGELOG 追記案

```markdown
## [Unreleased]

### Added
- **US11**: 引取作業記録機能（`POST /handling/receive`）
  - RECEIVE イベント登録フォーム（港湾オペレーター向け）
  - 同一予約への RECEIVE 重複登録防止（`canReceive()` ドメインメソッド）
  - 重複時 HTTP 409 Conflict レスポンス
- **US12**: 貨物状態手動更新機能（`POST /handling/manual-update`）
  - MANUAL_UPDATE イベント登録フォーム（管理者限定）
  - メモ必須バリデーション
- **US13**: 追跡情報照会機能（`GET /tracking/{trackingNumber}`）
  - 公開追跡ページ（認証不要）
  - 荷役履歴表示（completionTime 降順）
  - 予約詳細画面からの追跡番号リンク
  - 追跡情報 REST API（`GET /api/v1/tracking/{trackingNumber}`）
```

---

## 付録 C: TrackingRestController OpenAPI アノテーション案

```java
@Tag(name = "tracking", description = "貨物追跡 API — 追跡番号による荷役履歴照会（認証不要）")
@RestController
@RequestMapping("/api/v1/tracking")
public class TrackingRestController {

    @GetMapping("/{trackingNumber}")
    @Operation(summary = "追跡情報照会",
               description = "追跡番号（TRK-XXXXXXXX）で貨物の現在状態と荷役履歴を取得する。認証不要。")
    @ApiResponses({
        @ApiResponse(responseCode = "200", description = "照会成功"),
        @ApiResponse(responseCode = "404", description = "指定した追跡番号が存在しない")
    })
    public ResponseEntity<TrackingResponse> getByTrackingNumber(...) { ... }
}
```
