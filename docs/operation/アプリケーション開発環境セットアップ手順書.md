# アプリケーション開発環境セットアップ手順書

## 概要

本ドキュメントは、国際貨物輸送管理システム（case-study-cargo-tracker）のアプリケーション開発環境をセットアップする手順を説明します。

テスト駆動開発（TDD）のゴールは **動作するきれいなコード** です。それを実現するためには [ソフトウェア開発の三種の神器](https://t-wada.hatenablog.jp/entry/clean-code-that-works) が必要です。

> 今日のソフトウェア開発の世界において絶対になければならない 3 つの技術的な柱があります。
> 三本柱と言ったり、三種の神器と言ったりしていますが、それらは
>
> - バージョン管理
> - テスティング
> - 自動化
>
> の 3 つです。
>
> — https://t-wada.hatenablog.jp/entry/clean-code-that-works

### 環境の 3 段構え

本プロジェクトは環境を 3 段階で構成します。本書が対象とするのは **1 と 2** です。

| 段階 | 環境 | 基盤 | 用途 |
| :--- | :--- | :--- | :--- |
| 1 | 日常開発 | Gradle bootRun + H2（`local` プロファイル） | 単一サービスの TDD サイクル。Docker 不要で即座起動 |
| 2 | ローカル統合 | kind + Kustomize | 全 7 サービス + PostgreSQL + RabbitMQ の結合確認 |
| 3 | 開発 / ステージング / 本番 | Heroku / AWS ECS | [開発環境セットアップ手順書](開発環境セットアップ手順書.md) 以降を参照 |

---

## 1. 前提条件

以下のツールがインストールされていることを確認してください。

| ツール | バージョン | 確認コマンド | 必須段階 |
| :--- | :--- | :--- | :--- |
| Java (JDK) | 25 (Eclipse Temurin) | `java -version` | 1 |
| Docker Desktop | 最新 | `docker -v` | 1（Testcontainers）/ 2 |
| Git | 最新 | `git -v` | 1 |
| Node.js | v22.x LTS | `node -v` | 1 |
| npm | v10.x | `npm -v` | 1 |
| kind | 0.24 以上 | `kind version` | 2 |
| kubectl | 1.31 以上 | `kubectl version --client` | 2 |

> **Note**: Gradle は Gradle Wrapper（`apps/backend/gradlew`）を使用するため、別途インストールする必要はありません。
> Kustomize は `kubectl` に同梱されているため（`kubectl apply -k`）、追加インストールは不要です。

### Java のインストール

Java 25 をインストールします。SDKMAN! を使用すると複数バージョンの管理が容易です。

```bash
# SDKMAN! でインストール
sdk install java 25-tem

# バージョン確認
java -version
```

公式サイトから直接ダウンロードする場合：

- https://adoptium.net/

### Docker のインストール

Docker Desktop をインストールします。統合テスト（Testcontainers）と kind クラスタの実行に必要です。

- **Windows**: https://docs.docker.com/desktop/install/windows-install/
- **macOS**: https://docs.docker.com/desktop/install/mac-install/

```bash
docker -v
docker compose version
```

### kind / kubectl のインストール

ローカル統合環境（段階 2）で使用します。

```bash
# macOS (Homebrew)
brew install kind kubernetes-cli

# Windows (Scoop)
scoop install kind kubectl

# 確認
kind version
kubectl version --client
```

### Node.js のインストール

コミット前の品質チェック（husky + lint-staged）およびタスクランナー（Gulp）に Node.js が必要です。

- https://nodejs.org/

```bash
node -v
npm -v
```

---

## 2. プロジェクトの取得

### リポジトリのクローン

```bash
git clone https://github.com/k2works/case-study-cargo-tracker.git
cd case-study-cargo-tracker
```

### Node.js 依存パッケージのインストール

```bash
npm install
```

> **Note**: husky（Git Hooks）が `prepare` スクリプトで自動的にセットアップされます。

---

## 3. サブシステム一覧

国際貨物輸送管理システムは以下のマイクロサービスで構成されています。

| サービス | ディレクトリ | Gradle モジュール | 説明 | ポート |
| :--- | :--- | :--- | :--- | :--- |
| API Gateway | `apps/backend/gatewayms` | `:gatewayms` | API ルーティング・JWT 検証（Spring Cloud Gateway） | 8080 |
| Auth Service | `apps/backend/authms` | `:authms` | 認証・認可（JWT）・アカウントロック | 8081 |
| Booking Service | `apps/backend/bookingms` | `:bookingms` | 貨物予約・経路割り当て・キャンセル承認 | 8082 |
| Routing Service | `apps/backend/routingms` | `:routingms` | 航海スケジュール・経路候補算出 | 8083 |
| Tracking Service | `apps/backend/trackingms` | `:trackingms` | 貨物追跡・状態管理・例外処理 | 8084 |
| Handling Service | `apps/backend/handlingms` | `:handlingms` | 荷役作業記録・通関申告 | 8085 |
| Billing Service | `apps/backend/billingms` | `:billingms` | 請求・精算・キャンセル料 | 8086 |
| Frontend | `apps/frontend` | - | コンポジット UI（React SPA） | 3000 |

### 共有コンポーネント

| コンポーネント | ディレクトリ | Gradle モジュール | 説明 |
| :--- | :--- | :--- | :--- |
| Shared Library | `apps/backend/shared` | `:shared` | 共有カーネル（`Location` / UN/LOCODE）・クロスコンテキストイベント |

### ローカル統合環境の外部サービス（kind クラスタ内）

| サービス | 名前空間 | ポート | 説明 |
| :--- | :--- | :--- | :--- |
| PostgreSQL | cargo | 5432 | 6 DB（auth_db〜billing_db）を 1 インスタンス内に作成 |
| RabbitMQ | cargo | 5672 / 15672 | メッセージブローカー / 管理 UI |

---

## 4. 技術スタック

詳細は [技術スタック選定](../design/tech_stack.md) を参照してください。

### バックエンド

| カテゴリ | 技術 | バージョン |
| :--- | :--- | :--- |
| 言語 | Java (Eclipse Temurin) | 25 |
| フレームワーク | Spring Boot | 4.x |
| ビルドツール | Gradle (Wrapper) | 9.x |
| データアクセス | MyBatis (spring-boot-starter) | 4.x |
| データベース | PostgreSQL / H2 | 16.x / 2.x |
| マイグレーション | Flyway | 10.x |
| メッセージング | Spring Cloud Stream + RabbitMQ | 4.x |
| API ゲートウェイ | Spring Cloud Gateway | 4.x |
| API ドキュメント | springdoc-openapi | 3.x |
| テスト | JUnit 5 / Mockito / AssertJ | 5.x / 5.x / 3.x |
| 統合テスト | Testcontainers | 1.20.x |
| アーキテクチャテスト | ArchUnit | 1.x |
| 契約テスト | Spring Cloud Contract | 4.x |
| 品質管理 | Checkstyle / SpotBugs | - |
| カバレッジ | JaCoCo | - |
| 設計ドキュメント生成 | JIG (jig-gradle-plugin) | 2026.7.4 |
| ER 図生成 | jig-erd | 0.2.2 |

### フロントエンド

| カテゴリ | 技術 | バージョン |
| :--- | :--- | :--- |
| 言語 | TypeScript | 5.x |
| フレームワーク | React | 19.x |
| ビルドツール | Vite | 6.x |
| サーバー状態管理 | TanStack Query | 5.x |
| クライアント状態管理 | Zustand | 5.x |
| CSS | Tailwind CSS | 4.x |
| ルーティング | React Router | 7.x |
| テスト | Vitest / Testing Library / Playwright | - |

### インフラストラクチャ

| カテゴリ | 技術 |
| :--- | :--- |
| ローカル統合 | Docker / kind / Kustomize |
| CI/CD | GitHub Actions |
| タスクランナー | Gulp 5.x |

---

## 5. プロファイル構成

| プロファイル | データベース | メッセージブローカー | 用途 |
| :--- | :--- | :--- | :--- |
| `local`（既定） | H2（インメモリ、PostgreSQL 互換モード） | 無効（またはローカル RabbitMQ） | 日常開発・即座起動 |
| `product` | H2 メモリ DB | CloudAMQP | Heroku 開発環境 |
| `staging` / `production` | RDS PostgreSQL | Amazon MQ | AWS 環境 |

> kind クラスタ上では ConfigMap / Secret で PostgreSQL・RabbitMQ の接続先を注入します（`local` プロファイル + 環境変数の上書き）。

### 日常開発（推奨）

Docker なしで即座に起動できます。各サービスは H2 インメモリデータベース（PostgreSQL 互換モード）を使用します。

```bash
cd apps/backend
./gradlew :bookingms:bootRun
```

---

## 6. 開発サーバーの起動

### タスクランナー経由（推奨）

```bash
# バックエンド開発サーバー起動（既定サービス）
npm start
# または
npx gulp dev:backend

# 個別サービス起動
npx gulp dev:backend:routingms

# フロントエンド開発サーバー起動（port 3000）
npm run dev:frontend

# TDD モード（テスト自動再実行）
npm run tdd

# タスク一覧を表示
npx gulp dev:help
```

### Gradle / npm 直接実行

```bash
# バックエンド（Gradle）
cd apps/backend
./gradlew :bookingms:bootRun          # local プロファイル
./gradlew test --continuous            # TDD モード

# フロントエンド（npm）
cd apps/frontend
npm run dev
npm test
npm run build
```

### フロントエンドのコマンド一覧

`apps/frontend` で実行します。手順書に無いコマンドは、書いた本人以外に使われません。

| コマンド | 何をするか | いつ使うか |
| :--- | :--- | :--- |
| `npm run dev` | API をモック（MSW）した開発サーバを起動する | 画面だけを触るとき。バックエンドを起動しなくてよい |
| `npm run dev:api` | モックを使わず実バックエンドに繋ぐ開発サーバを起動する | モックを実物に差し替えて確かめるとき |
| `npm test` | ユニット・コンポーネントテストを 1 回実行する | 変更のたび |
| `npm run test:watch` | 同上を監視モードで実行する | TDD サイクル中 |
| `npm run test:coverage` | カバレッジ付きで実行する | クローズ前の品質ゲート |
| `npm run lint` | oxlint を実行する | コミット前 |
| `npm run typecheck` | 型検査（`tsc -b`） | コミット前・品質ゲート |
| `npm run build` | 型検査 + 本番ビルド | デプロイ前・CI |

> **型検査は `npm run typecheck`（`tsc -b`）を使ってください。**
> `tsc --noEmit` は、このプロジェクトのプロジェクト参照構成（`tsconfig.json` が
> `files: []` + `references`）では**何も検査せず終了 0 を返します**。未定義の関数を
> 呼んでも通るため、**緑を見て安心する分、検査が無いより危険です**。
> 使ってはいけないコマンドが混入していないことは `typecheck-command.test.ts` が検査します。
| `npm run test:e2e` | Playwright の E2E を実行する | 受け入れの確認 |
| `npm run test:e2e:ui` | 同上を UI モードで実行する | E2E が落ちた原因を追うとき |
| `npm run test:e2e:real` | **実バックエンドに対する E2E**。kind の統合環境（`http://localhost`）に対して 1 本通す。事前に k8s の frontend・Gateway・全 backend サービス・PostgreSQL・RabbitMQ を起動し、変更後の image とマニフェストを反映する | モックで検証した機能を「動く」と言う前 |
| `npm run k8s:db:reset` | k8s ローカル統合環境の PostgreSQL を初期化し、DB 利用サービスを再起動して Flyway を再実行する | E2E や手動確認のデータを空に戻すとき |
| `npm run test:e2e:production` | **本番相当ビルドの検査**。動作確認用のログイン事前入力が焼き込まれていないことを確かめる | デプロイ前。**[ADR-006](../adr/006-demo-login-prefill.md) を守っているかを確かめる唯一の手段** |
| `npm run screenshots:manual` | ユーザーマニュアルの画面キャプチャを再生成する | 画面を変えたとき。PNG を手で差し替えない |
| `npx gulp manual:build` | マニュアルの Markdown を HTML に変換し `apps/www/manual` へ置く（**リポジトリルートで実行**） | キャプチャを撮ったあと。これを飛ばすと、読者が開く公開サイトのマニュアルだけ古いまま残る |

> `test:e2e:production` を飛ばすと、事前入力を有効にしたイメージを本番へ出しても誰も気づきません。
> ビルド時に成果物へ焼き込まれるため、実行時の環境変数では取り消せません。

### アクセス確認

| サービス | URL | 説明 |
| :--- | :--- | :--- |
| Frontend | http://localhost:3000 | コンポジット UI（React SPA） |
| API Gateway | http://localhost:8080 | メインエントリポイント |
| Auth Service | http://localhost:8081 | 認証サービス |
| Booking Service | http://localhost:8082 | 予約サービス |
| Routing Service | http://localhost:8083 | 経路サービス |
| Tracking Service | http://localhost:8084 | 追跡サービス |
| Handling Service | http://localhost:8085 | 荷役サービス |
| Billing Service | http://localhost:8086 | 請求サービス |
| Swagger UI | http://localhost:{ポート}/swagger-ui.html | API ドキュメント（各サービス） |
| H2 Console | http://localhost:{ポート}/h2-console | データベース管理（local プロファイル） |
| Health Check | http://localhost:{ポート}/actuator/health | ヘルスチェック（各サービス） |

---

## 7. ローカル統合環境（kind + Kustomize）

全 7 サービスを結合して確認する場合は、kind クラスタ上に Kustomize でデプロイします。
Kustomize は素の Kubernetes マニフェストを base（共通の土台）と overlay（環境ごとの差分）に分けて合成します。

### 7.1 Kustomize ディレクトリ構成

```text
ops/k8s/kustomize/
├── base/
│   ├── kustomization.yaml      # 束ねる定義 + configMapGenerator + images
│   ├── namespace.yaml
│   ├── secret.yaml             # DB 認証（ローカル用）
│   ├── init-databases.sql      # 6 DB 作成（ConfigMap 化）
│   ├── postgres.yaml
│   ├── rabbitmq.yaml
│   ├── authms.yaml             # ┐
│   ├── bookingms.yaml          # │ サービスごとに Deployment + Service
│   ├── routingms.yaml          # │
│   ├── trackingms.yaml         # │
│   ├── handlingms.yaml         # │
│   ├── billingms.yaml          # ┘
│   ├── gatewayms.yaml
│   ├── frontend.yaml
│   ├── www.yaml                # ドキュメントポータル（apps/www）
│   └── ingress.yaml
└── overlays/
    ├── local/
    │   └── kustomization.yaml  # ローカル差分（replicas: 1、イメージタグ）
    └── ci/
        └── kustomization.yaml  # E2E 用差分
```

### 7.2 クラスタの作成

```bash
# Ingress 用のポートマッピングを持つクラスタを作成
# クラスタ名は kind-cluster.yaml の name（cargo）が使われる
kind create cluster --config ops/k8s/kind-cluster.yaml

# NGINX Ingress Controller の導入
kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/main/deploy/static/provider/kind/deploy.yaml
kubectl -n ingress-nginx wait --for=condition=ready pod \
  --selector=app.kubernetes.io/component=controller --timeout=180s
```

### 7.3 イメージのビルドとロード

```bash
# 全サービスの jar を一度にビルドし、サービスごとにイメージ化
cd apps/backend && ./gradlew bootJar -x test
for s in gatewayms authms bookingms routingms trackingms handlingms billingms; do
  docker build -t "cargo-$s:0.0.1" "$s"
done
cd ../frontend && docker build -t cargo-frontend:0.0.1 .
cd ../www && docker build -t cargo-www:0.0.1 .

# kind クラスタへイメージをロード（9 イメージ）
cd ../..
for s in gatewayms authms bookingms routingms trackingms handlingms billingms frontend www; do
  kind load docker-image "cargo-$s:0.0.1" --name cargo
done
```

### 7.4 デプロイと動作確認

```bash
# 合成結果の確認（クラスタに影響なし）
kubectl kustomize ops/k8s/kustomize/overlays/local

# 適用
kubectl apply -k ops/k8s/kustomize/overlays/local

# 10 Pod がすべて 1/1 Running になるまで待機（初回はイメージ取得を含め 3〜8 分）
kubectl -n cargo get pods -w

# フロントエンド（Ingress 経由）
curl -o /dev/null -w "%{http_code}\n" http://localhost/

# ドキュメントポータル（apps/www）
curl -o /dev/null -w "%{http_code}\n" http://localhost/docs-portal/

# 6 DB が作成されていること
kubectl -n cargo exec deploy/postgres -- \
  psql -U cargo_tracker -d postgres -tAc \
  "SELECT datname FROM pg_database WHERE datname LIKE '%_db' ORDER BY datname"

# RabbitMQ に各サービスが接続していること
kubectl -n cargo exec deploy/rabbitmq -- rabbitmqctl list_connections
```

> タスクランナーを使う場合は `npx gulp dev:k8s:up`（イメージ作成 → ロード → 適用 → 状態表示）で
> 7.3 と 7.4 をまとめて実行できます。ドキュメントポータルは `npx gulp dev:k8s:docs:open` で
> `http://localhost/docs-portal/` を開けます。

> ローカルでは PostgreSQL 1 台の中に各サービス専用のデータベース（auth_db〜billing_db）を作成します。
> Database per Service の論理分離は保ちつつ、開発の起動コストを抑えます。
> DB 起動待ちの再起動は `readinessProbe` の範囲で復帰させます（起動順序制御は作り込みません）。

DB の中身を空に戻したい場合は、リポジトリルートで次を実行します。

```bash
npm run k8s:db:reset
```

このタスクは `postgres` Deployment を再起動してローカル統合環境の DB データを削除し、
PostgreSQL の Ready を待ってから DB を持つ 6 サービスを再起動します。各サービスの起動時に
Flyway が再実行され、初期スキーマが作り直されます。RabbitMQ のキューや Docker image は変更しません。

### 7.5 コードの変更をクラスタへ反映する

jar を作り直しただけでは、走っている Pod は古いものを掴んだままです。反映には次の 2 つを踏みます。

```bash
# 1. jar ビルド → イメージ作成 → kind へロード
npx gulp dev:k8s:images

# 2. Deployment に反映（Pod を作り直す）
npx gulp dev:k8s:rollout:image
```

> **マニフェスト（環境変数・Service・Ingress）を変えたときは、先に `dev:k8s:apply` を
> 踏みます。** `dev:k8s:rollout:image` が変えるのは**イメージだけ**であり、
> Deployment の `env` は古いままです。
>
> ```bash
> npx gulp dev:k8s:apply            # マニフェストを適用（env / Service / Ingress）
> npx gulp dev:k8s:rollout:image    # イメージを切り替え
> ```
>
> 実バックエンド E2E を流す前も同じ順序にします。特に IT13 の荷主向け追跡は
> `trackingms -> authms`、`trackingms -> bookingms`、`bookingms -> trackingms`
> の連携と RabbitMQ のイベント伝播を通るため、frontend / Gateway だけを更新しても
> 「実環境で通した」ことにはなりません。データを初期状態から確認したい場合は、
> `npm run k8s:db:reset` で PostgreSQL を初期化してから E2E を実行してください。
>
> **入れ忘れても起動は成功します。** 相手サービスの所在（`APP_*_SERVICE_BASE_URL`）は
> 既定値が開発機（localhost）を指すため、クラスタの中では自分自身を指し、
> **その相手を使う操作だけが失敗します**——IT5（経路の確定）、IT12（見積の候補）、
> IT13（荷主向け追跡の内部 link 照会）で踏みました。設定漏れは
> `ServiceEndpointConfigTest`（shared）が落とします。

タグを分けたい場合は、**両方に同じタグを渡します**。

```bash
npx gulp dev:k8s:images --tag it11-rev1
npx gulp dev:k8s:rollout:image --tag it11-rev1
```

`--tag` の代わりに `DEV_K8S_IMAGE_TAG=it11-rev1` でも指定できます。

#### 反映できたことを確かめる

**タスクの成功メッセージは「反映できたか」を判別しません。** `dev:k8s:rollout:image` は最後に
Pod の起動時刻を表示するので、**それが今の時刻になっていること**を見てください。

```text
POD                          IMAGE                       STARTED                STATUS
bookingms-5d6df85d74-vszkb   cargo-bookingms:it11-rev1   2026-08-26T00:41:37Z   Running
```

起動時刻が前のままなら、その Pod は作り直されていません。jar の日時でも確かめられます。

```bash
kubectl -n cargo exec deploy/bookingms -- ls -l /app/app.jar
```

> **タグを変えなくても Pod は作り直されます。** `kubectl set image` は spec が変わったときだけ
> ロールアウトを起こします。既定のタグ（`0.0.1`）は base のマニフェストに直書きしてあるため、
> イメージを作り直しても spec は変わらず、**同じタグのまま古い jar が走り続けます**。
> IT10 でこれを踏み、最初の実環境確認が誤った赤を出しました。
> いまは**タグが今と同じときは `rollout restart` に切り替える**ので、どちらの場合も掴み直します。

### 7.6 クラスタの停止・削除

```bash
# デプロイのみ削除
kubectl delete -k ops/k8s/kustomize/overlays/local

# クラスタごと削除
kind delete cluster --name cargo
```

### 7.7 イベントの取りこぼしを見つけて送り直す

サービス間のイベント（[ADR-022](../adr/022-domain-event-contract.md)）は、**届かなくても発行側がエラーになりません**。追跡番号を発行した予約の追跡が始まっていなくても、営業の画面には何も出ません。荷主から見ると「番号はもらったのに追えない」状態です。

気づく手段と、直す手段を用意しています。

#### 取りこぼしを照会する

```bash
npx gulp dev:k8s:events:missing
```

`booking_db` の発行済み追跡番号と `tracking_db` の追跡を突き合わせ、**追跡が始まっていない予約番号を並べます**。件数だけでは次の行動に繋がらないため、番号まで出します。

#### デッドレターのイベントを送り直す

```bash
npx gulp dev:k8s:events:redeliver

# 一度に送り直す件数を変える（既定 50）
DLQ_REDELIVER_LIMIT=10 npx gulp dev:k8s:events:redeliver
```

`trackingms.tracking-number-issued.dlq` から 1 件ずつ取り出し、元の交換機へ送り直します。送り直したあとに取りこぼしの照会を続けて実行するので、直ったかどうかがその場で分かります。

> **送り直す前に、原因が直っているかを確かめてください。** 購読側の不具合（地点マスタの欠落など）がそのままなら、送り直したイベントは**またデッドレターへ戻ります**。「再実行したのに直らない」ときは、まず `kubectl -n cargo logs deploy/trackingms` を見てください。

> **送り直しでは本番と同じ `__TypeId__` ヘッダを付けています。** 付け忘れると購読側が読めず、内容が正しくても戻ってきます。

#### 交換機の引数を変えたとき

交換機に `alternate-exchange` を足すなど、**引数を変えたときは既存の交換機を作り直します**。

```bash
npx gulp dev:k8s:events:redeclare
```

> **RabbitMQ は既存の交換機を違う引数で宣言し直せません**（`PRECONDITION_FAILED - inequivalent arg`）。宣言はそこで失敗し、**その後ろに続くキューの宣言まで行われません**。症状は「新しいキューが無い」（`NOT_FOUND - no queue ...`）として出るため、原因が交換機だと分かりにくくなります。IT7 で実際に踏みました。

> **Testcontainers のテストでは見つかりません。** テストのブローカーは毎回新品で、既存の交換機がある状態を再現しないためです。**新品では緑・既存環境では赤**という形の食い違いです。

> 交換機を消してもキューのメッセージは消えません。結びつけは消えますが、各サービスが起動時に宣言し直します。

#### どのキューにも入らなかったイベント

デッドレターが守るのは「受け取ったが処理できなかった」だけです。ルーティングキーの綴り違いや購読側の配線漏れでは、イベントはどのキューにも入りません。交換機の予備の行き先（`alternate-exchange`）に流れるので、そちらも見てください。

```bash
kubectl -n cargo exec deploy/rabbitmq -- rabbitmqctl list_queues name messages
```

`cargo.unroutable.queue` に溜まっていれば、**送り先の名前が合っていません**（両側の契約テストが落ちるはずの食い違いです）。

---

## 8. テストの実行

### 全テスト実行

```bash
cd apps/backend

# テスト実行（カバレッジレポート付き）
./gradlew test jacocoTestReport

# または npm スクリプト（プロジェクトルートから）
npm test
```

### テストの種類

テスト形状の設計方針は [テスト戦略](../design/test_strategy.md) を参照してください。

| テスト種別 | ツール | 対象 | 説明 |
| :--- | :--- | :--- | :--- |
| 単体テスト | JUnit 5 + Mockito + AssertJ | バックエンド | ドメインロジック（通関ガード・キャンセル承認・誤配検知・アカウントロック） |
| 統合テスト | Testcontainers（PostgreSQL / RabbitMQ） | バックエンド | 実 DB への SQL・イベント送受信 |
| アーキテクチャテスト | ArchUnit | バックエンド | レイヤー依存・BC 独立性（ACL ポートのみ越境可） |
| 契約テスト | Spring Cloud Contract | サービス間 | マイクロサービス間 API の契約 |
| コンポーネントテスト | Vitest + Testing Library | フロントエンド | React コンポーネント |
| API モックテスト | MSW | フロントエンド | API モックを使用した統合テスト |
| E2E テスト | Playwright | 全体 | kind + Kustomize（`overlays/ci`）上で主要シナリオを検証 |

### テストの規律

以下は過去の失敗から得た規律です。テストを書く際は必ず守ってください。

- **破壊検証**: 安全装置（通関ガード・アカウントロック・楽観的ロック）は「入れたこと」ではなく「働くこと」を検証する。実装を壊すと赤になるテストを対にする
- **分岐結果で判定**: 経過時間のアサートは脆弱な実装に戻しても緑になる。どの検査で落ちたか（分岐の結果）で判定する
- **同じ Clock**: 業務日付を扱うテストは本体と同じ `Clock`（業務タイムゾーン）を注入する。`LocalDate.now()` を直接呼ばない
- **CI（UTC）でも回す**: 日付判定を含むテストは `TZ=UTC` で一度実行して確認する
- **方言差の双方向スモーク**: 全クエリを PostgreSQL と H2 の両方で実行し、解釈できるかを確認する（「本番で緑・ローカルで赤」も同頻度で起きる）
- **本番経路の採番**: テストデータの ID は MAX+1 の自前採番をせず、本番と同じシーケンス経路で採番する

### テスト実行時の注意事項

- Testcontainers 使用時は Docker Desktop が起動している必要があります
- E2E テストは kind クラスタが必要です（`overlays/ci` を適用）。詳細は [E2E テストセットアップ手順書](E2Eテストセットアップ手順書.md) を参照してください

---

## 9. コード品質管理

### 静的コード解析ツール

| ツール | 目的 | コマンド |
| :--- | :--- | :--- |
| Checkstyle | Java コーディング規約 | `npx gulp dev:backend:check` |
| SpotBugs | Java バグパターン検出 | `npx gulp dev:backend:check` |
| ArchUnit | レイヤー依存・BC 独立性 | `npx gulp dev:backend:test` |
| JaCoCo | バックエンドカバレッジ | `npx gulp dev:backend:test` |
| ESLint | フロントエンド静的解析 | `npx gulp dev:frontend:lint` |
| SonarQube | 総合品質分析 | `npx gulp sonar-local:check`（**ホストのポートは 9001**。9000 は IntelliJ IDEA が使うため、IDE を落とさずに走査できるようにしている） |

### 品質チェックの実行

```bash
# タスクランナー経由（推奨）
npx gulp dev:backend:check        # バックエンド品質チェック
npx gulp dev:frontend:lint         # フロントエンド ESLint

# Gradle 直接実行
cd apps/backend
./gradlew checkstyleMain spotbugsMain    # 品質チェックのみ
./gradlew check                          # テスト + 品質チェック
```

> **重要**: Port の追加や ADR の起票を伴う変更では、必ず `./gradlew test`（フル）を実行してください。
> レビューだけでは ArchUnit による構造的検証は代替できません。

### 品質基準

| 指標 | 閾値 | 測定ツール |
| :--- | :--- | :--- |
| テストカバレッジ（ドメイン層） | 90% 以上 | JaCoCo |
| テストカバレッジ（全体） | 80% 以上 | JaCoCo |
| 循環的複雑度 | 10 以下 | SonarQube / Checkstyle |
| コード重複率 | 3% 以下 | SonarQube |
| ArchUnit 違反 | 0 件 | ArchUnit |
| ファイルサイズ | 500 行 | Checkstyle |
| メソッドサイズ | 150 行 | Checkstyle |
| パラメータ数 | 7 | Checkstyle |

### 品質管理設定ファイル

| ファイル | 説明 |
| :--- | :--- |
| `apps/backend/config/checkstyle/checkstyle.xml` | Checkstyle ルール定義 |
| `apps/backend/config/spotbugs/exclude-filter.xml` | SpotBugs 除外フィルター |

---

## 10. 設計ドキュメント生成（JIG / jig-erd）

`docs/design` は「こう設計した」を、JIG と jig-erd の出力は「こう実装されている」を示します。
両者を突き合わせることで、設計と実装の乖離を目視ではなく生成物として検出できます。

> 生成物は `build/` 配下に出力され、Git 管理外です。コミットしません。

### 10.1 JIG（コードからの設計ドキュメント）

Java のバイトコードを解析し、パッケージ構造・ドメインモデル・使用箇所などを HTML で出力します。

```bash
npx gulp dev:jig          # 全サービス分を生成
npx gulp dev:jig:open     # 既定サービス（bookingms）のドキュメントを開く

# Gradle 直接実行
cd apps/backend
./gradlew jigReports              # 全サービス
./gradlew :bookingms:jigReports   # 個別サービス
```

出力先は各サービスの `build/jig/index.html` です（`shared` を含む 8 モジュール分）。

| 出力 | 内容 |
| :--- | :--- |
| `domain.html` | ドメインモデルの一覧と関連 |
| `usecase.html` | ユースケース（サービスメソッド）の一覧 |
| `package.html` | パッケージ関連図 |
| `inbound.html` / `outbound.html` | 入出力（Controller / Repository）の一覧 |
| `insight.html` | メトリクスに基づく気づき |

> 骨格の段階では「SQL が見つかりませんでした」「@Service がない」等の情報メッセージが出ます。
> 実装が進むにつれて出力が充実します。

### 10.2 jig-erd（実スキーマからの ER 図）

Testcontainers で PostgreSQL を起動し、Flyway が構築した**実際のスキーマ**から ER 図を生成します。
`docs/design/data-model.md` の ER 図は設計、jig-erd の出力は実装です。

```bash
npx gulp dev:jig-erd

# Gradle 直接実行
cd apps/backend
./gradlew jigErd              # DB を持つ 6 サービス分
./gradlew :bookingms:jigErd   # 個別サービス
```

出力先は各サービスの `build/jig-erd/` で、SVG が 3 種類（overview / summary / detail）生成されます。

**Database per Service のため、ER 図はサービスごとに分かれます。** サービスをまたぐ参照は
論理参照であり図には現れません。それが設計上正しい姿です（1 枚の全体 ER 図にはなりません）。

| 前提 | 確認コマンド |
| :--- | :--- |
| Docker（Testcontainers 用） | `docker info` |
| Graphviz | `dot -V` — 無ければ `brew install graphviz` |

`SchemaErdDocument` は検証を行わないドキュメント生成であるため、通常の `test` タスクからは
除外しています。`jigErd` タスクでのみ実行されます。

> **注意**: `jig.properties` の `jig.erd.filter.schema.pattern=public` は対象スキーマを
> public に限定する設定であり、Flyway 管理テーブルの除外ではありません。
> `flyway_schema_history` は図に現れます（実測）。

---

## 11. ディレクトリ構造

```text
case-study-cargo-tracker/
├── .husky/                          # Git Hooks (Husky)
│   └── pre-commit                   # コミット前品質チェック
├── apps/
│   ├── backend/                     # バックエンド（Gradle マルチプロジェクト）
│   │   ├── settings.gradle          # Gradle マルチプロジェクト設定
│   │   ├── build.gradle             # 共通設定（品質管理・テスト）
│   │   ├── gradlew, gradlew.bat     # Gradle Wrapper
│   │   ├── config/
│   │   │   ├── checkstyle/checkstyle.xml
│   │   │   └── spotbugs/exclude-filter.xml
│   │   ├── shared/                  # 共有カーネル（Location）
│   │   ├── gatewayms/               # API Gateway
│   │   ├── authms/                  # Auth Service
│   │   ├── bookingms/               # Booking Service（ヘキサゴナル構造）
│   │   │   ├── build.gradle
│   │   │   ├── Dockerfile
│   │   │   └── src/
│   │   │       ├── main/java/com/example/bookingms/
│   │   │       │   ├── interfaces/         # rest/, events/
│   │   │       │   ├── application/        # commandservices/, queryservices/, acl/
│   │   │       │   ├── domain/             # aggregates/, valueobjects/, commands/
│   │   │       │   └── infrastructure/     # repositories/, brokers/, services/
│   │   │       ├── main/resources/
│   │   │       │   ├── application.yml
│   │   │       │   ├── db/migration/       # Flyway マイグレーション
│   │   │       │   └── mapper/             # MyBatis マッパー XML
│   │   │       └── test/
│   │   ├── routingms/               # 以下同様のヘキサゴナル構造
│   │   ├── trackingms/
│   │   ├── handlingms/
│   │   └── billingms/
│   ├── frontend/                    # コンポジット UI（React SPA）
│   │   └── src/
│   │       ├── features/            # auth/booking/routing/tracking/handling/billing
│   │       ├── components/          # ui/, layout/
│   │       ├── stores/              # Zustand
│   │       ├── lib/                 # API クライアント
│   │       └── test/
│   └── www/                         # ドキュメントポータル（配信用）
├── docs/                            # ドキュメント
├── ops/                             # 運用（アプリケーションではないもの）
│   ├── k8s/
│   │   ├── kind-cluster.yaml        # kind クラスタ定義
│   │   └── kustomize/               # base/ + overlays/（local, ci）
│   ├── docker/                      # 開発用コンテナ定義
│   ├── nix/
│   └── scripts/                     # 運用スクリプト（Gulp タスク）
└── package.json                     # Node.js 依存関係（husky, lint-staged, Gulp）
```

---

## 12. 命名規則

| 要素 | 規則 | 例 |
| :--- | :--- | :--- |
| テーブル名 | スネークケース | `cancellation_request` |
| カラム名 | スネークケース | `tracking_number` |
| クラス名 | パスカルケース | `CargoBookingCommandService` |
| フィールド名 | キャメルケース | `trackingNumber` |
| REST エンドポイント | ケバブケース + `/api/v1/` | `/api/v1/bookings/{id}/cancellation` |
| Gradle モジュール名 | サービス名 + `ms` | `bookingms`, `routingms` |
| Java パッケージ | `com.example.{サービス名}` | `com.example.bookingms` |

---

## 13. Git 規約

### コミットメッセージ

[Conventional Commits](https://www.conventionalcommits.org/ja/) に従います。

| タイプ | 説明 |
| :--- | :--- |
| `feat` | 新機能 |
| `fix` | バグ修正 |
| `docs` | ドキュメントのみの変更 |
| `style` | コードの意味に影響しない変更 |
| `refactor` | バグ修正でも機能追加でもないコード変更 |
| `perf` | パフォーマンス改善 |
| `test` | テストの追加・修正 |
| `chore` | ビルドプロセス・補助ツールの変更 |

### スコープ

サブシステムを示すスコープを使用します。

```text
feat(booking): 輸送中キャンセルの承認フローを追加
fix(tracking): 誤配検知が予定ルート外の荷役を拾わない問題を修正
docs: アプリケーション開発環境セットアップ手順書を作成
chore(setup): Gradle マルチプロジェクト基盤を構築
```

### Git Hooks（Husky + lint-staged）

コミット時に自動で品質チェックが実行されます。

`npm install` 実行時に Husky は自動的にセットアップされます（`prepare` スクリプト）。

Java ソースファイル（`apps/**/src/**/*.java`）に変更がある場合、Checkstyle・SpotBugs・テストが自動実行されます。いずれかが失敗するとコミットがブロックされます。

緊急時にフックをスキップする場合（非推奨）：

```bash
git commit --no-verify -m "メッセージ"
```

> **Warning**: フックのスキップは緊急時のみ使用してください。

---

## 14. セットアップの確認

```bash
# 1. Node.js 依存パッケージのインストール
npm install

# 2. バックエンドビルド確認
npx gulp dev:backend:build

# 3. バックエンドテスト実行
npx gulp dev:backend:test

# 4. バックエンド品質チェック
npx gulp dev:backend:check

# 5. フロントエンドビルド確認
npx gulp dev:frontend:build

# 6. フロントエンドテスト実行
npx gulp dev:frontend:test

# 7. バックエンド開発サーバー起動
npx gulp dev:backend

# 8. フロントエンド開発サーバー起動（別ターミナル）
npx gulp dev:frontend

# 9. 設計ドキュメント生成（JIG）
npx gulp dev:jig

# タスク一覧の確認
npx gulp dev:help
```

---

## 15. CI/CD

### ワークフロー一覧（予定）

| ワークフロー | ファイル | トリガー | 説明 |
| :--- | :--- | :--- | :--- |
| Backend CI | `.github/workflows/backend-ci.yml` | `apps/backend/**` 変更時 | 品質チェック・テスト・ビルド |
| Frontend CI | `.github/workflows/frontend-ci.yml` | `apps/frontend/**` 変更時 | Lint・テスト・ビルド |
| E2E | `.github/workflows/e2e.yml` | main / PR | kind + Kustomize で起動し Playwright 実行 |
| Docker Publish | `.github/workflows/docker-publish.yml` | タグ push / 手動実行 | イメージを Registry に公開 |
| Docs Deploy | `.github/workflows/docs.yml` | main ブランチ push | ドキュメントをデプロイ |

> CI はサービス単位のパスフィルタで、変更のあったマイクロサービスのみビルド・デプロイします（モノレポでの独立デプロイ）。
> セキュリティ走査は導入失敗と検出が区別できるよう、公式イメージの直接実行とします。

---

## トラブルシューティング

### Gradle ビルドが失敗する

**問題**: Java バージョンが一致しない

```text
Unsupported class file major version 69
```

**解決策**: Java 25 がインストールされていることを確認

```bash
java -version
sdk use java 25-tem
```

### Gradle Wrapper が見つからない

**問題**: `gradlew: No such file or directory`

**解決策**: `apps/backend/` ディレクトリに移動してから実行

```bash
cd apps/backend
./gradlew build
```

### ポートが既に使用されている

**問題**: サービス起動時に `Port already in use` エラー

**解決策**: 使用中のプロセスを確認して停止

```bash
lsof -i :8082
kill -9 <PID>
```

### kind クラスタで Pod が Running にならない

```bash
kubectl -n cargo describe pod <Pod 名>
kubectl -n cargo logs <Pod 名>
```

| 症状 | 原因 | 対処 |
| :--- | :--- | :--- |
| `ImagePullBackOff` | イメージ未ロード | `kind load docker-image cargo-<svc>:0.0.1 --name cargo` |
| `CrashLoopBackOff`（DB 接続エラー） | postgres が未起動 | `readinessProbe` で自動復帰。数分待つ |
| Ingress にアクセスできない | Ingress Controller 未導入 | 7.2 の NGINX Ingress 導入手順を実行 |
| 正常起動しているのに再起動を繰り返す | exec probe の既定タイムアウト（1 秒）に診断コマンドが間に合わない | probe に `timeoutSeconds` を明示する（postgres / rabbitmq は設定済み） |

> **probe の注意**: ログ上は `Server startup complete` でも、liveness probe がタイムアウトすると
> 健全なコンテナが再起動ループに入ります。`kubectl describe pod` の Events で
> `Liveness probe failed: command timed out` が出ていないか確認してください。

### H2 Console に接続できない

`application.yml` で H2 Console が有効になっていることを確認します。

| 項目 | 値 |
| :--- | :--- |
| JDBC URL | `jdbc:h2:mem:booking_db` |
| User Name | `sa` |
| Password | （空欄） |

### pre-commit フックが失敗する場合

```bash
cd apps/backend
./gradlew checkstyleMain spotbugsMain
# エラーを修正してから再度コミット
```

---

## 関連ドキュメント

- [技術スタック選定](../design/tech_stack.md)
- [バックエンドアーキテクチャ](../design/architecture_backend.md)
- [フロントエンドアーキテクチャ](../design/architecture_frontend.md)
- [インフラストラクチャアーキテクチャ](../design/architecture_infrastructure.md)
- [テスト戦略](../design/test_strategy.md)
- [開発環境セットアップ手順書](開発環境セットアップ手順書.md)
