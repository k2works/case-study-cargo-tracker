# 2026-06-26 開発ジャーナル

Haskell 版 Cargo Tracker の Sprint 0 (分析フェーズ) を完走し、続けて **運用基盤** (環境構築 → 開発スクリプト → 監査 → CI) を Skills の組み合わせで一気通貫に整備した。

## ハイライト

- **分析フェーズ Sprint 0 完了**: ADR 起票 → ドメインモデル等 8 設計ドキュメント → マルチパースペクティブレビュー → Sprint 0 指摘 **42 件全件対応** → リリース計画標準化までを `orchestrating-analysis` 経由の連鎖で完走
- **Skills の連鎖で運用基盤を完成**: `operating-setup` → `operating-script` → `validating-operating-setup` (新規) → `operating-cicd` の流れで、スタブ実装にもかかわらず CI まで動く土台を構築
- **`validating-operating-setup` スキルを新規作成**: 「手順書を作っただけで実施が放置される」問題を防ぐため、`skill-creator` を使って監査スキルを新規追加
- **CI を 3 回の修正で安定化**: Nix ベースで lts-24 / GHC 9.10.2 のフル CI (Lint + Format + Build + Test + Security) が成功

## 分析フェーズ Skill 実行の時系列

### A1. `creating-adr` — ADR 0001 / 0002 起票

| ADR | 決定 | 主要な代替案比較 |
| :--- | :--- | :--- |
| `docs/adr/0001-haskell-servant-stack.md` | Haskell + Servant + Warp + Lucid + postgresql-simple + dbmate | Servant / Yesod / Scotty / IHP の 4 つを比較 |
| `docs/adr/0002-arch-check-implementation.md` | 自作 arch-check (HLint + AST ハイブリッド) を 4 フェーズで段階導入 | weeder のみ・カスタムリンタ・haskell-src-exts 単体 |

### A2. `orchestrating-analysis` — 分析フェーズ全体の連鎖実行

Scala 版 (`tmp/case-study-cargo-tracker/docs/design/`) を参考に Haskell 版を導出。Ralph Loop で長時間自律実行し、以下を生成:

| ドキュメント | 内容 |
| :--- | :--- |
| `docs/design/architecture_backend.md` | DDD + Hexagonal + CQRS、8 BC × 7 集約ルート、トランザクション境界 T-01/T-02/T-03 |
| `docs/design/architecture_frontend.md` | Lucid SSR + htmx、CsrfToken newtype 型安全パターン |
| `docs/design/architecture_infrastructure.md` | AWS ECS Fargate + RDS PostgreSQL 16、256 CPU / 512 MB スタート |
| `docs/design/domain-model.md` | 8 BC、7 集約ルート、DiscountPolicy 等の sum type 表現 |
| `docs/design/data-model.md` | 20 テーブル、hedgehog ジェネレータ規約、Circuit Breaker 境界 |
| `docs/design/ui_design.md` | 24 画面、Leaflet 追跡画面、aria-live / breadcrumb 規約 |
| `docs/design/tech_stack.md` | mtl 利用制限、GHC コンパイラフラグ強制 |
| `docs/design/test_strategy.md` | BDD 形式 §3.5、境界値 29 件 §3.6、k6 スモーク §9.1 |
| `docs/design/non_functional.md` | 業務ルール定数 (割引 30%、パスワード 12 文字、セッション 30 分) |
| `docs/design/operation.md` | 監視・バックアップ・障害対応・変更管理 |

ユーザーストーリーは 27 件に整理 (US26 通知 / US27 通関を追加、US08 を 08a/08b に分割、US13 キャンセル料 3 段階)。

### A3. `analyzing-review` — マルチパースペクティブレビュー

5 つの XP エージェント (`xp-product-manager` / `xp-architect` / `xp-interaction-designer` / `xp-tester` / `xp-user-representative`) を並列起動し、Sprint 0 指摘を統合。

成果物: `docs/review/analysis_multi_perspective_review_20260626.md`

| 重要度 | 件数 | 主な内容 |
| :--- | ---: | :--- |
| 高 | 16 | 集約数齟齬 (5 vs 7)、トランザクション境界未定義、ADR で servant-auth-server 代替案不足 |
| 中 | 16 | DiscountPolicy 表現、境界値表、hedgehog 規約、Leaflet マップ、aria-live 規約 |
| 低 | 10 | breadcrumb 仕様、コミットメッセージ規律強化、命名規則の具体例追加 |

### A4. Sprint 0 指摘 42 件の全件対応

ユーザーから「高 → 中 → 低」と順次依頼があり全件対応。

| バッチ | 主な対応 |
| :--- | :--- |
| 高 16 件 | architecture_backend.md の集約数を 7 に修正、トランザクション境界規約 T-01/T-02/T-03 追加、CsrfToken の type-safe パターン化、ADR 0001 への servant-auth-server 代替セクション追加 |
| 中 16 件 | 境界値テーブル追加 (`test_strategy.md` §3.6)、Hedgehog ジェネレータ規約 §3.7、k6 スモークテスト §9.1、業務ルール定数の `non_functional.md` §9 化、aria-live 規約、Leaflet 地図実装方針 |
| 低 10 件 | breadcrumb 仕様、mtl 利用制限の明文化 (`tech_stack.md` L-03)、GHC コンパイラフラグ強制 (L-04)、命名規則の具体例追加 |

### A5. `planning-releases 標準フォーマットを適用する`

`docs/development/release_plan.md` をテンプレート準拠に再生成。

- 4 段階リリース戦略 (0.1 Internal Alpha → 0.2 → 1.0 MVP → 2.0 GA)
- 学習コスト係数 1.20 (Scala 1.15 比)
- 計算済み + 実績の 2 系統 Gantt、バーンダウン、9 イテレーション詳細、13 リスク、進捗追跡セクション

### A6. PlantUML エラー修正

レビューの過程で 2 つの図が壊れていたため修正:

| ファイル | 問題 | 対処 |
| :--- | :--- | :--- |
| `docs/design/ui_design.md` 画面遷移図 | composite state 内でブロックごとにダッシュボードが複製される構文エラー | フラットなコメント区切り構造に書き換え |
| `docs/design/data-model.md` 概念データモデル | skinparam 1 行記述の構文エラー、関係性方向逆転 (cargo→shipper)、cross-cutting パッケージの孤立 | `skinparam linetype ortho` + `hide circle`、フルネーム化、カーディナリティ向き修正 |

その他の図も確認したが破損なし。

### A7. `operating-docs --update`

`docs/index.md` / `mkdocs.yml` / 各サブディレクトリの `index.md` をドキュメント構成に同期。

## 運用フェーズ Skill 実行の時系列

### 1. `operating-setup` — Haskell スキャフォルディング作成

手順書 `docs/operation/アプリケーション開発環境セットアップ手順書.md` (§1〜§7) に従い `apps/cargo-tracker/` を生成。

主要成果物:

| ファイル | 内容 |
| :--- | :--- |
| `apps/cargo-tracker/package.yaml` | hpack 仕様、ADR 0002 のコンパイラフラグ |
| `apps/cargo-tracker/stack.yaml` | lts-24.0 (GHC 9.10) |
| `apps/cargo-tracker/app/Main.hs` | Warp 起動の最小 JSON ヘルスチェック |
| `apps/cargo-tracker/docker-compose.yml` | postgres + adminer + mailhog |
| `apps/cargo-tracker/.hlint.yaml` / `fourmolu.yaml` / `arch-check.yaml` | 品質ツール設定 |

途中で macOS の BSD `ar` が GHC 9.8 の `@xxx.rsp` を扱えない既知問題に遭遇し、**`lts-23` → `lts-24` (GHC 9.8 → 9.10)** に切替えて解決した。`Main.hs` が import する `wai` を依存に追加してビルド成功。

### 2. `operating-script` — Gulp タスク `develop.js` の作成

`ops/scripts/develop.js` を新規作成し、`gulpfile.js` から読み込み。

| グループ | 主要タスク |
| :--- | :--- |
| DB | `dev:db:start` / `:stop` / `:logs` / `:clean` / `:migrate` / `:rollback` |
| ビルド・実行 | `dev:setup` / `dev:build` / `dev:run` / `dev:ghci` |
| テスト | `dev:test` / `dev:test:coverage` / `tdd:cargo-tracker` |
| 品質 | `dev:lint` / `dev:format` / `dev:format:check` / `dev:arch-check` |
| 一括 | `dev:bootstrap` (初回) / `dev:verify` (CI 相当) |

その後 `npm start` で **アプリ + ドキュメント + ブラウザ自動オープン**まで一発で立ち上がる構成に発展させた。`execSync` が Node イベントループをブロックして並列タスクが動かない問題を `spawn` への変更で解決。MkDocs daemon の起動 (約 90 秒) を可視化するため進捗ドット出力を追加し、「フリーズに見える」問題を防止した。

### 3. `validating-operating-setup` — スキル新規作成

手順書のステップ実施状況を **章別チェックリスト → サマリ → A/B/C/D 分類 → 着手提案** の順で監査する新スキルを `skill-creator` で作成。

`.claude/skills/validating-operating-setup/SKILL.md` (118 行)

設計判断:

- 証拠列を必須化 (`OK` だけでなくバージョン番号 / パス / 検出結果を残す)
- **A 今すぐ / B 実装と一体 / C 任意 / D 別フェーズ** の 4 バケツに分けて、ユーザーが短い決断で次へ進めるようにした
- 「新メンバーがこの手順書だけで進めて詰まるか?」を判定の迷い解消の基準に
- 手順書の改変はスコープ外 (`operating-docs` に委譲)

別の手順書 (`開発環境セットアップ手順書.md`) でテスト実施したところ、リモート/ローカルの判別まで自律的に補完できることを確認 (定性的に検証成功)。

### 4. 監査 → 「A 全部やる」で運用補強を一括実施

`validating-operating-setup` で `アプリケーション開発環境セットアップ手順書.md` を監査した結果:

- ✅ 16 / ⚠️ 8 / ❌ 17

`A 全部やって` の一言で以下を即時対応:

| 項目 | 対応 |
| :--- | :--- |
| dbmate | `brew install dbmate` |
| husky + lint-staged | `npm i -D` + `npx husky init` |
| npm scripts 整備 | `start:dev` / `test:tdd` / `lint` / `format` / `check` / `build` / `db:*` の 12 件 |
| `.husky/pre-commit` | Haskell 変更時のみ fourmolu → hlint → stack test (Domain) を実行 |
| `weeder.toml` | 雛形配置 |

### 5. `operating-cicd` — Nix ベース CI 構築

`.github/workflows/ci.yml` を新規作成。`ops/nix/environments/haskell/shell.nix` に `hlint` / `fourmolu` / `weeder` / `ghcid` / `dbmate` / `postgresql` を追加して **ローカルと CI で同じツールセット** を使用。

ジョブ構成:

| ジョブ | 役割 |
| :--- | :--- |
| quick-checks | fourmolu (format check) + HLint |
| build-and-test | stack build + stack test --coverage |
| security | GitHub Dependency Review (PR 時) |
| ci-success | 結果集約 |

並列実行 + Stack キャッシュ 2 層 (`~/.stack` と `.stack-work`) で 2 回目以降のフィードバックを短縮する想定。

### 6. CI を 3 回の修正で安定化

| 試行 | 問題 | 修正 |
| :--- | :--- | :--- |
| 1 回目 | fourmolu --mode check で既存ソースが違反 | `fourmolu --mode inplace` で整形 (`a2ac9ec3`) |
| 2 回目 | stack build --pedantic が `-Werror=unused-packages` で失敗 | スタブ期間中は `--pedantic` を外す (`f1a6d50b`) |
| 3 回目 | `.hlint.yaml` の YAML 構文不正、かつ `within` セマンティクスの誤解で Main.hs を巻き込む | アーキテクチャルールを撤回し IT1 の arch-check に委譲 (`27525bb2`) |

最終的に全 4 ジョブが ✅ で安定。

## 学び

- **Scala 版を「参考」にして Haskell 版を「導出」する流れは効率的だが、忠実に写すと表現ミスマッチが発生する**: 例として「sum type で割引ポリシーを表現」「型クラスポートの境界」など、Haskell に翻訳する過程で得られた知見が中優先度指摘の半分以上を占めた。最初から純 Haskell ネイティブで設計し直せば回避できたかもしれない
- **マルチパースペクティブレビューは Sprint 0 の安全網として極めて有効**: 5 エージェントの並列レビューで 42 件指摘が発見でき、ユーザー単独レビューでは見落とされていたであろう「集約数が backend.md と domain-model.md で齟齬」「トランザクション境界が未定義」が拾えた
- **PlantUML の composite state は罠**: composite 内で同名要素を参照すると複製される。ER 図の概念モデルでも skinparam の一行記述やショートエイリアスは避け、フルネーム + `skinparam linetype ortho` が安全
- **Skills の役割分担** が想定通り機能した: 設定生成系 (`operating-*`) と監査系 (`validating-*`) を分けることで、各タスクの責務が明確化された
- **「手順書 vs 現状」のギャップ可視化** が IT 着手前のチェックリスト代わりになる。これは `tracking-progress` や `validating-iteration-plan` と同じ「ドキュメントを腐らせない」型のスキル
- **HLint の `within` は直感に反する** (ホワイトリスト) ので、依存方向の制約は HLint ではなく **arch-check 等の専用ツール** に乗せた方が安全
- **macOS の BSD `ar` + GHC 9.8 の組み合わせは地雷**。GHC 9.10 (lts-24+) に上げるだけで回避可能
- **`stack build --pedantic` はスタブ期間中は不適合**: 未使用 dep が大量に発生するため。IT1 で全 dep が使われるようになってから再有効化する

## 完了した運用基盤

```
✅ 環境構築 (apps/cargo-tracker/)
✅ 開発タスク (Gulp / npm scripts)
✅ pre-commit 品質ゲート
✅ 手順書監査スキル
✅ CI (Lint + Format + Build + Test + Security)
⏳ ステージング/本番 CD (AWS 構築後)
⏳ Terraform プロビジョニング (operating-provision 待ち)
```

## 主要コミット

### 分析フェーズ

| ハッシュ | コミット |
| :--- | :--- |
| `e8677b1f` | docs(adr): ADR 0001 Haskell + Servant スタックを起票 |
| `db51b33e` | docs(adr): ADR 0002 arch-check 実装方針を起票 |
| `80ef38c4` | docs(design): バックエンドアーキテクチャ等 8 設計ドキュメントを追加 |
| `911e38eb` | docs(review): Sprint 0 マルチパースペクティブレビュー結果を追加 |
| `2b167ec3` | fix(design): Sprint 0 高優先度指摘 16 件に対応 |
| `1a2bcc96` | fix(design): Sprint 0 中優先度指摘 16 件に対応 |
| `3ff9228e` | fix(design): Sprint 0 低優先度指摘 10 件に対応 |
| `4bea9e81` | docs(release): リリース計画に標準フォーマットを適用 |
| `b88d505b` | fix(design): ui_design.md と data-model.md の壊れた PlantUML を修正 |
| `2f03a948` | docs(operation): アプリケーション開発環境セットアップ手順書を作成 |
| `f5f4100b` | docs(operation): 開発環境セットアップ手順書を作成 |
| `55ad87b7` | docs(operation): AWS ステージング環境セットアップ手順書を作成 |
| `a069cf84` | docs(operation): 環境セットアップ手順書 段階 2-4 を作成 |

### 運用フェーズ

| ハッシュ | コミット |
| :--- | :--- |
| `a23a6028` | feat(scaffolding): apps/cargo-tracker/ に Haskell プロジェクトの初期構成を作成 |
| `27391e95` | feat(ops): Haskell アプリ開発用の Gulp タスクを追加 |
| `73e9caf6` | feat(ops): npm start でアプリとドキュメントを同時起動しブラウザを自動オープン |
| `194fc125` | chore(deps): husky と lint-staged を追加し npm scripts を整備 |
| `e018d6cd` | chore(ops): pre-commit フックと weeder.toml を追加 |
| `59c5e4b2` | chore(skills): validating-operating-setup スキルを追加 |
| `329d0349` | chore(nix): Haskell シェルに品質ツールと dbmate を追加 |
| `10ed1fdb` | ci: Nix ベースの CI ワークフローを追加 |
| `27525bb2` | fix(hlint): アーキテクチャルールを撤回 (IT1 の arch-check に委譲) |
