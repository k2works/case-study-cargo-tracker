# 2026-07-07 開発ジャーナル

IT8 の計画セッション。プロジェクト状態の確認から始め、IT8 イテレーション計画の作成 → 整合性検証（4 エージェント並列）→ 検証指摘の反映 → 設計セクションの IT7 同等レベルへの拡充までを 1 セッションで完了した。検証で「Settlement BC 新設案が既存 Billing Context と衝突」という計画の根本欠陥を開発着手前に検知・修正できたのが最大の成果。

## ハイライト

- **IT8 計画を作成** (`iteration_plan-8.md`、22 SP): US23 精算 + IT7 繰越高優先 T7-A〜T7-D + 保証系 T7-E〜T7-I + Release 2.0 GA クロージング + ストレッチ US10/US12
- **validating-iteration-plan が初版の BC 設計欠陥を検知**: 初版は US23 向けに「Settlement BC 新設」を提案していたが、上流には **Billing Context（Invoice 集約 + PaymentStatus）が定義済み**だった。設計ドキュメントを正として全面修正
- **架空のロール名を実コードで検証して修正**: 計画初版の「RoleGate (Accounting/Admin)」は実在せず、実コード `Shared/Auth/Domain/User.hs` の `Role` sum type は `Accountant`/`MasterAdmin` だった
- **設計セクションを IT7 と同レベルに拡充** (+479 行): モジュール構造 / URL 設計 / salt 3 画面 / Servant API 型 / シーケンス 2 本 / Tx 境界 / BillingError / DDL / テスト戦略 / CI / ADR-0016 骨子

## Skill 実行の時系列

### 1. `orchestrating-project --status` — プロジェクト状態サマリー

release_plan.md・iteration_report-7.md・GitHub Milestone を突合し、IT1〜IT7 完了（累計 179+ SP / 計画対比 229%）、Release 2.0 GA Milestone に open 3 Issue（#255/#242/#244）、IT8 未着手・計画未作成という状態を確認。次アクションとして `planning-releases --iteration 8` を提示。

### 2. `planning-releases --iteration 8` — IT8 計画作成

**入力**: retrospective-7.md の Try（T7-A〜T7-N、IT8 目標 SP 20-25 の示唆）、release_plan.md の IT8 スコープ（US23 + ストレッチ US10/US12）、user_story.md、GitHub open Issue。

**出力**:

| 成果物 | 内容 |
| :--- | :--- |
| `docs/development/iteration_plan-8.md` | 22 SP（本体 3 + 繰越高優先 5 + 保証系 7 + GA クロージング 2 + ストレッチ 5） |
| `release_plan.md` | IT8 行を「計画済」に更新 |
| `docs/index.md` / `docs/development/index.md` / `mkdocs.yml` | インデックス追加 |

Ralph Loop 運用を想定し、retrospective-7 の学び（1 週目本体 / 2 週目保証系、Docker 依存タスク到達で end-of-life 早期判定）を計画に明記した。コミット `d880beb7`。

### 3. `validating-iteration-plan` — 8 ステップ整合性検証

ステップ 2〜6・8 を Explore エージェント 4 体で並列検証し、ステップ 1・7 は自前で確認。結果: **NG 5 ステップ / 19 件の不整合**を検出。

**最重要の発見（ステップ 3〜5 が同じ根本原因に収斂）**:

- 計画初版の「Settlement BC 新設 + settlements テーブル (uuid PK, amount numeric) + /settlements パス」は、上流 3 ドキュメントすべてと衝突
- 正: domain-model.md §6 **Billing Context**（Invoice 集約、PaymentStatus = Pending/Confirmed/Overdue/Refunded、Payment は独立集約にしない Scala 版 ADR 0019 同方針）、data-model.md **invoice/invoice_line_item**（単数形・BIGSERIAL・金額 BIGINT 最小通貨単位・version 楽観ロック・BC 間 FK なし）、ui_design.md **/billing/invoices 系 5 画面**

**その他の指摘と反映**:

| 指摘 | 反映 |
| :--- | :--- |
| 対応 UC 番号未記載（US23/US10/US12） | UC18/UC08/UC10 を追記 |
| US12 受入基準の「料金概算」欠落 | 復元 |
| タスク 4.3「Settlement を追記」が抽象的（IT6/IT7 は画面名まで明記する慣行） | salt ワイヤーフレーム 3 種 + 遷移図統合を明記、見積 2h→3h |
| ステップ 8（過去レビュー）は指摘 16 件中、高優先 4 件 100% 反映・未反映は全て backlog 化明記済 | 修正不要 |

修正コミット `7d59dbbb`。

### 4. 設計セクションの拡充（ユーザー指示「iteration_plan-7 と同じレベルで」）

iteration_plan-7 の設計セクション構成（モジュール構造 → URL → UI → API → シーケンス → Tx 境界 → エラー処理 → DDL → テスト戦略 → CI → ADR 骨子）を踏襲し、Billing Context 版を書き下ろした。domain-model.md §6 の集約フィールド・ビジネスルール（Delivered 後のみ発行可、法人割引最大 30%、Money は Integer 最小通貨単位 + HALF_UP）、data-model.md の DDL、実コードの `Role`・migration 命名（`20261012100000_create_invoice.sql`）に基づく。コミット `26d777df`。

## 学び

- **計画は「新規に設計」する前に上流を grep する**: US23 は「精算」という言葉から Settlement BC を新設しそうになったが、上流では Billing Context として設計済みだった。ストーリー → BC 対応は domain-model.md のコンテキスト一覧（§冒頭の表）を最初に引くべき。同じ概念が「精算/Billing/Invoice」と複数の名前を持つとき、既存名が正
- **validating-iteration-plan は計画作成の直後に必ず実施する価値がある**: 今回 19 件の不整合（uuid vs BIGSERIAL、numeric vs BIGINT、複数形 vs 単数形、架空ロール名）は、実装後に発覚したら migration やり直し + 全レイヤ改名になっていた。検証コストはエージェント 4 体・数分
- **ロール名・パス・型名は計画段階でも実コードを 1 回 grep して裏取りする**: 「Accounting/Admin」のような もっともらしい捏造は、レビューでも通過しやすい。`data Role` の定義 15 行を読むだけで防げた
- **検証エージェントには「設計ドキュメントが正」という判定基準を明示的に与える**: どちらに合わせるかを曖昧にすると、指摘が「どちらかに統一せよ」で止まり、修正方針の判断が往復する
- **IT 計画の設計セクションの粒度はプロジェクト慣行（直前 IT）が基準**: テンプレートは最小構成しか定めないため、IT7 が確立した「シーケンス図 + Tx 境界表 + エラー型 + DDL + CI ステップ」の水準を維持しないと、Ralph Loop の反復が設計判断で停滞する

## 主要コミット

| ハッシュ | コミット |
| :--- | :--- |
| `d880beb7` | docs(iteration_plan-8): IT8 計画を作成 (US23 精算 + IT7 繰越 T7-A〜T7-I + Release 2.0 GA クロージング = 22 SP) |
| `7d59dbbb` | docs(iteration_plan-8): 整合性検証の指摘を反映 (Settlement BC 案を既存 Billing Context/Invoice 集約・data-model/ui_design 準拠に修正) |
| `26d777df` | docs(iteration_plan-8): 設計セクションを iteration_plan-7 と同レベルに拡充 (モジュール構造/URL/UI/Servant API/シーケンス/Tx 境界/エラー処理/DDL/テスト戦略/CI/ADR-0016 骨子) |
