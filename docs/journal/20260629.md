# 2026-06-29 開発ジャーナル

IT2 クロージング後の最初の計画フェーズ。`orchestrating-project --iteration 3` で IT3 計画を策定し、`validating-iteration-plan` で 8 ステップ整合性検証を実施、検出された 21 件の不整合を全件修正してから GitHub に同期した。IT2 と同等水準まで設計セクションを拡張する追加リクエストにも対応し、最終的に 956 行 / 29 SP の IT3 計画が完成。

## ハイライト

- **IT3 計画 (29 SP / 10 営業日)** を策定。本体 US07/US08a/US27 (11 SP) + IT2 繰越 U-01〜U-10 (12 SP) + レビュー高優先 H-01/H-02/H-03/H-07/H-09 (2 SP) + ストレッチ U-11〜U-14 (4 SP)
- **validating-iteration-plan で 21 件の不整合を検出** (うち 4 件が重大): US27 の HS コード桁数誤り (4 → 6-10)、`customs_info` 新規テーブル提案 vs 既存 `customs_declaration` 重複、PK/FK 規約違反、UI セクション欠落
- **設計セクションを IT2 同等水準 (844 行) を上回る 956 行に拡張**: ドメインモデル Haskell 型定義、3 ユースケースのシーケンス図、トランザクション境界 T-01〜T-03、エラー処理戦略 (Booking.Error 分離)、DB マイグレーション、テスト戦略、CI 統合 (yaml 抜粋)、画面遷移 + htmx パターン表
- **GitHub 同期**: Milestone #24 期限 2026-08-30 設定、Issue #238/#239/#259 本文を整合性検証反映に更新、IT3 トラッカー Issue #271 を新規作成 (30 項目チェックリスト)
- **ADR-0005 起票決定** (Booking 固有エラー分離、H-07 対応) — ADR-0003 (arch-check AST) / ADR-0004 (ShipperRef VO) と合わせて IT3 で 3 件起票予定

## Skill 実行の時系列

### 1. `orchestrating-project --iteration 3` → `planning-releases --iteration 3` (前セッション継続)

前セッションで作成した `docs/development/iteration_plan-3.md` (初版 529 行) と `release_plan.md` (IT3 行 27 SP) を確認。本セッションは "then sync to github" の追加指示から再開。

### 2. `validating-iteration-plan` — 8 ステップ整合性検証

検証対象: iteration_plan-3.md、user_story.md、domain-model.md、data-model.md、ui_design.md、it2_code_review_20260627.md、テンプレート。

| ステップ | 結果 | 主な不整合 |
| :--- | :--- | :--- |
| 1 テンプレート | NG (1) | UI セクション (View/Model/Interaction) 欠落 |
| 2 ストーリー | NG (4) | **US27: HS 4 桁 → 6-10 桁誤り**、申告ステータス欠落、US07 貨物種別欠落、US08a rank=0 欠落 |
| 3 ドメインモデル | NG (2) | `Voyage` を独立 BC で表記 (正は Routing)、`RouteCandidate` を Routing 配置 (正は Estimation) |
| 4 データモデル | NG (4 / **重大**) | **`customs_info` 新規 vs 既存 `customs_declaration` 重複**、PK `uuid` vs 規約 `BIGSERIAL`、FK 規約違反、`updated_at` 欠落 |
| 5 UI ビュー | NG (2) | `/voyages/search` が画面一覧未定義、`/bookings/:id/routes` 既存画面との関係不明 |
| 6 UI 遷移 | NG (1) | 画面遷移図 / htmx / フィードバック規約すべて欠落 |
| 7 ゴール整合 | NG (2 警告) | 27 SP は IT2 実績 18 SP の 1.5 倍、release_plan 原案 22 SP からのスコープ差分根拠なし |
| 8 過去レビュー | NG (5) | H-01/H-02/H-03/H-07/H-09 が未反映 |

### 3. 不整合修正 (TaskCreate × 8 で進捗管理)

すべての NG を計画ドキュメント側で修正:

- US27 受入条件を user_story.md 準拠 (HS 6-10 桁・申告ステータス・通関業者名)
- データモデルを `customs_declaration` 拡張に変更し DDL (ALTER TABLE) + CHECK 制約を記述
- ドメインモデルの BC 配置を正準化、`RouteCandidate` を Estimation Context に移動
- UI セクション新規追加 (ワイヤフレーム × 2 + クラス図 + 画面遷移 + htmx パターン)
- レビュー高優先 5 件をタスク §7 として起こし +2 SP / 9h を追加 → 合計 27 → **29 SP**、コミット必達 25 SP
- スコープ差分根拠 (US08b/US09/US11/US13 を IT4 繰越) とストレッチ枠区分を明記
- ADR-0005 (Booking.Domain.Error 分離) を追加

### 4. ユーザー指示 "設計トピックは iteration_plan-2.md と同等のレベルで" → 設計セクション大規模拡張

IT2 計画 (844 行) の §設計を読み込み、IT3 を同等水準に拡張:

| サブセクション | IT3 拡張後 |
| :--- | :--- |
| ドメインモデル | Haskell 型定義 4 集約 (VoyageSearchCriteria / RouteCandidate / RouteFinder / HsCode / CustomsDeclaration / DeclarationStatus) を追加 |
| データモデル DDL | `ALTER TABLE customs_declaration` + CHECK + index を SQL で記述 |
| モジュール構造 | 3 BC × Domain/App/Infra/Interfaces のフル展開 + arch-check/PhaseAST 構造 |
| URL 設計 | 4 エンドポイントを表に |
| アプリケーション層シーケンス | US07/US08a/US27 の 3 シーケンス図 (PlantUML 各 25-30 行) |
| トランザクション境界 | T-01〜T-03 の適用箇所表 + Haskell コード例 |
| エラー処理戦略 | `Shared.DomainError` (Routing/Estimation 共通) と `Booking.Error` (H-07 分離) の 2 ファイル分解 + HTTP マッピング表 |
| DB マイグレーション順序 | 011 / dbmate 命名規約 |
| テスト戦略 | 層別件数表 + hedgehog プロパティ例 (prop_directVoyageIsRankZero) |
| CI 統合 | yaml 抜粋 4 ステップ (arch Phase 2/3 + testcontainers + criterion + HPC per-module) |
| 画面遷移 + htmx | PlantUML 8 state 詳細化 + htmx 4 パターン表 |

最終: **956 行 (IT2 の 844 行を上回る)**。

### 5. `syncing-github-project --sync`

既存状態を確認:

- Project #31 `CargoTracker haskell/take-1` 既存 (PVT_kwHOADFh2M4BbtdO)
- Milestone #24 `[haskell/take-1] Release 0.2` 既存 (期限未設定)
- Issue #238 (US07) / #239 (US08a) / #259 (US27) すべて既存・正しいラベル付与済

同期アクション:

- Milestone #24 に期限 **2026-08-30** + 詳細説明をセット
- Issue #238/#239/#259 本文を **整合性検証反映後の受入基準** に更新 (Gherkin 形式、HS 6-10 桁、rank=0、性能ゲートなど)
- **Issue #271 IT3 トラッカー新規作成** — 本体 3 + 繰越 U-01〜U-10 + レビュー H-01/H-02/H-03/H-07/H-09 + ストレッチ U-11〜U-14 + Phase 3 + ADR-0003/4/5 のチェックボックス計 30 項目
- Project #31 に Issue #271 を追加

### 6. `tracking-progress --iteration 3`

開始まで残り 35 日 (2026-06-29 → 2026-08-03)。0% 進捗で計画策定済み状態を確認。

## 学び

- **整合性検証は計画作成直後に必ず実施する**: HS コード 4 桁 vs 6-10 桁の誤り、`customs_info` vs `customs_declaration` の重複は実装着手後に発覚すれば DB マイグレーション + UI + Domain 全層の手戻りになる。21 件すべてを開始前に検出できたのは大きな投資効果
- **「同等水準で」の依頼には先行イテレーション計画のサブセクション網羅性を物差しにする**: IT2 計画の `^### ` を grep で抽出し、IT3 計画と差分を取って欠落 (シーケンス図 / トランザクション境界 / エラー処理戦略 / CI 統合) を機械的に拾えた
- **過剰計画の警告は必須**: 29 SP / IT2 実績 18 SP の 1.6 倍。**ストレッチ枠 4 SP を明示区分** + 「コミット必達 25 SP」を別表記することで、消化遅れ時の判断 (何を IT4 へ繰越すか) が事前に決まる
- **GitHub Issue 本文は「リンクのみ」より「Gherkin 受入基準」を直書きするほうが PR レビュー時に効率が良い**: 旧版の Issue は `user_story.md` への単純リンクのみで、HS 桁数誤りに気付けない構造だった。受入基準を本文に直書きすることで、Issue を見るだけで仕様の最新版が分かる
- **計画ドキュメントを Single Source of Truth とし、GitHub は同期先**: skill の運用方針に従い、Issue を直接編集せず計画ドキュメント側を修正してから sync を回す原則を堅持。今回は計画 → Issue の片方向同期で破綻なし
- **IT3 トラッカー Issue (#271) は IT2 まで存在しなかった新パターン**: 本体ストーリー Issue とは別に、ストーリー外のタスク (U-XX 繰越、H-XX レビュー対応、ADR 起票) を 1 つの Issue でチェックリスト化することで、Milestone レベルで「何が残っているか」が即座に見える

## 主要コミット

| ハッシュ | コミット |
| :--- | :--- |
| (未コミット) | docs(it3): iteration_plan-3.md 新規 + release_plan.md IT3 行を 29 SP に更新 |

> 本セッションは計画ドキュメントの編集のみ。実装コミットは IT3 開始日 (2026-08-03) 以降に発生する。

---

## 追記: IT3 実装フェーズ (Ralph Loop 51 反復 → クロージング → post-nav レビュー)

午後セッション。`/ralph-loop:ralph-loop orchestrating-development IT3` を起動して 51 反復消化、終盤の Ralph Loop end-of-life 状態を `/ralph-loop:cancel-ralph` で抜けて、ユーザー差込 (E2E / ナビ / 見積一覧) → `developing-review` → 高優先指摘の即時修正 → ふりかえり → 完了報告書まで一気に走らせた。

## ハイライト

- **本体 3 ストーリー (US07 航海検索 / US08a 経路候補 / US27 通関) を 1 IT で完走** + arch-check Rule 4 ALLOWLIST を 6 → 0 件化
- **ADR 3 件 (0004 ShipperRef / 0005 BC エラー / 0006 ページネーション) 起票**、特に 0006 は提案 → post-nav レビューで「採用」に昇格 → 全 `findAll*` Port に `{-# DEPRECATED #-}` 付与で規律を機械化
- **post-nav マルチパースペクティブレビュー → 高優先 H-01/H-02/H-03 即時修正**: 未認証情報漏洩・開発識別子 (US08a) UI 露出・ADR 自己矛盾を同セッション内で潰した
- **テスト 207 → 300 (+93)**、HPC ゲート 60% → 70%、48 コミット、すべて pre-commit (fourmolu + hlint + arch-check + stack test) を通過

## Skill 実行の時系列 (午後セッション)

### 7. `ralph-loop:ralph-loop orchestrating-development IT3` (51 反復)

反復 1〜41 で本体ストーリー + Try + 高優先指摘 + ストレッチを順次消化。コミット 41 件 (`3919e52d`〜`ad2549ae`)。

反復 42 以降は AI 単独完結タスクが枯渇し no-op が 9 回連続。U-04 (arch Phase 2 / 8h) / U-08 (Playwright / 6h) / U-12 (testcontainers / 3h) / Phase 3 T-01〜T-03 (9h) はいずれも環境依存または大物リファクタで、Ralph 終盤に集中していた。**第 51 反復で `/ralph-loop:cancel-ralph` 発火 → ループ離脱**。

### 8. ユーザー差込: E2E / ナビ / 見積一覧

Ralph Loop 離脱後にユーザーから順次差込:

1. **「playwright e2e テスト追加」** → `apps/cargo-tracker/e2e/src/tests/it3-stories.spec.ts` を新規作成 (US07 該当あり/なし、US08a、US27 正常/HS 5 桁不正の 5 ケース、152 行)
2. **「ダッシュボードからナビゲーションがない。他の一覧表示機能と合わせて直す」** → `Layout.hs` の `menuItemsForRole` を全ロール拡張、`HomeView.hs` に「一覧・検索メニュー」セクション追加、`BookingShowView.hs` に経路候補リンク追加、`LayoutSpec.hs` を実装に追従
3. **「見積もり一覧画面も追加」** → `EstimateRepository.findAllEstimates` を Port に追加 (Postgres `ORDER BY id DESC LIMIT 100`)、`estimateListPage` Lucid view、`GET /estimates` ルート

### 9. `developing-review` (post-nav レビュー)

5 視点並列 (xp-programmer / xp-tester / xp-architect / xp-technical-writer / xp-user-representative) で `ad2549ae..HEAD` の 4 コミットをレビュー。**高 5 / 中 11 / 低 7 件**、矛盾事項なし。重要指摘:

- **H-01** 未認証ナビから業務一覧露出 (情報漏洩)
- **H-02** 「経路候補を見る (US08a)」の開発識別子 UI 露出
- **H-03** ADR-0006 を「提案」のまま `findAllEstimates` を新規追加した自己矛盾
- H-04 `estimateListPage` / `bookingListPage` コピー重複、H-05 `/estimates` の hspec-wai 受入テスト不在

結果を `docs/review/it3_post_nav_review_20260629.md` に保存。

### 10. ホットフィックス: H-01/H-02/H-03 即時対応 (commit `f41fcfc3`)

- H-01: 未認証ナビを `/estimates/new` `/voyages/search` `/login` の 3 項目に縮退
- H-02: ボタン文言を「経路候補を見る」に変更、`data-story="US08a"` 属性に退避
- H-03: ADR-0006 を「採用」に昇格、4 つの `findAll*` Port (Booking/Routing/Shipper/Estimation) に `{-# DEPRECATED #-}` プラグマ付与

LayoutSpec も同期、`stack test` 300/300 pass を維持。

### 11. `orchestrating-project --retrospective`

KPT 形式で `docs/development/retrospective-3.md` を作成。Keep 7 / Problem 9 / Try 15 (T3-01〜T3-15)。3 IT 通算ベロシティ「Ralph Loop 1 日 ≒ 20 SP」を IT4 基準値に設定。

### 12. `orchestrating-project --report`

`docs/development/iteration_report-3.md` 作成 (166 行)。バーンダウン・成功基準対 8 件・主要メトリクス 13 項目・学び 7 件・次ステップ。

## 学び (午後)

- **ADR を「提案」のまま放置するとパターン増殖の温床になる**: ADR-0006 を起票しながら同 IT 内で `findAllEstimates` を旧パターンで実装した自己矛盾は post-nav レビューまで気付かなかった。**ADR 起票時のチェックリストに「同 IT 内で旧パターン新規追加禁止 + DEPRECATED 即時付与」を入れる**ことで予防可能
- **`{-# DEPRECATED #-}` プラグマは「規律の機械化装置」として最強**: GHC が新規 callsite ごとに警告を出すので、コードレビュー頼みの規律から脱却できる。これは `feedback_archunit-and-fulltest` の教訓を Haskell 流儀で実装したもの
- **未認証で何を見せるかは UI 設計フェーズで明示すべき**: H-01 (情報漏洩) は「ロール × メニュー」マトリクスが `ui_design.md` に欠落していた結果。「Nothing ロールに何を見せるか」をストーリー単位で意識的に設計する必要
- **開発識別子 (US08a) の UI 露出は user-representative 観点で必ず捕捉される**: TDD では機能正しさは担保できてもプロダクション UX 観点は出ない。**多視点レビューをルーチン化する価値**
- **Ralph Loop 終盤の no-op 連続は計画段階のタスク分類で減らせる**: 第 42 反復以降の no-op 9 回は「環境依存タスク (Playwright / testcontainers / GitHub Release)」が分散したまま残った結果。**タスク登録時に「AI 完結可 / 環境依存 / 人手作業」を分類すべき**
- **マルチパースペクティブレビューの 2 段運用が IT3 で初めて機能した**: Ralph Loop 中盤の self-review (IT2 マルチパースペクティブ反映) + 完了直後の post-nav レビュー、両者で「持ち越す指摘」と「同反復で潰す指摘」を分離できた ([[feedback_review-two-stage]] が IT3 で本格運用)
- **コピー重複 (estimateListPage / bookingListPage) は Rule of Three を待たず 2 件目で抽出**: SOT の早期確立が、3 重ハードコード (View / SQL / コメント) の連鎖を防ぐ

## 主要コミット (午後 / IT3 実装〜クロージング)

| ハッシュ | コミット |
| :--- | :--- |
| `3919e52d` | fix(booking): H-01 submitBooking のエラー型修正 |
| `b14a5c93` | fix(shared): H-02 IdGenerator の partial 関数を total 化 |
| `4c68ddf5` | feat(booking): H-03 US06 Submit ボタンと SubmitBookingCommand |
| `a0bb89f3` | feat(booking): H-07 BC 固有エラーを Booking.Domain.Error に分離 (ADR-0005) |
| `bb434323` | feat(shared): U-05 ShipperRef VO 導入 + ALLOWLIST 解消 (ADR-0004) |
| `e7f9299f` | ci(coverage): U-06 HPC ゲート 60→70% |
| `62330307`〜`517c3aa2` | feat(routing): US07 航海検索 (Domain/App/HTTP/UI) |
| `e7baa5cc`〜`82e50007` | feat(routing): US08a 経路候補算出 (RouteFinder/hedgehog/HTTP/UI) |
| `c3c75afc` | perf(routing): US08a RouteFinder criterion ベンチ (12.6ms/1000 航海) |
| `0eb4f34f`〜`f3ca90f7` | feat(booking): US27 通関情報 (Domain/migration/App/Repo/UI) |
| `4cdd7b3d` | docs(release): U-10 v0.1.0-alpha リリースノートドラフト + CHANGELOG |
| `00dfce1a` | docs(it3): ADR-0006 ページネーション戦略起票 |
| `64e30ed7` | test(e2e): IT3 ストーリー Playwright E2E 追加 |
| `34ff9c37` | feat(ui): IT3 ストーリーへのナビゲーション導線 |
| `0d3ad5ab` | feat(estimation): 見積一覧ページを追加 |
| `29222ac1` | docs(review): IT3 post-nav マルチパースペクティブレビュー結果 |
| `f41fcfc3` | fix(review): H-01/H-02/H-03 即時対応 (情報漏洩 / 識別子露出 / ADR 昇格 + DEPRECATED) |
| `5dd9614d` | docs(retro): IT3 ふりかえり (KPT) |
| `85ac2a0b` | docs(it3): IT3 完了報告書 |
