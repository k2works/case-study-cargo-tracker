# IT19 ふりかえり（KPT）

期間: 2026-08-12
局面: **出荷と是正**（全 36 US 実装完了後の最初のイテレーション）
スコープ: Release 2.1 の出荷確定 ＋ `ui_design.md` 未達 2 件 ＋ IT18 の Try 5 件
SP: **0**（新規ストーリーなし）

## 結果

| 対象 | 結果 |
| :--- | :--- |
| Release 2.1 の出荷 | **完了**（`java/take-6/v2.1.0`・CHANGELOG・完了報告書） |
| 正典未達（C3 再見積 / C4 絞り込み） | **完了**（Issue #517） |
| Try T1〜T5 | **5 件すべて完了**（T2 以外は検査になった） |
| 返済枠 C2 / C6a / C6b / C6c / C6d | **完了** |
| 計画外（利用者の依頼 2 件） | **完了**（開発環境のシード・ドメインモデルの分割） |

**工数は 11h 超過**（計画 75h に対し実績 91h）。計画外の 2 件（タスク 0 と C7）が 16h。

---

## Keep（続けること）

### K1. 実コードの違反を探してから検査を書いた

ADR-022 の検査は、先に `urgencyClass`（残り 3 日以内は赤、7 日以内は橙）という**実際の違反を見つけてから**書いた。「規則を作って終わり」にならず、書いた瞬間に 1 件返せた。

### K2. 破壊検証で 5 件の空振りを捕まえた

- ラチェットの上限を 5 に下げて内訳が出ることを確認
- 名簿に幽霊テーブルを足して赤を確認
- 委譲を注入だけして呼ばない状態で赤を確認
- 品名の永続化を外して往復テストが赤になることを確認
- 壊れた `@link` を入れて…**検査そのものが走らないことが分かった**（下記 P3）

### K3. 5 視点のレビューが「作った本人には見えない」ものを 11 件出した

とくに利用者視点は、マニュアルの手順が**荷役作業員には 403 になる**ことを権限設定まで読んで確かめていた。書いた側は全ロールの権限を持って読んでいた。

### K4. 分割の代償を隠さず ADR に書き、レビューで訂正した

ADR-024 は「失ったもの」を独立の節に置き、公開したメソッドを表にした。**レビューで「実費は 7 でなく 4（`record` は元から公開）」と指摘され、同じイテレーションの中で訂正した**。誇張したまま残すと、次に同じ判断をする人が過大なコストを見積もる。

---

## Problem（問題）

### P1. 検査を足すイテレーションなのに、その検査自身に穴があった

| 検査 | 穴 | 見つけた人 |
| :--- | :--- | :--- |
| H2 スモークの載せ忘れ | **型単位でしか見ておらず、メソッド 6 件を素通り** | クローズ前レビュー（programmer / tester） |
| ADR-022 の規則の置き場 | **数値リテラルしか拾わず、定数比較は素通り**。移設先の `DeadlineUrgency` がまさにその形 | クローズ前レビュー（programmer） |
| 型名の照合 | 部分一致で、接尾辞が重なる型を見分けられない | クローズ前レビュー（tester） |

**「2 回続けて外したなら方式の問題」と書いて方式を変えたのに、新しい方式の粒度が事故の粒度と合っていなかった。** IT17・IT18 で外したのは「新しいサービス」だったが、次に増えるのは「新しいメソッド」である。

### P2. テストの空振りを 4 件作った

- `contains("120")` は HTML 全体でほぼ常に真（寸法・個数を捨てても緑）
- 再見積リンクのクエリ文字列を誰も見ていない（href を書き換えても緑）
- 貨物種別はセレクトのため `value=` では見えない
- 未知の状態を渡すと「有効で絞る」に化ける（打ち間違いが黙って別の結果に）

**どれも「テストがある」状態だった。**

### P3. ソースを読む検査が、ソースを入力にしていなかった

Javadoc だけを変えるとバイトコードが変わらず `compileJava` が UP-TO-DATE になり、**検査そのものが走らないまま緑**になった。破壊検証をしていなければ気づかなかった。

### P4. 分類を最初に誤り、ADR の主張を自分で打ち消した

ADR-024 は「集約ルートの数が目に見える」ことを利得に挙げながら、**識別子を `aggregates` に同居させたため数が読めなくなり**、本文の「Booking は 4」も誤りだった。`entities` も 8 件中 6 件が record（同一性を持たない）だった。

**参照実装の構成をそのまま写したことが原因である。** 参照実装が `BookingId` を `aggregates` に置いているのを、意味を確かめずに真似た。

### P5. 「名前で記録した」と書きながら、記録が無かった

ADR-024 は 2 か所で「次のイテレーションで ArchUnit ルールに落とす（名前で記録した）」と書いていたが、**`docs/development/` を grep しても 1 件もヒットしなかった**。ADR 本文が最も強い言葉で戒めている形を、その ADR 自身が踏んでいた。

### P6. 計画外が 16h（工数の 18%）

タスク 0（シード投入）と C7（package-info）はどちらも利用者の依頼で、価値はあった。ただし**計画の余白は 5h しかなく**、超過が確定した時点で落とす順序を見直していない。

---

## Try（次に試すこと）

| # | 内容 | 期待する効果 | 由来 |
| :--- | :--- | :--- | :--- |
| **T1** | **検査を書くときは「過去に起きた事故の粒度」で Red を作る** —— 型単位でなくメソッド単位、サービス単位でなく画面単位 | P1 の再発防止。粒度が粗い検査は、次の事故を捕まえない | P1 |
| **T2** | **HTML を見るテストは、描画された形（単位付き・区切り付き）でアサートする** | P2 の再発防止。裸の数字・短い語は HTML 全体でほぼ常に真になる | P2 |
| **T3** | **参照実装を写すときは、1 クラスずつ「なぜそこか」を言えるか確かめる** | P4 の再発防止。構成だけを写すと、その構成が持つ意味が抜ける | P4 |
| **T4** | **ADR に「次 IT へ送る」と書いたら、同じコミットで計画に起票する** | P5 の再発防止。書いた本人が忘れる前提で仕組みにする | P5 |
| **T5** | **計画外を受けたら、その場で落とす順序を見直して記録する** | P6 の再発防止。超過が確定してから見直すのでは遅い | P6 |

---

## 次イテレーションへの引き継ぎ

| # | 内容 | 実測 | 育つか |
| :--- | :--- | :--- | :--- |
| **D5** | 集約ルート以外から `entities` の生成・変更を呼ばない ArchUnit ルール | 4 メソッド・**違反 0 件** | **育つ**（レコードを分けるたびに公開が増える） |
| D1 | `Cargo` が 499 行（上限 500） | 1 クラス・**猶予 1 行** | **育つ**（次に 1 メソッド足すと赤） |
| D2 | 返金の扱い（ADR-018 が「別の業務」で止まっている） | 1 件 | 育たない |
| D3 | 適格請求書（インボイス制度）の要件 | 1 件 | 育たない |
| D4 | `Location.of` が `new Location(...)` に委譲するだけ | 1 件 | 育たない |
| U4 | 見積一覧の既定を「有効」にする | 1 画面 | 育たない |
| U5 | 絞り込み 0 件の理由を分ける（港コードの誤り・作成日の逆転） | 2 件 | 育たない |
| U6 | 見積一覧に件数と並び順を出す | 1 画面 | 育たない |
| U7 | **「有効期限」の語が実際は「希望到着期限」を指している** | 1 語・画面 3 か所 | 育たない（**が、荷主に誤解を与える**） |
| U8 | 期限切れの一覧から一件ずつ開かないと作り直せない | 1 画面 | 育たない |
| P-M2 | `EstimateFilter` が全件取得してメモリで絞る | 1 か所 | 育たない（件数が増えるまで） |

**育つ負債は 2 件（D5・D1）。** どちらも次の IT の先頭に置く。
