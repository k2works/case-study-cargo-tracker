---
title: イテレーション 2 完了報告書
description: IT2（US03・US04・US05、8 SP）の達成状況・技術的成果・品質指標・残作業。
---

# イテレーション 2 完了報告書

対象: US03（法人荷主）・US04（貨物予約）・US05（危険物・冷凍貨物）= 8 SP
期間: 2026-08-20（IT2）
局面: 序盤（アウトサイドイン）の最終イテレーション。ここで Release 0.1 が閉じる。

## エグゼクティブサマリー

営業担当者が**荷主の登録から貨物予約の仮受付までを 1 本で通せる**状態になりました。
危険物・冷凍貨物の特別情報も保存され、経路設計（IT3）が読める形になっています。

IT2 は「基盤が揃った状態で業務価値を積む最初のイテレーション」であり、計画 8 SP を
全量完了しました。IT1 から持ち越した返済枠 10 件もすべて片付けています。

実装の過程で、**検査が 5 件の実欠陥を捕まえました**。とくに次の 2 件は、全テストが緑の
まま本番でだけ壊れる形でした。

- 同時に届いたログイン失敗を取りこぼし、**何度間違えてもロックが成立しない**（US31 の保護が名目だけになる）
- CORS プリフライトが 401 になり、**画面と Gateway を別オリジンに置いた瞬間にログインすらできない**

## 達成状況

| ストーリー | SP | 状態 |
| :--- | :--- | :--- |
| US03 法人荷主を登録する | 1 | 完了（IT2 スコープ内） |
| US04 貨物予約を登録する | 5 | 完了（IT2 スコープ内） |
| US05 危険物・冷凍貨物の予約を登録する | 2 | 完了（IT2 スコープ内） |
| **合計** | **8** | **100%** |

### 満たしていない受入基準

依存先が未実装のため、**IT2 のスコープ外**として正直に記録します。次のイテレーションの
計画時に、この表を入力として使います。

| 受入基準 | 依存先 | 扱い |
| :--- | :--- | :--- |
| US03「登録した法人情報は US22 で参照される」 | US22（IT11） | 参照される側のデータは用意済み |
| US04「経路設計者に予約登録の通知が送信される」 | US06（IT3） | 仮受付での保存までを完了とした |
| US04「見積情報との整合性が確認される」 | US01（IT12） | 見積が無いため確認しようがない |
| US05「経路設計時に対応可能な航海・ルートのみが候補として表示される」 | US08（IT4）・US09（IT5） | 特別情報を保存し、経路設計が読める形にするところまで |

## 技術的成果

### 判断を ADR に落とした（ADR-007〜013）

IT3 以降で決めると既に書いたコードやデータを壊す判断を、着手前に決めて記録しました。

| ADR | 決定 | 決めなければどうなったか |
| :--- | :--- | :--- |
| [007](../adr/007-authenticated-user-header-required.md) | 業務サービスは利用者ヘッダを必須とし、登録漏れをメタテストで落とす | ADR-004 の前提が誰にも確かめられないまま、サービスが 2 → 6 に増える |
| [008](../adr/008-no-user-shipper-link-in-it2.md) | IT2 では利用者と荷主を紐付けず、予約は営業担当者のみ | 絞り込めないまま `ROLE_SHIPPER` に開くと**全荷主の予約が見える** |
| [009](../adr/009-cargo-status-columns-from-the-start.md) | 状態列は最初から NOT NULL、料金列は NULL 許容 | 後から NOT NULL を足すと IT2 の行が読めなくなる／0 を置くと算出漏れが無料の予約として通る |
| [010](../adr/010-location-master-shape.md) | 地点はサロゲート PK + UN/LOCODE の UK、業務タイムゾーン必須 | IT2 の形が IT3 で 4 サービスに複製される |
| [011](../adr/011-booking-id-numbering.md) | 予約番号は DB シーケンスで `BKG-YYYYNNNNNN`、追跡番号とは別 | 5 サービスが論理参照するキーで、後から変えられない |
| [012](../adr/012-value-object-granularity.md) | 値オブジェクトは不変条件を持つ属性にだけ、荷主は単一クラス | 正典と実装が割れたまま集約が増える |
| [013](../adr/013-frontend-pages-and-features.md) | `pages/` は画面の入口、`features/` は業務単位 | 画面を増やしてから決めると決め直しの対象が増える |

### 検査が捕まえた実欠陥

| # | 欠陥 | どの検査が捕まえたか |
| :--- | :--- | :--- |
| 1 | 同時に届いたログイン失敗を取りこぼし、ロックが成立しない | 並列 5 リクエストの結合テスト（先に赤を確認） |
| 2 | CORS プリフライトが 401 になる | Gateway フィルタのユニットテスト（通過を外すと赤） |
| 3 | `DEFAULT` を制約より後ろに置いた SQL が H2 で解釈できない | 方言スモーク（**本番では通り、ローカルだけ落ちた**） |
| 4 | マッパーが増えて短い名前が衝突し、方言スモーク自体が落ちる | 方言スモーク（共有側の型の扱いを修正） |
| 5 | 荷主ダッシュボードのリンクが 403 になる画面を指す | ロール別到達性のテスト（新設・壊すと赤） |
| 6 | 起動プローブの `timeoutSeconds` が既定の 1 秒で、**正常に起動しているコンテナを「異常」と判定**していた | kind への配置（IT1 の「ヘルスチェックを一律の防御から外す」と同じ family） |
| 7 | SonarQube の品質ゲートが**一度も動いていなかった**（照会先のプロジェクトキーがスキャン時と食い違い、curl の失敗として出るだけ）。加えて 1 プロジェクトしか見ていなかった | クローズ作業での実行 |
| 8 | E2E がログインの完了を待たず、**認証されていない状態を「403 だ」と取り違えて**いた | CI（ローカルより遅い環境で露見） |

### 実装の要点

- **予約番号の組み立てをマイグレーションに置いた**。アプリ側に置くと、別の経路（移行・運用スクリプト）が違う形式を発行できてしまう
- **到着期限を目的地の暦で判断する**。UTC で判断すると、時差の分だけ受付が拒否される時間帯ができる。可変 Clock で日付境界をまたぐ振る舞いまで検証した
- **付け忘れと同じく付けすぎも拒む**。一般貨物に危険物申告や温度条件を付けられない。経路設計（IT3）が扱いを判断できなくなるため
- **料金は 0 で埋めない**。未算出と無料が区別できなくなり、算出漏れが「無料の予約」として通る

## 品質指標

| 項目 | 結果 |
| :--- | :--- |
| `./gradlew build`（ユニット・統合・ArchUnit・カバレッジ検証） | 緑 |
| `TZ=UTC ./gradlew test` | 緑 |
| フロントエンドのユニット・コンポーネントテスト | 94 件すべて緑 |
| フロントエンドの lint（oxlint） | 警告 0 |
| E2E（モック） | 19 件すべて緑 |
| **E2E（実バックエンド）** | 緑（`npm run test:e2e:real` を新設） |
| 本番相当ビルドの検査（`test:e2e:production`） | 緑 |
| **CI（4 ジョブ）** | **緑**（実行 32333797229） |
| 追加した検査を壊して赤になることの確認 | 全件実施 |
| **kind 統合環境（Ingress → Gateway → bookingms → PostgreSQL）** | 緑（法人荷主の登録・冷凍貨物の予約・荷主ロールの 403・未認証の 401） |
| Bug / Vulnerability（SonarQube） | **0 / 0** |
| 重複率 | 0.0%（閾値 3%） |

## レビュー結果

正式なマルチパースペクティブレビュー（`developing-review`）はクローズ作業の一部として
実施します。本報告書の時点では、実装中の自己検査で 5 件の実欠陥を検出・修正済みです。

## 課題と残作業

| # | 内容 | 扱い |
| :--- | :--- | :--- |
| 1 | `Shipper.email` が `String` のまま（[ADR-012](../adr/012-value-object-granularity.md) で明記） | 荷主編集のストーリーに着手するとき |
| 2 | 公開追跡経路（`/api/v1/public/tracking/*`）とポータルの追跡番号欄 | US18（IT6） |
| 3 | 予約詳細画面（`/booking/:bookingId`） | 経路・配送状況がそろう IT3 以降 |
| 4 | `location` の複製の同期方法 | IT3 の着手時（ADR-010 で先送り） |
| 5 | サービス間の呼び出しで利用者ヘッダをどう引き継ぐか | IT3 の ACL 実装前（ADR-007） |

## ユーザーマニュアル

画面を伴うイテレーションのため更新しました。

- 「[04 貨物予約](../manual/04-貨物予約.md)」を新設（登録・一覧・危険物と冷凍の扱い）
- 「[03 荷主管理](../manual/03-荷主管理.md)」に法人契約情報の節（3.4）を追加
- 「[01 業務フロー](../manual/01-業務フロー.md)」の工程 3 を「使えます」に更新（検査が赤になるため忘れられない）
- 画面キャプチャを生成 spec 経由で再生成し、目視で確認

## 次イテレーションへの引き継ぎ

[ふりかえり](retrospective-2.md#次イテレーションit3への引き継ぎ)を正典とします。要点は次のとおりです。

- **IT3 の着手前に決めること 2 件**（地点の複製の同期方法・サービス間のヘッダ引き継ぎ）
- **起票した項目 2 件**（荷主の編集・共用端末の無操作タイムアウト）
- **Try 6 件**（置換後を読む・開発サーバの鮮度確認・自前の拒否メッセージ・E2E のデータ自給・モックにも規則・免除一覧の検査）

## 更新履歴

| 日付 | 内容 |
| :--- | :--- |
| 2026-08-20 | 初版作成 |
