---
title: イテレーション 9 完了報告書
description: US29・US30（10 SP）と返済枠 14 件の完了報告。実績・品質指標・レビュー結果・残作業。
---

# イテレーション 9 完了報告書

日付: 2026-08-25
対象: IT9（US29 通関申告の登録・管理／US30 輸送中の予約キャンセル承認）
計画 SP: 10 ／ 実績 SP: 10（達成率 100%）

## エグゼクティブサマリー

**通関という関門が、引取の前に立った。** IT7 で引取を実装して以来、通関書類の確認は
現場の目視に委ねられ、システム側の歯止めは「荷受人の確認」欄だけだった。IT9 では
通関申告を登録・管理する画面と、**通関済でなければ引取を断るガード**が入っている。
申告が 1 件も無い貨物も断る——申告が無いのは「確かめなくてよい」ではなく「通関済では
ない」からである。

あわせて **輸送中の予約に承認フローが入った**。輸送を始めた貨物のキャンセルは、陸揚げ地を
決めずに確定させると、貨物が行き先を失ったまま船に乗り続ける。追跡管理者が陸揚げ地を
指定して承認する経路を作り、**承認を迂回して取消へ落ちる辺が無いこと**を検査で固定した。

**返済枠は 14 / 14 完了**（3 IT 連続で全返済）。

本 IT の特徴は、**サービス単体のテストが全部緑のまま、画面と実環境でだけ成り立たない**
欠陥が 5 件出たことである。3 件は実環境で通す枠（Phase 6）とクローズの作業で、
**2 件は遅れて届いた 2 視点のレビュー**が捕まえた。

> **クローズの宣言が早すぎた。** レビューの 5 エージェントから応答が返らなかったため
> 単一レビュアーで通してクローズを完了としたが、その後 2 視点の結果が届き、そこに
> **受入基準の未達（US29-6）と実欠陥（陸揚げ候補）**が入っていた。どちらも本 IT 中に
> 対応している。「エージェントが返らない」は「結果が無い」ではなく「まだ来ていない」
> だった（[ふりかえり](retrospective-9.md) Problem 4）。

## 達成状況

### ユーザーストーリー

| US | 内容 | SP | 状態 |
| :--- | :--- | :--- | :--- |
| US29 | 通関申告を登録・管理する | 5 | 完了（US29-4 の通知は**代替**）。**US29-6 はクローズ間際まで未達だった**——遅着レビューが指摘し、本 IT 中に対応 |
| US30 | 輸送中の予約キャンセルを承認する | 5 | 完了。ただし **US30-10（履歴）は一部未達**（最新 1 件しか見えない。IT10 へ）。US30-6・US30-7 の通知は**代替**、US30-9 のキャンセル料は US21 |

### スコープ外にしたもの（理由つき）

- **メールの送信**（US29-4・US30-6・US30-7）。**代替**として、送られていないことを画面が
  言う。書かないと担当者は「更新したから相手に届いた」と受け取って連絡をせず、荷主は
  引き取りにも来ない。**受入基準は満たしたと数えているが、仕組みで埋めていない部分である**
- **キャンセル料の算定**（US30-9）。billingms に受け口が無い。Release 2.0 の US21 に含める
- **陸揚げの作業指示**（US30-6・[ADR-025](../adr/025-customs-declaration-and-cancellation-approval.md) 決定 5）。
  荷役は実績を記録するモデルで予定を持たない。**陸揚げ地が決まり、荷役の担当者がそれを
  見られる**までを IT9 の範囲とし、承認した追跡管理者から連絡する運用ルールを添えた

## 技術的成果

- **handlingms が通関の集約を持った。** `CustomsDeclaration`（申告番号・状態・履歴）と、
  状態変更を記録する監査の履歴。**未決着の申告は貨物あたり高々 1 件**を不変条件にし、
  「最新の 1 件を暗黙に選ぶ」実装にしていない
- **通関ガードを荷受人の確認より先に置いた。** 順序が入れ替わっていないことを検査で固定
  している——後に置くと、確認を入れさえすれば通関前でも通る
- **handlingms が 2 種類目のイベントを発行した。** `CustomsStatusChanged` は荷役の交換機に
  相乗りする（ルーティングキーを 1 本足す）。**交換機の引数は既存環境では宣言し直せない**
  ため、3 サービスが同じ引数で宣言していることを検査する
- **bookingms が初めてイベントの購読側に回った。** 荷役の積込・引取で予約が輸送中・配送完了へ
  進む。**巻き戻さない・キャンセル済みは動かない・冪等**を集約の述語で決めている
- **trackingms が留置を例外として自動起票する。** 手で起票できるのは DELAY / DAMAGE / LOST
  だけという [ADR-024] 決定 11 の前提を守り、仕組みが検知する種別は別の入口から入る
- **返済枠 14 件。** 0.1 の設計突合検査は、置いた直後に 13 件の実ドリフトを指した

## 品質指標

| 指標 | 実績 | 目標 |
| :--- | :--- | :--- |
| バックエンドのテスト | 1,087 件（全緑） | 全緑 |
| フロントエンドのテスト | 397 件（全緑） | 全緑 |
| E2E（モック） | 58 件（全緑） | 全緑 |
| E2E（実バックエンド・kind） | 16 件（全緑） | 全緑 |
| バックエンドのカバレッジ | 91.4% | 80% |
| ドメイン層のカバレッジ | 91.9〜100%（6 サービス + shared） | 90% |
| フロントエンドのカバレッジ | 88.9% | 80% |
| SonarQube Quality Gate | **PASS**（バックエンド・フロントエンドとも） | PASS |
| Bug / Vulnerability | 0 / 0 | 0 / 0 |
| 重複率 | 0.1%（バックエンド）／ 1.0%（フロントエンド） | 3% 未満 |
| CI | 緑（run 32804555585） | 緑 |
| `TZ=UTC` での再実行 | 緑 | 緑 |

## レビュー結果

[イテレーション 9 レビュー](../review/イテレーション9_review_20260825.md)。高 7 件・中 7 件は
すべて対応済み、低・中の 9 件は方針を明記して IT10 へ送った。

**5 視点のうち 2 つが遅れて届いた。** 単一レビュアーで通した後に user-representative と
tester の結果が到着し、そこに高 2 件（上の 6・7）が含まれていた——**単一レビュアーでは
見つけられなかったもの**である。programmer / architect / technical-writer は応答が無いまま
であり、その分の見落としは残っている。

### 高優先度の 7 件

1. **営業の画面にキャンセル申請の入口が出なかった。** 集約に述語があるのに応答の操作一覧に
   載せておらず、モックだけがその操作を返していた。API を直接叩く受け入れテストは 201 を
   返すので緑になる
2. **`/api/v1/cancellations` が Gateway のどこにも振られていなかった。** 画面からは 404、
   サービス側は無実。検査を置いたところ、**本番プロファイルでは公開追跡（荷主の唯一の
   入口）とロック解除も同じ状態**だと分かった
3. **E2E が通関ガードの手前で止まり、「荷受人の確認」の守りを踏まなくなっていた**
4. **型検査が何も見ていなかった。** `tsc --noEmit` はプロジェクト参照構成では素通りする
5. **空リストに対する述語アサート**（常に真になる）
6. **US29-6 が未達だった**（遅着分） — `GET /api/v1/customs/overdue` も `useOverdueCustoms()`
   も用意されているのに、どこからも呼ばれていなかった。留置の長期化に気づく手段が
   「1 日 1 回一覧を確認する」だけになっており、**それは仕組みではなく心がけ**である。
   追跡管理者のパネルに件数バナーを置いた（承認待ちのキャンセル件数も同時に）
7. **陸揚げ候補に通過済みの港が残っていた**（遅着分） — 「香港なら近い」と選んで承認すると、
   **船が二度と寄らない港で荷降しする約束**を荷主にする。最後の荷役より後に荷降しする
   区間だけを候補にした

## 課題と残作業

| # | 内容 | 扱い |
| :--- | :--- | :--- |
| 1 | 通関一覧が「毎朝の待ち行列」になっていない（未決着を同時に見られない・表示件数と 200 件の切り捨てが出ない） | IT10 |
| 1-b | **US30-10 の履歴が最新 1 件しか見えない**（`decidedBy` / `decidedAt` は型にあるのに画面に出ていない）。却下後に再申請すると前回の却下理由が消える | **IT10。受入基準の一部未達として記録する** |
| 2 | US33（荷主が自社の貨物を追跡する）の配置 | IT9 で起票（[Issue #555](https://github.com/k2works/case-study-cargo-tracker/issues/555)）。リリースへの配置は次の見直し |
| 3 | 通知は依然として代替 | Release 2.0 以降 |
| 4 | キャンセル料の算定 | US21（Release 2.0） |
| 5 | 陸揚げの作業指示は自動で作られない | 運用ルールで補う（ADR-025 決定 5） |

## ユーザーマニュアル

**UI に変更があったため更新している。** [10 通関管理](../manual/10-通関管理.md)と
[11 キャンセル](../manual/11-キャンセル.md)を新設し、[08 荷役管理](../manual/08-荷役管理.md)に
通関ガードの説明を加えた。画面キャプチャは生成 spec 経由で撮り直している（手で差し替えて
いない）。索引・ナビゲーション・キャプチャの有無は検査に入っている。

## 次イテレーションへの引き継ぎ

[IT9 ふりかえり](retrospective-9.md)の Try 6 件と引き継ぎ 7 件を参照。特に次の 4 つは
IT10 の DoD に入れる。

1. **応答の一覧に依存する画面は、画面から踏む受け入れテストを 1 本持つ**
2. **検査がリポジトリのファイルを読むなら、その場で入力に宣言し、壊して赤を確認する**
3. **新しい検査コマンドを使い始めたら、一度壊して赤になることを確かめる**
4. **受入基準の項目ごとに、達成を確かめる検査を 1 本持つ**——US29-6 は書いてありチェックも
   付けたが、確かめる検査が無く未達のまま残った
