apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookingms
  namespace: cargo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: bookingms
  template:
    metadata:
      labels:
        app: bookingms
    spec:
      containers:
        - name: bookingms
          image: cargo-bookingms:0.0.1
          ports:
            - containerPort: 8082
          env:
            - name: PORT
              value: "8082"
            - name: DB_URL
              value: jdbc:postgresql://postgres:5432/booking_db
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef: { name: cargo-secrets, key: DB_USERNAME }
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef: { name: cargo-secrets, key: DB_PASSWORD }
            # 経路の再照会先（ADR-019）。既定値は開発機を指すため、クラスタでは必ず上書きする。
            # 入れ忘れても起動は成功し、経路を確定する瞬間にだけ
            # 「いま経路を確認できません」に化ける（IT6 の kind 往復で実際に踏んだ）
            - name: APP_ROUTING_SERVICE_BASE_URL
              value: http://routingms:8083
            - name: RABBITMQ_HOST
              value: rabbitmq
            - name: RABBITMQ_PORT
              value: "5672"
            - name: RABBITMQ_USERNAME
              valueFrom:
                secretKeyRef: { name: cargo-secrets, key: RABBITMQ_USERNAME }
            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef: { name: cargo-secrets, key: RABBITMQ_PASSWORD }
          # DB / ブローカーの起動待ちは readinessProbe の範囲で復帰させる（起動順序制御は作り込まない）
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8082
            initialDelaySeconds: 20
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 30
          # 起動が終わるまで liveness を働かせない。10 サービスが一斉に立ち上がる環境では
          # Spring Boot の初期化が liveness の猶予を超え、正常に起動中のコンテナが
          # 殺されて再起動ループに入る（実際に発生した）。
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            periodSeconds: 10
            # 既定の 1 秒では、負荷のかかった作業機で JVM が応答しきれず、
            # 正常に起動しているコンテナが「起動できていない」と判定される
            timeoutSeconds: 5
            # 最大 10 分待つ。起動しないコンテナはここで初めて再起動される
            failureThreshold: 60
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8082
            periodSeconds: 30
            failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: bookingms
  namespace: cargo
spec:
  selector:
    app: bookingms
  ports:
    - port: 8082
      targetPort: 8082
