# 開発・ローカル検証専用のデフォルト値。共有環境では必ず上書きする（手順書「5. 機密管理」参照）
apiVersion: v1
kind: Secret
metadata:
  name: cargo-secret
type: Opaque
stringData:
  POSTGRES_USER: cargo_tracker
  POSTGRES_PASSWORD: dev-only-password
  PLAY_HTTP_SECRET_KEY: dev-only-secret-key-please-override
