/**
 * lint-staged 設定。
 *
 * コマンドを「関数」で返しているのは、lint-staged が既定でステージ済みファイル名を
 * コマンド末尾に追記するためである。Gradle はファイル名を引数に取れないので、
 * 関数形式で固定のコマンド文字列を返して追記を抑止する。
 *
 * Markdown は対象にしない。既存ドキュメントが markdownlint の既定ルール
 * （MD013 行長・MD060 表スタイル）に多数抵触しており、対象にすると
 * ドキュメントを触るすべてのコミットが止まるためである。
 * ルールセットを整備してから導入する。
 */
export default {
  // Java の変更時のみ静的解析を実行する。
  // 実行はプロジェクト全体に対して行う（Checkstyle / SpotBugs は
  // ファイル単位ではなくコンパイル済みクラス単位で解析するため）。
  'apps/cargo-tracker/**/*.java': () => 'npm run app:lint --silent',
};
