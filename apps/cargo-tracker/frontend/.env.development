# ローカルの開発サーバ（npm run dev / npx gulp dev:frontend）だけに効く。
# 本番相当のビルド（vite build。mode=production）はこのファイルを読まない。
#
# 動作確認用の利用者を事前入力する（ADR-0004）。既定は無効で、ここと
# k8s のイメージビルドの 2 箇所だけが明示的に有効化する。
VITE_DEMO_LOGIN_ENABLED=true
