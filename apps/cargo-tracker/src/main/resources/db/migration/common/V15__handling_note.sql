-- 担当者メモ（IT7 レビュー H1 / US16）。
--
-- 引取確認の画面は「予約の荷受人と異なる場合は担当者メモに理由を記入してください」と
-- 案内していたが、**メモ欄そのものが存在しなかった**。作業員は案内どおりに書こうとして
-- 書く場所を探し、見つからず、そのまま登録する。結果、**なぜ別人が受け取ったのかが
-- どこにも残らない**。
--
-- 代理受領（倉庫の従業員・運送会社のドライバーが受け取る）は実務で日常的に起きる。
-- 拒否しない以上、理由を残す場所は要る。
--
-- 引取に限らず記録できる列とする。予定と違う場所での受領、投機的な登録など、
-- 「なぜそうしたか」を残したい場面は引取だけではない。

ALTER TABLE handling_activity
    ADD COLUMN note VARCHAR(500);
