<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cargotracker.shipper.infrastructure.repositories.ShipperMapper">

    <resultMap id="shipperResultMap" type="com.example.cargotracker.shipper.infrastructure.repositories.ShipperRecord">
        <constructor>
            <idArg column="id" javaType="java.util.UUID"/>
            <arg column="name" javaType="java.lang.String"/>
            <arg column="email" javaType="java.lang.String"/>
            <arg column="phone" javaType="java.lang.String"/>
            <arg column="address" javaType="java.lang.String"/>
            <arg column="category" javaType="java.lang.String"/>
            <arg column="contract_number" javaType="java.lang.String"/>
            <arg column="discount_rate" javaType="java.math.BigDecimal"/>
            <arg column="created_at" javaType="java.time.LocalDateTime"/>
            <arg column="updated_at" javaType="java.time.LocalDateTime"/>
        </constructor>
    </resultMap>

    <insert id="insert">
        INSERT INTO shippers (
            id, name, email, phone, address, category,
            contract_number, discount_rate, created_at, updated_at
        ) VALUES (
            #{record.id}, #{record.name}, #{record.email}, #{record.phone},
            #{record.address}, #{record.category},
            #{record.contractNumber}, #{record.discountRate},
            #{record.createdAt}, #{record.updatedAt}
        )
    </insert>

    <select id="findById" resultMap="shipperResultMap">
        SELECT id, name, email, phone, address, category,
               contract_number, discount_rate, created_at, updated_at
        FROM shippers
        WHERE id = #{id}
    </select>

    <select id="findByEmail" resultMap="shipperResultMap">
        SELECT id, name, email, phone, address, category,
               contract_number, discount_rate, created_at, updated_at
        FROM shippers
        WHERE email = #{email}
    </select>

</mapper>
