<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cargotracker.tracking.infrastructure.repositories.TrackingMapper">

    <insert id="insert">
        INSERT INTO tracking_numbers (tracking_number, booking_id)
        VALUES (#{trackingNumber}, #{bookingId})
    </insert>

    <select id="findByTrackingNumber"
            resultType="com.example.cargotracker.tracking.infrastructure.repositories.TrackingRecord">
        SELECT tracking_number, booking_id
        FROM tracking_numbers
        WHERE tracking_number = #{trackingNumber}
    </select>

    <select id="findByBookingId"
            resultType="com.example.cargotracker.tracking.infrastructure.repositories.TrackingRecord">
        SELECT tracking_number, booking_id
        FROM tracking_numbers
        WHERE booking_id = #{bookingId}
    </select>

    <select id="findHandlingEventsByTrackingNumber"
            resultType="com.example.cargotracker.tracking.infrastructure.repositories.HandlingEventRecord">
        SELECT he.completion_time, he.location_code, he.event_type, he.memo
        FROM handling_events he
        INNER JOIN tracking_numbers tn ON he.booking_id = tn.booking_id::uuid
        WHERE tn.tracking_number = #{trackingNumber}
        ORDER BY he.completion_time DESC, he.id DESC
    </select>

</mapper>
