<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.cargotracker.handling.infrastructure.persistence.HandlingActivityMapper">

    <!--
      追記の表なので、リプレイで増えないのは主キーの働きによる。主キーは
      活動 ID（クライアントが作る冪等キー）で、再配送も読み直しも同じ行に落ちる。
      内容は書き換えない——同じ記録の内容は変わらない。
    -->
    <insert id="insert">
        INSERT INTO handling_activity (
            activity_id, tracking_number, booking_id, handling_type, unlocode,
            voyage_number, off_route, operator, completed_at, projected_at
        ) VALUES (
            #{activityId}, #{trackingNumber}, #{bookingId}, #{handlingType}, #{unlocode},
            #{voyageNumber}, #{offRoute}, #{operator}, #{completedAt}, #{projectedAt}
        )
        ON CONFLICT (activity_id) DO NOTHING
    </insert>

</mapper>
