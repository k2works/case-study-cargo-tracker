enforce_dependencies: true
enforce_privacy: true # パック外からは app/public の公開 API のみ参照可
dependencies:
  - "." # ApplicationRecord（共通基盤）
  - "packs/booking" # 貨物特定（追跡番号）・状態同期は公開 API（Booking::Public::CargoBookingService）経由
