/**
 * 認証コンテキストのドメインモデル。集約ルート・値オブジェクト・列挙を置く。
 *
 * <p><strong>一貫性の境界である。</strong> 1 回の取引で守りきる範囲がここで決まる。
 * 集約ルートは {@link com.example.authms.domain.model.User} であり、
 * 連続失敗によるロックの判断（US31）もここが持つ。
 *
 * <p><strong>永続化された行からの復元では業務的な検査を行わない。</strong>
 * 不変条件を後から足すと、その規則が無かったころの行が読めなくなる。
 * 検査は新規に受け入れるときだけ行う。
 *
 * <p>フレームワークに依存してはならない（ArchUnit が検証する）。日時の判断は
 * 注入された {@code Clock} を使い、業務タイムゾーンで行う。
 */
package com.example.authms.domain.model;
