/**
 * 共有カーネル。全バウンデッドコンテキストが同じ意味で共有する型だけを置く。
 *
 * <p><strong>置いてよいのは「複数の BC が同じ言葉で同じ意味を持つもの」に限る。</strong>
 * 業務概念（貨物・航海・請求）は BC ごとに意味が違うため置かない。便利だからという理由で
 * 置き始めると、BC の独立性が静かに失われる。
 *
 * <p>現在の内容は次の 2 つである。
 *
 * <ul>
 *   <li>{@code domain.model} — 全 BC が同じ定義で扱う値オブジェクト（地点）</li>
 *   <li>{@code auth} — Gateway と全サービスの間のヘッダ契約とロール定義（ADR-004）</li>
 * </ul>
 */
package com.example.shared;
