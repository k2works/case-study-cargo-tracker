/**
 * JWT の検証と、検証済みクレームの転送。
 *
 * <p>公開経路（認証不要）の判定は名簿方式であり、<strong>一覧に無い経路は必ず認証を要求する</strong>。
 * 「載っていないものを通す」向きにすると、載せ忘れた経路ほど無防備になる。
 * 公開する業務 API は {@code /api/v1/public/} 配下に分け、業務 API と接頭辞を共有しない。
 *
 * <p>利用者が自分で名乗ったクレームヘッダは、公開経路であっても必ず剥がす。
 * 下流のサービスは署名を見ないため、残すと認証なしで管理者を名乗れる。
 *
 * <p>ヘルスチェック（{@code /actuator/health} 配下）は認証を要求しない。probe は認証情報を
 * 持てないため、一律に適用すると正常なサービスが再起動ループに入る。
 */
package com.example.gatewayms.security;
