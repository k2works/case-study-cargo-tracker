/**
 * API Gateway（gatewayms）。外部からの入口を 1 つに集約する。
 *
 * <p>経路の振り分けと、<strong>JWT の署名検証</strong>を責務とする（ADR-004）。
 * 検証をここに一元化することで、鍵を持つのは発行側の authms と検証側の本サービスだけになり、
 * 配布とローテーションが 2 箇所で済む。
 *
 * <p>認可（どのロールが何をしてよいか）は各サービスが持つ。業務的な認可はドメイン知識を
 * 要するため、Gateway に置くと二重管理になる。
 *
 * <p><strong>本サービスはセキュリティ境界である。</strong> ここを通らない経路が存在しないことは
 * ネットワーク層（kind の Service / Heroku のアプリ分離 / VPC）が担保する前提であり、
 * 変更時には破壊検証の実行を必須とする。
 */
package com.example.gatewayms;
