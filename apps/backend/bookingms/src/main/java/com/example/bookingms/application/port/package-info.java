/**
 * 予約コンテキストの出力ポート。
 *
 * <p>ユースケースが外側に求めることをインターフェースとして宣言する。実装は
 * {@code infrastructure} に置く（依存性逆転）。
 *
 * <p>ここに置くのは「何が必要か」であって「どう実現するか」ではない。
 * DB・メッセージング・外部 API といった手段の名前をインターフェース名に出さない。
 */
package com.example.bookingms.application.port;
